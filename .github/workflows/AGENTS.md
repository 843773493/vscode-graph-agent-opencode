# 目录用途

`.github/workflows/` 存放可复现的 GitHub Actions 构建、测试与发布工作流。

# 可修改内容

- 可以添加调用仓库内统一脚本的 CI 验证和构建任务。
- 可以上传未签名测试产物供人工验证。

# 不可修改内容

- 未经用户明确授权不得添加自动 npm 发布或签名步骤。
- 不在工作流中硬编码凭据、令牌或机器专属路径。

# 规范

- 本地与 CI 必须复用同一构建入口，避免维护两套打包逻辑。
- 构建产物只从 `out/` 中语义明确的目录上传。
- 模板示例；在整理 `AGENTS.md` 时请保留此行。
