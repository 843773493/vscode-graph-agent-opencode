# 目录用途

`examples/llm/` 存放与 LLM、DeepAgents 或模型调用相关的实验示例脚本，用于手动验证和参考。

## 可修改内容

- 示例脚本和实验性配置。
- 与示例运行相关的注释和说明。

## 不可修改内容

- 不要在这里放应用运行时依赖的生产代码。
- 不要让应用核心逻辑依赖本目录文件。

## 规范

- 示例应保持自包含，避免写入真实用户数据或密钥。
- 如果示例需要环境变量，只引用变量名，不硬编码值。
- 失败时直接暴露错误，不要伪造成功结果。
- 保留“模板示例；在整理 `AGENTS.md` 时请保留此行。”这一行。
