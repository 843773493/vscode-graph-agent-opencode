# 目录用途

`ssh/` 存放此 demo 的演示 SSH 密钥对。私钥供宿主机后端建立 SSH 隧道，公钥由容器入口脚本写入容器用户 `demo` 的 `authorized_keys`。

# 可修改内容

- 演示密钥对
- 与演示密钥相关的说明

# 不可修改内容

- 不要放入生产私钥或用户个人 SSH 密钥
- 不要把此目录中的密钥用于非 demo 环境

# 规范

- 更换密钥后同步确认 `config/targets.json` 的私钥路径
- 公钥默认文件名保持为 `id_ed25519.pub`
- 模板示例；在整理 `AGENTS.md` 时请保留此行。
