# 目录用途

`src/client/` 存放 demo 的浏览器前端，用于选择 attach 目标、查看和编辑目标后端管理的 txt 文件。

# 可修改内容

- 原生 HTML/CSS/JS 前端
- attach、刷新和保存交互

# 不可修改内容

- 不要在前端伪造保存成功状态
- 不要把业务状态作为前端唯一真实来源

# 规范

- 保存成功后用后端返回的完整 file 快照替换页面状态
- 错误信息必须展示给用户
- 模板示例；在整理 `AGENTS.md` 时请保留此行。
