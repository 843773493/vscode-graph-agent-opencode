# 目录用途

`src/` 存放 demo 的前端和后端源码。

# 可修改内容

- `client/` 浏览器前端
- `server/` 本地后端、SSH attach 和 txt 文件 API

# 不可修改内容

- 不要在这里引入主应用生产逻辑
- 不要把运行时 txt 文件写到源码目录

# 规范

- JavaScript 使用 ESM
- 前端状态以后端返回对象为准
- 模板示例；在整理 `AGENTS.md` 时请保留此行。
