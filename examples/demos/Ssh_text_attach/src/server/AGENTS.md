# 目录用途

`src/server/` 实现 demo 后端：静态前端服务、txt 文件管理 API、attach 目标配置解析和 SSH 隧道代理。

# 可修改内容

- HTTP API
- 文件存储逻辑
- SSH 隧道管理
- 配置解析与校验

# 不可修改内容

- 不要吞掉远程连接错误
- 不要返回虚假的默认文件内容

# 规范

- 端口默认后端 `7910`、前端 `7911`
- 文件默认写入 demo 根目录下的 `.boxteam/`
- 模板示例；在整理 `AGENTS.md` 时请保留此行。
