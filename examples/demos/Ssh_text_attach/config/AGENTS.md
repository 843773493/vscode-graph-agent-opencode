# 目录用途

`config/` 存放 demo 的 attach 目标配置，包括本地后端和 SSH 远程后端。

# 可修改内容

- `targets.json` 中的目标名称、SSH 主机、端口、用户、私钥路径和远程后端端口

# 不可修改内容

- 不要在配置中写入生产密钥内容
- 不要把运行时文件内容放进配置文件

# 规范

- 路径默认相对 demo 根目录解析
- SSH 私钥路径应指向 `ssh/` 下的演示密钥或用户明确提供的绝对路径
- 模板示例；在整理 `AGENTS.md` 时请保留此行。
