# 目录用途

`docker/` 存放容器启动脚本，用于把 demo 公钥写入容器用户的 `authorized_keys` 并启动 SSH 服务。

# 可修改内容

- 容器入口脚本
- SSH 服务启动参数

# 不可修改内容

- 不要在镜像里写入生产私钥
- 不要开启密码登录作为默认行为

# 规范

- 容器启动失败时必须直接退出
- 默认从挂载的 demo 目录读取公钥
- 模板示例；在整理 `AGENTS.md` 时请保留此行。
