# 目录用途

`tests/` 存放 Playwright 远程屏幕实验项目的协议和命令解析测试。

# 可修改内容

- Bun 测试文件
- 只依赖实验项目公开函数的轻量单元测试

# 不可修改内容

- 不要在测试中启动主应用服务
- 不要把真实浏览器下载或系统环境配置写死到测试里

# 规范

- 测试应聚焦消息校验、命令解析和 URL 规范化
- 失败时直接暴露断言错误
- 模板示例；在整理 `AGENTS.md` 时请保留此行。
