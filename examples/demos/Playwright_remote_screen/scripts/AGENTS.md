# 目录用途

`scripts/` 存放 Playwright 远程屏幕实验项目的本地开发脚本。

# 可修改内容

- 检查、测试和辅助运行脚本
- 仅服务于当前实验项目的 Node.js 工具脚本

# 不可修改内容

- 不要在这里接入主应用构建流程
- 不要写入实验项目之外的文件

# 规范

- 脚本使用 ESM
- 失败时直接退出非零状态
- 模板示例；在整理 `AGENTS.md` 时请保留此行。
