{
  "name": "interactive-terminal-prototype",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "node src/server/server.js --terminal-cwd ../../..",
    "dev": "node src/server/server.js --terminal-cwd ../../..",
    "test": "bun test",
    "check": "node --check src/server/server.js && node --check src/server/terminalManager.js && node --check src/server/protocol.js && node --check src/client/main.js"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^1.1.0",
    "ws": "^8.18.0"
  }
}
