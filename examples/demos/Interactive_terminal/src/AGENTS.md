# 目录用途

`src/` 存放交互式终端原型的服务端和浏览器端源码。

# 可修改内容

- `server/` 下的 Node 服务与终端管理逻辑
- `client/` 下的浏览器页面、交互和样式
- 原型内部共享的轻量源码

# 不可修改内容

- 不要在这里引入主应用生产状态管理
- 不要依赖 VS Code 扩展宿主

# 规范

- JavaScript 使用 ESM
- 终端错误必须显式抛出或发送错误事件
- 浏览器端只保存展示状态，终端进程状态由服务端持有
- 模板示例；在整理 `AGENTS.md` 时请保留此行。
