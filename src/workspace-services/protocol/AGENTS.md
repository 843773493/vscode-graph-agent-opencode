# 目录用途

存放 Terminal Manager 和 Browser Manager 共用的 Node.js Protobuf 生成绑定。

## 可修改内容

- 可以通过协议生成命令更新 JavaScript 与 TypeScript 声明绑定。

## 不可修改内容

- 不放主 Web 客户端代码或 Python 业务逻辑。

## 规范

- 生成代码必须使用 ESM import，并与服务运行时的 Node 版本兼容。
