# 目录用途

保存浏览器端未发送的 Composer 文本草稿，并按工作区和会话隔离。

# 可修改内容

- 草稿 localStorage 适配与作用域键处理。
- 草稿数量和文本大小限制。

# 不可修改内容

- 不保存附件 Blob、已发送消息或后端会话业务状态。
- 不调用 Gateway UI 设置接口。

# 规范

- 草稿键必须使用 `sessionScopeKey` 产生的工作区会话作用域。
- 空草稿必须删除；代码注释使用中文，专业术语除外。
