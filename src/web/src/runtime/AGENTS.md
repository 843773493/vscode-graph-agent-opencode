# 目录用途

`runtime/` 存放浏览器前端按需加载的重量级运行时依赖及其初始化边界。

# 可修改内容

- 第三方运行时的动态导入、一次性初始化和类型收口。

# 不可修改内容

- 不放 React 组件、业务状态、后端协议或普通纯工具函数。
- 不在运行时适配器中吞掉加载或初始化错误。

# 规范

- 重量级依赖必须按需加载，并保证全局初始化只执行一次。
- 代码注释使用中文，专业术语除外。
