# 目录用途

集中定义 Web UI 持久化偏好的默认值、归一化与作用域转换。

# 可修改内容

- Gateway UI 设置的纯数据解析与稳定集合转换。
- 会话侧栏、工作区文件树和 Gateway 控制台偏好的前端值对象。

# 不可修改内容

- 不直接调用后端 API 或操作 React 状态。
- 不保存 Composer 草稿或工作区业务数据。

# 规范

- 新增持久化偏好必须先在本目录声明默认值和作用域，再由统一控制器写入。
- 所有集合输出去重并稳定排序；代码注释使用中文，专业术语除外。
