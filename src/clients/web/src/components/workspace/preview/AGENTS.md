# 目录用途

`components/workspace/preview/` 存放工作区预览区的专用内容渲染组件。

# 可修改内容

- Markdown、Mermaid、媒体等文件预览 renderer 及其局部展示状态。

# 不可修改内容

- 不直接实现后端协议、工作区路径安全规则或标签持久化。
- 不保存跨标签的业务状态。

# 规范

- renderer 通过 props 接收资源内容和动作，不直接依赖应用级状态。
- 用户可见错误必须明确展示，不得静默忽略。
