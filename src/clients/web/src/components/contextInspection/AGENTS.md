# 目录用途

既有“上下文状态”中的冻结请求检查组件。

## 可修改内容

- 服务端 assembly、selection、history 投影与 loss 的只读展示。

## 不可修改内容

- 不实现 HTTP、不读正文文件、不重建 selection 或替换普通聊天 history。

## 规范

- 请求只通过专用 hook，保持服务端顺序，分页和错误状态必须可见。
- 用户可操作控件提供中文可访问名称；每批修改运行静态检查与 Web build。
