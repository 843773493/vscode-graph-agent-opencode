# 目录用途

存放 Web 客户端对公开协议生成绑定的 JSON、SSE 和 WebSocket adapter。

## 可修改内容

- 可以维护后端事件解析、协议消息构造和浏览器传输转换。

## 不可修改内容

- 不得在这里拥有业务状态；后端仍是业务状态唯一来源。

## 规范

- 动态 metadata 和 raw payload 不得静默丢弃。
- 浏览器 DOM、快捷键和展示状态留在客户端业务组件中。
