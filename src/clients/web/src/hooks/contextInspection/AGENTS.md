# 目录用途

既有上下文诊断视图的冻结 assembly 查询副作用。

## 可修改内容

- 请求生命周期、分页、显式选择和错误状态。

## 不可修改内容

- 不修改普通聊天历史或生成后端不存在的 assembly/selection。

## 规范

- 绑定 workspace/session；切换和卸载时取消旧请求。
- 只保存后端镜像与展示态，失败可见，不回退 active view。
