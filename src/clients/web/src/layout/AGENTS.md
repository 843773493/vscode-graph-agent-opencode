# 目录用途

存放浏览器前端工作台布局相关的纯函数、常量和窄职责状态辅助。

# 可修改内容

- 三栏布局、预览区、侧栏尺寸约束等纯计算逻辑。
- 布局持久化所需的类型和默认值。

# 不可修改内容

- 不渲染 React UI。
- 不调用后端 API，不持有业务状态权威来源。

# 规范

- 仅导出无副作用的布局工具，涉及 `window` 的函数必须显式处理 SSR/测试环境。
- 用户可见文案不要放在本目录。
