## 1. Context mutation、stable-prefix 与 source domain contract

- [ ] 1.1 定义统一 `ContextMutationIntent` union及其 `AppendCanonicalItemIntent`、`ApplySourceLifecycleDecision`、`SwitchToolSetIntent`、`RebuildContextEpochIntent` 分支；同时定义 thread-qualified `ContextSourceItem`、source/facet identity、revision、base/full/delta manifest与 tracking state，明确 CSM只负责 source lifecycle且各 intent保留自己的 domain owner、不变量、幂等键和 failure outcome。
- [ ] 1.2 定义`prefix_epoch`、`epoch_reason`、parent assembly、stable prefix byte length/hash、Provider-profile item frame、精确ToolSetRef/policy compatibility key、appended item references及`PendingPrefixEpochTransition`状态；固定只有首次组装、实际compaction重建、rewind重建和ToolSet hard rebase能开启新epoch，同一Turn允许多个model-call-scoped epoch。rewind/compaction可先提交view/pending transition，只有首个assembly成功seal才应用epoch并消费transition。
- [ ] 1.3 固定 `skill_load(name, mode="snapshot" | "tracked" | "untrack" = "snapshot")` 工具 schema、结果 schema、名称解析及路径隐藏合同，并确认不存在其它 Skill 即时移除入口。
- [ ] 1.4 增加mutation/source/stable-prefix/ToolSet/Provider-profile compatibility schema与hash golden vectors及错误合同，覆盖Unicode、空内容、连续revision、frame serialization、`not_tracked`、source conflict、非法intent组合、ToolSet mismatch、stable-prefix violation和`provider-profile-change-requires-rebuild`；已有assembly后的desired profile变化不得形成第五种epoch或静默沿用旧模型，只能在实际compaction/rewind/有效ToolSet hard rebase时与首个新epochassembly原子applied。

## 2. Storage、checkpoint 与 assembly owner

- [ ] 2.1 审计并补齐 `(session_id, thread_id)` owner、thread catalog locator、`SessionLifecycleFence`/`SessionOperationLease`、canonical mutation provenance、逻辑attachment/variant reference、resource id/display URI/revision/snapshot ref、ResourceActivationSnapshot、source revision/detail、metadata/activation facet、tracking registration、prefix epoch/reason、assembly parent、stable prefix hash/length、desired/applied ToolSet revision、ToolSet/policy compatibility key和 selection provenance字段；复用36-byte ASCII `ses_[0-9a-f]{32}`/`thr_[0-9a-f]{32}`外形且payload第13个hex=`4`、第17个hex属于`8|9|a|b`的完整canonical validator覆盖API、Proto、catalog、link、typed ref、生产/测试IdentifierFactory与path resolver，并测试超长/Unicode/分隔符/`.`/`..`/百分号编码/非v4 bits/大小写错误在落盘前失败、完整path预算和历史ID显式migration lineage；不得清洗、截断或提供旧ID path alias，不得新增注入计数或自动到期状态，不得把 `checkpoint_ns` 当 owner key，也不得把workspace blob物理路径、provider locator或credential写入canonical/source/detail。
- [ ] 2.2 为tracking control state与Session lifecycle schema做显式版本化迁移，并与itemized change的旧JSON/物理导航树→workspace SQLite catalog/Session日期分桶迁移共用maintenance gate及单一authority切换点；先quiesce所有mutation，冻结旧ID/父链/locator/created_at与bytes/assembly lineage，按目标catalog active/deleting状态恢复fence而不默认active。正常runtime不读旧JSON、嵌套children或旧路径alias，失败quarantine/fail closed，不覆写旧canonical JSONL。
- [ ] 2.3 在每个SessionThread唯一的logical RolloutCheckpointSaver/ContextStore owner增加四类mutation intent端口，并让CSM只使用source observe/register/track/untrack/reconcile子端口；所有分支使用同一read snapshot和owner transaction，resident owner可按runtime generation重建但任意时刻只能有一个writer，workspace runtime只负责组件装配，业务层不得旁路RolloutStorage。
  - 2.3-A owner只接受itemized创建record/staging/SQLite catalog单一可见性事务发布且字段完整的active SessionThread；准入先取workspace topology shared→目标Session gate，再fresh验证catalog active/日期locator及local fence generation并提交lease/等价record，最终可见性publication重取topology shared/Session gate、fresh验证catalog active和原fence token。普通child由同库preparing ThreadCreationRecord冻结精确target/staging与artifact manifest、发布前CAS验证delegation/collaboration revision；board migration只由BoardMigrationRecord/entry整批发布。CSM/history/before-model不得修补半创建main/child，恢复不扫盘吸收无record目录。
  - 2.3-D Session及递归Folder删除复用itemized的`NavigationSubtreeDeleteRecord`：topology exclusive下冻结完整逻辑子树/日期locators、逐Session预检retention，workspace SQLite一次提交全树deleting并关闭新准入；catalog已deleting但部分local fence active时所有新thread history/detail和mutation返回`session_deletion_pending`。随后按batch逐Session关闭fence、排空旧lease、定点隔离目录、持久进度，全树完成后同库一次tombstone；故障注入catalog commit前后、单/多目录隔离中、late callback、重启/多进程锁及目标revision漂移，确认不会部分可见、扫盘、双gate/双DB写事务或回退active。
  - 2.3-E 冻结SessionOperationLease schema/state、holder generation/fencing token、非终态索引、跨库主体commit→lease terminal顺序与崩溃恢复；不按墙钟过期、不以缺内存owner冒充terminal。实现跨进程`NavigationTopologyGate`与`.boxteam/navigation/session-lifecycle-gates/{session_id}.lock`短时OS锁，统一topology→至多一个Session gate→至多一个SQLite写事务的锁序；cold read在topology shared下取得fresh locator/Session shared guard，后者覆盖全部handle。catalog deleting后新准入立即失败，删除逐Session等待原reader并收敛旧lease后隔离目录；锁inode不重建，进程退出自动释放。
  - 2.3-F 以topology gate串行化pinned ForkRetentionClaim与整树catalog deleting：claim先行阻断整个batch且全部节点仍active，删除先行使claim在local fence仍active时也零副作用失败；覆盖active/preparing/released claim、不同后代Session blocker、两种顺序和SQLite commit前后崩溃，删除不得先关闭任一local fence再发现pin。
  - 2.3-G 对齐itemized的NavigationMutationRecord/202队列协议：入队及Web optimistic overlay不得创建SessionOperationLease、CSM source item、ToolSet/GraphBinding变化或owner rehydrate；worker只在实际执行时竞争topology/Session gate。排队删除前其它业务以已提交active catalog正常准入，整树deleting提交后即使本地fence仍active或Web事件迟到也拒绝新业务；committed纯导航move/rename只更新breadcrumb，不开启prefix epoch。覆盖pending→committed/rejected、同thread活跃model call及恢复时的stable-prefix bytes不变和无旧目录路径推断。
  - 2.3-B 为每个active execution持久化捕获Session lifecycle generation的operation lease和ExecutionContextFence；busy thread的新用户acceptance可立即提交queued Turn/root，但旧execution plan必须按causal admission排除未来entry root/seed/notice，并按Turn/plan因果选择其后提交的tool/final item。queue entry取得active slot时冻结ResourceActivationPolicySnapshot与TurnResourceSnapshot；旧execution后续tool-loop复用turn-bound binding，只有policy中显式配置为model_call的resource kind才在安全preparation通过parent-linked ModelCallResourceSnapshot重新冻结Registry内存revision，policy本身Turn内不漂移。前序终态后同事务推进下一entry visibility与新fence，之后才运行该entry首次CSM/ToolSet reconciliation；item_sequence不得替代model/history顺序，Session删除关闭fence后旧generation只可收敛control outcome而不得新增业务item。
  - 2.3-C 新execution必须先通过Session生命周期准入、进入thread-local持久FIFO并取得唯一active slot，之后才运行before-model CSM/ToolSet reconciliation和seal；pending entry不提前读取source，active sealed call不受排队期间变化影响，callback绑定原execution并校验lease/generation且不得成为插队entry。
- [ ] 2.4 定义三个不混淆的原子边界：异步team/inbox/runtime producer事务只提交幂等pending observation/ambient item及wakeup且不推进applied/injected revision；rewind/compaction事务提交active view、checkpoint-versioned CSM control state和pending epoch transition；后续model-call preparation事务先收敛完整tool-call/result group，再原子提交/选择source item、applied revision、ToolSet switch、plan/assembly selection、applied prefix epoch及transition消费。outstanding tool call未配对时source保持pending且不得插入call/result之间；全部result后按协议因果再选source，物理sequence/offset不决定模型顺序。失败不产生重复pending或半item/assembly，不误推进diff基准/消费transition；已成功提交的pending事实或view不得因后续source读取失败被隐式回滚。
- [ ] 2.5 将同一 assembly的 canonical ordinal/tool配对、source identity/revision唯一性、base→delta顺序、plan ordinal、ToolSet compatibility和 stable parent-prefix校验接入 seal/dispatch preflight。
- [ ] 2.6 让 Skill snapshot/tracked 正文和 source diff 使用现有受保护 detail owner及 retention/GC；required detail 缺失阻止 seal，optional detail只能显式 omission/loss。
- [ ] 2.7 增加独立进程的 checkpoint/assembly恢复检查，覆盖 canonical append、tracked、untracked、pending candidate、desired/applied ToolSet revision和 sealed Provider frames，禁止从当前文件或当前 ToolSelectionStore重建旧 request。
- [ ] 2.8 实现ContextStore/CSM与resident ThreadRuntime的rehydration port、generation/lease fence、可注入单调Clock和cold-read边界；提供只读`ThreadResidencySnapshot`（identity、residency、execution、last activity、deadline、脱敏blockers）。child默认30分钟idle unload只关闭可重建runtime资源，持久source/tracking/prefix/ToolSet/assembly状态逐字段不变，history/detail不得materialize writer，迟到callback必须重新取得精确thread的当前owner；测试用fake clock验证29:59/30:00，不缩短产品阈值。

## 3. ContextSourceManager 与 reconciliation

- [ ] 3.1 在 thread-qualified rollout context runtime owner中实现 source-specific `ContextSourceManager`，只消费结构化 observation、SkillCatalog descriptor和 Saver source端口并返回 lifecycle decision；不得截获 canonical append、ToolSet/compaction mutation，不直接读写 JSONL/SQLite、重组全量 history或构造 Provider message。
- [ ] 3.2 在`resource_platform/sources/`实现`ObservedSourceDescriptor`、provider-owned可重建handle、`SourceReconciler`和不可变`ObservedSourceRevision`；file provider使用`StableSourceReader`完成允许根/no-follow/普通文件、前后signature与双读同hash、最多三次attempt、固定byte上限、严格UTF-8和完整原始byte hash，network/memory provider使用各自可验证版本token，不套用文件双读。持续变化/过大/非法编码保留旧valid来源revision并标明unavailable；Gateway global Skill由Gateway读取并返回受认证内部snapshot，不让workspace假设本机路径。Skill name/description和正文facet由语义loader从已接受来源快照解析，只有对应facet payload变化才推进其语义revision。CSM、middleware、skill_load和model-call preparation不得直接调用reader。
- [ ] 3.3 实现tracked registration与activation-boundary reconciliation，只持久化稳定resource/source identity及catalog/provider binding revision；provider handle/locator归Registry/provider私有并由其跨Gateway/backend重启重建。默认turn snapshot、可选model_call snapshot都只消费Registry published revision，区分observed、pending、committed和latest-visible-committed revision，以后者作为diff基准。
  - 3.3-A activation registration绑定name+catalog entry+resolved source identity；同名优先级变化不自动改绑。显式再次tracked对相同entry幂等复用，对新effective entry原子freeze旧registration并追加/track新完整activation；原source不可读fail closed。同source/revision重复snapshot返回already_active，不追加重影。
- [ ] 3.4 实现多个未提交变化的合并：只生成 latest-visible-committed→current 的一个 delta，保留 observation provenance但不创建可撤销的中间 context item。
- [ ] 3.5 实现`untrack`的checkpoint-versioned tracked→untracked/frozen转换；每个thread/normalized name至多一个active registration，untrack按该索引定位而不重新解析effective catalog或读取source，覆盖同名高优先级entry出现后仍能停止原tracking、多个active返回`tracking-state-conflict`；该registration停止消费Registry revision和rewind自动恢复，但共享monitor可为其它consumer继续观察resource；不追加、删除或改写Skill source正文，非tracked情况返回`not_tracked`。除模型`skill_load`外，受信Session控制API必须以精确session/thread/name复用同一mutation和结果合同，作为`tracked-source-unavailable`阻止dispatch后的人工恢复入口；API不接受路径、不建立第二状态机，Web只以后端完整结果更新。
- [ ] 3.6 实现 rewind/compaction reconciliation：先验证rewind、compaction、replay与fork目标view的tool protocol closure，冲突时返回`tool-protocol-boundary-conflict`并保持旧view、不创建目标或pending transition；再按目标 checkpoint恢复 tracking state，tracked 缺失时从冻结Registry activation snapshot追加published完整 revision，snapshot/untracked 不恢复，物化和恢复 item均标记为 post-user user-role source且请求路径不读源。
- [ ] 3.7 让重复 tool call、observation、prepare、seal、model attempt 和 transport retry复用稳定 candidate/assembly/frame；revision、hash 或 item bytes 冲突时 fail closed。
- [ ] 3.8 在`app/services/infrastructure/events/`提取通用`EventChannelService`并把现有Job event bus改为`job.events/{job_id}` typed adapter；为`resource.observe/*`、`resource.state/*`、`config.lifecycle/*`和`context.source/*`建立独立queue/cursor/backpressure/gap合同，资源事件只携带source/watch identity与轻量状态，不得携带正文/credential或进入job队列，也不得把通知当durable事实。
- [ ] 3.9 在`app/abstractions/`、`app/domain/resources/`和`resource_platform/contributions|observation|providers/`建立typed port/纯值对象、只存贡献定义的`ResourceContributionRegistry`、file/network/memory monitor/snapshot/loader/reaction扩展协议；实现ResourceTaskSupervisor树形生命周期、完整`provider instance + locator + recursive/filter/exclude/correlation/options`watch key、共享subscription引用计数、provider health和plugin task取消/排空，禁止SessionThread独占watcher和plugin取得ContextStore writer。为每个owner进程增加不可被动态配置移除的最小`ResourcePlatformBootstrap`，只启动本进程事件/任务控制根、内置file snapshot能力并监视本配置域的已知配置/plugin manifest精确locator，不注册模型source或业务reaction；Gateway不得读取Workspace配置，Workspace不得接管Gateway控制面配置。每个新源码子目录同变更创建四段式`AGENTS.md`。
- [ ] 3.10 在`resource_platform/sources|derivation|registry/`分别实现来源registry/`SourceReconciler`、无环`ResourceDerivationGraph`、语义`ResourceRegistry`和两级immutable revision/CAS；file raw hash与network/memory版本token分离，按一致依赖向量做版本化parse/facet/semantic diff和reaction，原始变化但facet不变不推进语义revision。覆盖一源多facet、多源一有效配置、AGENTS链、team ledger投影、依赖缺失/环/混合generation、不同watch选项不误共用、重复/乱序/rename burst/overflow/reconnect、上一valid与unavailable区分、required readiness fail-closed和不扫描未登记目录。config merge仍由既有config owner实现，AGENTS/Skill/team业务loader进入`business/resource_derivations/`，不得复制权威业务状态。
- [ ] 3.11 在`services/orchestration/resource_activation/`实现ResourceActivationCoordinator并构造ResourceActivationPolicySnapshot、TurnResourceSnapshot/ModelCallResourceSnapshot，经唯一Saver/itemized port随assembly持久化而不建立第二writer：active slot冻结policy revision/hash和各kind effective boundary；Turn snapshot固定turn-bound binding，ModelCall snapshot引用parent并逐字节复用turn-bound binding、只在tool协议收敛后的安全preparation重冻结model-call-bound语义Registry内存snapshot。每个binding保存effective boundary与源lineage校验引用，`skill_load`使用发起tool call的snapshot；禁止assembly单值boundary、Turn内policy漂移、请求期stat/read/list/network fetch、immediate/TTL/request-count及sealed call漂移。
- [ ] 3.12 在Workspace配置族新增`context.resource_activation.default_boundary`与`overrides`，默认`turn`、value闭集`turn|model_call`、未列kind继承默认；同步`workspace_inline.jsonc`、`workspace_schema.jsonc`、`workspace_dev.jsonc`、最终递归合并/诊断和focused配置测试。配置reload可发布新desired policy，但effective policy只在下一Turn active slot冻结；不新增旧字段alias、Turn中途切换或双内部模型。
- [ ] 3.13 实现resource graph generation的shadow start→initial reconcile/health→atomic publish→old tree drain，启动/reconnect/gap期间建立readiness gate；失败保留旧generation并显式诊断，验证新旧generation不会双发revision。覆盖bootstrap冷启动、有效generation上的坏candidate、动态配置试图移除bootstrap及无valid内置/合并配置时直接启动失败；不提供空图、旧字段或未校验plugin fallback。
- [ ] 3.14 在`resource_platform/virtual_resources/`实现VRN parser/resolver及`ObservedSourceDescriptor/SemanticResourceDescriptor/ResolvedResourceHandle/ResourceProvenance`：支持workspace/gateway/builtin/memory/plugin规范URI，严格拒绝userinfo、query/fragment、控制字符、反斜杠、`.`/`..`、双重编码和scope/capability错配；display URI、source id、resource id、provider locator四者分离，URI不得作为identity、capability、dedupe或幂等key。

## 4. SkillCatalog 与完整 producer 迁移闭包

- [ ] 4.1 建立 bundled、`${BOXTEAM_HOME}/skills`、`${workspace_abs_path}/.boxteam/skills` 三层 SkillCatalog，按 `workspace > gateway-global > bundled` 解析唯一 entry；Gateway只管理全局 catalog，不写工作区 Session状态。
  - 4.1-A 让ResourceRegistry持续发布绑定entry/resource/source identity、metadata revision、display URI和Registry-owned provider binding ref的immutable SkillCatalog revision；bundled读发布manifest，Gateway/workspace file provider监视并只枚举固定skills直接entry，不递归rg、不扫描workspace或跟随越界symlink。Gateway global正文由Gateway Reconciler稳定读取并以受认证内部snapshot发布，无变化不唤醒/广播，descriptor不含物理路径且不写Session状态。skill_load只从当前activation snapshot解析，catalog/snapshot冲突显式失败且不在工具路径重新读盘。
- [ ] 4.2 使用禁用custom tag/alias/object构造的安全frontmatter parser，Skill metadata只接受唯一、有界scalar `name` 和 `description`，将 metadata 与activation建立不同source facet identity；其它字段不进入facet payload/revision，activation只取frontmatter后精确正文。模型可见catalog和诊断不得泄露内部路径；skill_load name仅作受校验catalog map精确key而不拼路径。移除 `allowed_tools` 对 Skill context contract 的参与，若工具事件仍需 skill_names 归因则迁到独立工具归属配置。
- [ ] 4.3 注册`skill_load`工具并让模型只传名称/mode；工具从发起该tool invocation的冻结Turn/ModelCall SkillCatalogSnapshot解析exact resource binding，结果只返回安全名称、mode、display URI、revision/hash标识和append状态，不返回物理路径、provider handle/locator或完整正文；工具执行前出现同名新revision不得改变本次解析。
- [ ] 4.4 删除旧Skills middleware的位置提示、依赖通用read完成Skill activation的路径、`/.boxteam/skills`/`/.boxteam/bundled-skills`模型可见挂载和`read_file`精确SKILL.md白名单；不保留兼容adapter、旧tool schema或双入口。
- [ ] 4.5 删除`WorkspaceAgentsMiddleware`的before-model读盘、内存snapshot和直接`HumanMessage`注入；AGENTS首次root及后续delta只消费ResourceRegistry snapshot并通过CSM/ContextStore提交。迁移完成后删除该旧middleware或仅保留与资源读取无关的明确新组件，不保留兼容壳。
- [ ] 4.6 迁移设计表 R01–R07/R09初始 instruction producer：Agent配置说明、runtime identity、条件化团队规则、Todo、Skill metadata、Filesystem、compact tool说明和显式 memory分别注册 source provenance；条件化 root绑定精确 ToolSet policy并只在合法 hard rebase epoch重编译，对应 tool descriptions只进入 ToolSet，默认未接线 memory不得伪装启用。
- [ ] 4.7 迁移设计表E01–E07 runtime event producer：Goal仅绑定main thread且child操作明确拒绝；delegated child任务/汇报使用同Session精确parent/child thread；generated result回到发起thread；模型侧`send_message_to_session`删除`simulate_user`并解析目标main，以source thread outbox、target main-thread inbox、受认证receipt和本地事务/幂等execution admission返回durable acceptance及binding，不引入分布式事务或Gateway业务owner，并校验question/reply correlation。source outbox与target inbox acceptance分别在各自Session gate内建立communication-side lease，禁止同时持有两个gate；target workspace单一`InboxAdmissionWorker`由acceptance事件/backend startup按状态索引恢复未绑定inbox，以原lease/generation、claim和wakeup key幂等admit，不扫目录、不依赖wait/read或内存future、不建立第二writer。以接受`communication_id|job_id|turn_id`、默认60秒/最大300秒和timeout可恢复selector的有界`wait_for_session`替换`monitor_session_agent_end`，由可注入单调Clock/DeadlineTimer和状态订阅驱动，持久DurableDeadline的UTC/monotonic origin以保证重启不延长预算，deadline竞态按target已提交revision裁决且不轮询数据库；`read_context`只生成调用方canonical tool-result。Session内部team membership/task、terminal精确callback、四类execution retry和各类checkpoint reminder先通过生命周期lease提交ambient/pending source，再走不创建user root的幂等execution wakeup；generated seed显式区分user-derived root与internal source。
  - 4.7-A send在route前以软件提供、模型不可见的`send_operation_id`先在source lifecycle gate建立lease，再提交outbox/communication/request preimage；target在自己的gate内建立lease后才提交inbox/ambient/wakeup，并以`admission_id + preimage hash`在该lease覆盖下create-or-get Job和补写binding。删除先关闭任一端fence则该端零副作用，acceptance先行则删除收敛原lease。保留Session生命周期内最小communication dedupe/correlation/receipt tombstone；不得靠短TTL、临时ID或重启后换ID破坏exactly-once admission。
  - 4.7-B 建立中心Gateway hub通过SSH `-L`到spoke并在隧道内维持全双工WebSocket对等RPC channel的联邦路由；spoke可在hub主动建立的channel上反向发起请求，最多中继`B → A → C`且响应原路返回，禁止反向SSH要求、spoke直连和继续多级转发。裸ID执行local+唯一hub有界exact-ID discovery，携带visited set、`max_transit_gateways=1`、`max_gateway_hops=2`和总deadline；hub从channel确认真实origin，再签发绑定origin/audience/path的transit discovery/operation grant，target只查本地cold catalog并按最新policy授权。持久connection config ID与瞬时channel instance/epoch/route lease分离且均不进业务identity/preimage；hub只存peer/policy/replay registry/audit控制状态。实现`permissions.federation`默认允许全部核心操作、规则为空、hardening关闭的原子热发布policy snapshot；权限变化不改ToolSet/context/channel，每个discovery/transit/admission/read page/wait disclosure/send retry/relay response使用最新revision，撤权停止未接受操作或披露、恢复后下一次调用生效，已durable acceptance不回滚。身份、完整性、audience/path、防重放、target解析和幂等始终强制。
  - 4.7-C Session collaboration ledger mutation先通过lifecycle gate，在同事务创建承担lease且冻结fence generation/recipient集合的fanout record；worker按ledger revision+recipient幂等提交pending source并校验lease/generation，不唤醒cold runtime。team_state默认在active slot冻结turn snapshot且后续tool-loop复用；只有显式model_call配置才在安全call边界重新捕获Registry内存snapshot。部分投递失败/重启只补未完成recipient，fence关闭后未提交recipient明确terminal且不得晚到append，禁止跨thread事务或已提交item重写。
  - 4.7-D read首请求按`(source GlobalThreadAddress, first_source_call_id)`唯一create-or-get并分配`observation_id/read_series_id`，签发版本化AEAD opaque ReadContextSnapshot，冻结identity/target/view/item上界/projection-policy/总预算；同call丢response重试找回同一snapshot。后续页使用新的`operation_invocation_id/source_call_id`但由cursor恢复同一observation并逐页重新授权。目标变化不混页，cursor不承载capability，解密/认证失败、冲突、retention过期或不可恢复loss显式返回且不泄露内部字段、不静默打开新view。每次read/wait source call先提交FederatedCallRecord；target在ContextStore外为read建立RemoteObservationRecord及按`(observation_id,page_ordinal)`去重、反向唯一绑定source call的RemoteObservationPageRecord，wait observation与source call一一对应并冻结selector/baseline/deadline。grant/response绑定call+observation/page；同一次调用的网络retry换grant不换identity，source崩溃恢复同一结果且每个调用最多一个tool result，过期不重开。
- [ ] 4.8 删除 checkpoint直接 message mutation、`PromptReplayCaptureMiddleware`、捕获标签/instrument链及 prompt diff/synthetic assembled prompt路径；增加 AST/import、source-registry completeness与 sealed manifest/dispatch hash正向对账断言，禁止未登记 `HumanMessage`、普通 `MessageRole.user` Turn或通用 read激活内部 source，同时保留旧历史读取。
- [ ] 4.9 以显式migration operation整块处理既有跨Sessionteam/member/task/coordinator状态：materialize只从终态/显式quiesce的legacy main checkpoint/view调用migration-only `materialize_thread_copy`，复用copy mapping/校验但只在coordinator Session的record冻结staging中生成goal-disabled child，不创建新Session/main、不写catalog/ledger、不自行发布或取得owner；公开`full_rollout_copy`继续只创建独立target Session/main。source Session保持不变；其它情况freeze/detach。创建任何staging前先在coordinator `session-control.sqlite` create-or-get不改变catalog/ledger的`BoardMigrationRecord(state=preparing)`，冻结operation/preimage、coordinator lifecycle、旧board/catalog revision，并为每个target内嵌唯一`MigrationChildCreationEntry`保存child ID、最终/内部staging locator、GraphBinding/capability、source view、lineage/mapping和artifact manifest/hash；该entry是batch child唯一creation journal，禁止同target独立`ThreadCreationRecord`或普通worker单独发布/启动。随后验证并durably flush，先把child原子rename到record冻结且尚未引用的最终locator，再由thread catalog、collaboration ledger与该record单一SQLite事务CAS验证coordinator/board/catalog/member/task precondition后发布完整mapping并标记`published`。CAS失败不覆盖并发变更、不重基或部分发布，只定点清理后标记`aborted`。覆盖内部/公开copy不共享creation key与发布语义、record/rename/发布/terminal response崩溃、迁移期间board mutation或Session删除，orphan只枚举预存record定点校验恢复/清理、无record目录不吸收且正常reader只见完整旧/新board，不假设跨thread/workspace分布式事务。禁止复制active runtime、空child、部分发布及外部Session/本地child混合member。删除/拒绝正常runtime的跨Sessionteam attach与状态同步；跨Sessionsend/read/wait通信ledger只保留resolved main、幂等、correlation和访问审计，read/wait不得进入目标CSM或加载目标runtime。
  - 4.9-A 所有跨Session copy由journal/entry预分配`source_snapshot_id`与内部locator，再在source shared `SessionReadGuard`下以固定SQLite read snapshot/online backup冻结同一revision的view/checkpoint/control、`storage_commits`、各JSONL committed end offset，只复制offset内JSONL及通过hash/length/capability校验的thread-local detail，并原子发布绑定operation/preimage、lifecycle generation、database/逐文件/detail/attachment-claim manifest hash的`SourceCopySnapshot(captured)`。关闭source handles并释放guard后才进入target staging/publication，且不得回读source。board migration先短持coordinator gate建立record/lease并释放，再按record顺序一次只持一个source guard，最后另行短持coordinator gate做publication CAS；公开copy同样分离source capture与target gate。实现锁序和revision断言；并发append/view变化不混入，partial定点abort，完整snapshot可不依赖source恢复，禁止扫盘/补齐/同operation换revision。source删除先行零可见target/整批abort，guard先行只让删除等待capture。
  - 4.9-B `pinned` fork在capture前由target journal预分配operation/preimage，再单独取得workspace topology shared/source gate创建绑定fork、两端GlobalThreadAddress、target operation/preimage、source generation与view/detail范围的`ForkRetentionClaim(preparing)`/retention占位；删除先行零可见target，claim先行使删除在整树catalog deleting前返回retention blocker。target提交只激活同一claim；abort/target删除以target durable release record驱动、释放target gate后单独取得source gate释放、再确认target terminal，禁止双gate/双事务和墙钟过期。fork/copy限定同一workspace，federated grant执行fork/远端retention必须拒绝且零远端副作用。
  - 4.9-C 对copy中的workspace attachment建立`ForkAttachmentClaim` saga：snapshot只记logical ref/digest/length/availability/owner/catalog revision，不复制blob，并预分配target-local item/attachment identity与mapping；source guard释放前在单个attachment catalog事务验证并claim，全部成功后才封存有序claim清单/hash，partial按精确operation ID释放。target/child发布前required claim owner_reserved且建立target owner ref，resolver在catalog active前拒绝；有required claim的公开copy target预置以`operation_kind=attachment`承担lease合同的`CopyAttachmentSettlementRecord`，board record保存等价字段，publication后保持`published_pending_attachment_commit`非终态；无required claim的公开copy不建settlement record。finalizer与target删除竞争同一gate，以不重叠事务提交claim/record；删除先行则释放reserved claim/ref，或验证committed claim归属后持久释放其target owner ref，再进入`target_deleted|coordinator_deleted`，禁止恢复重建/保留owner或非终态record下隔离节点。abort按record ID释放并进入`aborted`。覆盖source/target删除与GC、claim/owner/publication/finalization崩溃、required失败、unavailable历史映射、同digest并发和无source identity复用/重复blob/悬空ref/提前授权/扫盘。
  - 4.9-D 冻结附件结算状态闭集：`CopyAttachmentSettlementRecord`只能为非终态`preparing|published_pending_attachment_commit`或终态`committed|aborted|target_deleted`；`BoardMigrationRecord`只能为非终态`preparing|published_pending_attachment_commit`或终态`published|aborted|coordinator_deleted`。无required claim时公开copy不建settlement record，board可见性事务直接写`published`；有required claim时publication只能写pending，全部claim/settlement完成后才写成功终态。已发布后删除抢先必须写对应删除终态，且结算不得改变已发布board成员集合。替代4.9主项中未区分attachment的`published`简写。

## 5. ToolSet hard rebase、Provider、history 与 display projection

- [ ] 5.1 修改 plan compiler，使首次组装时把设计表中所有已启用 R类初始贡献编译为一个不可变 root system item并保存每个 source的 identity、revision、ordinal、hash、included reason、ToolSet binding、visibility和 loss；只在 compaction、rewind或 ToolSet hard rebase的新 epoch重编译 root，且不吸收 post-user source。
- [ ] 5.2 修改 LangChain/native Provider projector，使第一条真实用户消息后的 CSM full、delta、rewind restore 和 compaction materialization均输出独立 user-role item，不合并进旧 system/user item、不前插也不按 role 重排。
- [ ] 5.3 实现 Provider-profile item frame封存、ToolSetRef/policy compatibility key及父 assembly逐字节前缀校验；同一 sealed assembly重试必须复用 frames/tools，adapter自动合并/规范化或同 epoch ToolSet漂移时显式 reject。
- [ ] 5.4 保持 ToolSelectionStore/ToolService拥有 workspace/agent级 desired revision、每个 SessionThread ContextStore拥有 observed desired/applied binding，并在每次 model call前比较二者和实现 safe-boundary hard rebase：in-flight sealed call不可变、旧 outstanding call先产生真实配对 terminal outcome、快速切换可合并到最终 desired、应用后创建 `toolset_changed` epoch且同 Turn可多 epoch；Provider无法投影旧工具历史时 fail closed。Provider capability matrix只保留关闭状态的 Anthropic中途 system TODO，并保持原生 tool call/result role。
- [ ] 5.5 废弃 `ItemizedContextProjectionMiddleware` 的 prompt/tool/context反向捕获、prepare/seal、状态与 fallback职责；框架确需 model-call hook时实现无状态 sealed-assembly dispatch bridge并置于最后一个 request-mutating位置，否则删除该 middleware，由 Provider adapter直接消费 Saver-issued dispatch reference；history/diagnostic独立读取同一 sealed selection。
- [ ] 5.6 分离设计表 D01/C01–C03：派生摘要请求使用独立 sealed assembly且 summary保留 compaction owner；真实用户输入/附件及 assistant/reasoning/tool协议事实通过 canonical append intent提交，ToolSet通过 switch intent提交，它们共用 ContextStore owner但不进入 CSM；附件正文只由workspace content-addressed catalog按capability和thread/item/view membership读取，模型与历史不接收物理locator，展示文本不得反写任何 canonical/source/ToolSet/control状态。

## 6. Rewind、compaction、restart 与边界测试

- [ ] 6.1 增加跨多次正常 model request的 stable-prefix byte golden test，覆盖 root system、用户/assistant/tool canonical append、full source、delta、tool loop、重复 dispatch和 adapter禁止合并；验证普通 append保持 epoch，而 ToolSet变化必须 hard rebase并更新 compatibility key。
- [ ] 6.2 增加Skill snapshot/tracked/untrack状态机测试，覆盖默认snapshot、无变化零追加、A→B→C未提交合并、`not_tracked`、同名catalog覆盖后untrack旧registration、active registration唯一约束/`tracking-state-conflict`、重新tracked及路径不泄露；另覆盖tracked source不可读导致model dispatch fail closed后，受信Session控制API仍可在不读取source/路径、不删除旧item的情况下调用同一untrack mutation，未授权或错误thread零状态变化，Web以后端结果替换状态。
  - 6.2-A 覆盖同revision snapshot already_active、显式新revision snapshot、tracked source同名高优先级覆盖不自动改绑、显式tracked rebind的旧registration freeze/新完整activation及原source unavailable fail closed；file provider只监视/枚举固定一层且无递归rg/越界symlink，请求路径零stat/read/list。使用可控替换/原位写入覆盖双读TOCTOU、三次失败保留旧valid snapshot且不推进revision/dispatch、Gateway global跨双进程重启handle重建、catalog/activation snapshot冲突、byte上限/严格UTF-8/BOM/换行/Unicode source snapshot与facet hash、安全frontmatter拒绝custom tag/alias/重复或非scalar字段、忽略字段单独变化不生成item、skill name不拼路径和物理locator/正文不泄露；另覆盖同assembly混合turn/model_call binding、Turn内policy热更新不生效、下个Turn生效及skill_load固定使用发起tool call snapshot。
- [ ] 6.3 增加 rewind checkpoint矩阵，验证 snapshot移除不恢复、tracked registration保留时以 user role恢复完整 revision、untrack 前后恢复对应 checkpoint状态。
- [ ] 6.4 增加 compaction materialization测试，验证动态 source完整恢复仍为 post-user user item，旧链可审计且不重复投影，snapshot/untracked不重读文件。
- [ ] 6.5 增加进程重启、GraphBinding/ThreadRuntimeBinding与main/direct及non-main/date-shard locator解析、workspace attachment固定blob identity、Session-local pin先行的跨库ingest/claim saga，以及pin准入/owner-ref提交/Session删除竞争同一`SessionLifecycleGate`的双顺序测试；覆盖跨日期同digest去重、rename/publish崩溃恢复、先发布后删除的reference释放、先删除后发布拒绝、ref-release/tombstone/GC/capability、detail GC/forbidden、revision conflict、SQLite transaction failure、未提交candidate及pending desired ToolSet，确认禁止扫盘/物理路径泄露、fail closed、幂等、原子回滚与旧assembly bytes/tools不变。
  - 6.5-A 增加topology shared/Session gate/SessionReadGuard/lease与NavigationSubtreeDeleteRecord的并发矩阵：child/board、collaboration、owner rehydrate、execution、Skill/ToolSet control、communication、attachment和cold history在catalog deleting前后双顺序；已有lease可恢复且须排空，deleting后新业务零副作用，已开始read可结束但新read pending。验证全树deleting→部分local fence active→逐目录隔离→统一tombstone的每个崩溃点和锁序、跨进程释放、无双Session gate/双DB写事务、late callback fencing。
  - 6.5-B 增加pinned retention与整树删除线性化矩阵：active/preparing claim阻断catalog deleting，released不阻断；claim先行则所有后代保持active，deleting先行则新claim零副作用，即使目标local fence尚active。故障注入catalog commit前后及多个Session隔离中断，断言batch manifest/target IDs/locators不变、整树逻辑可见性原子、恢复不扫盘或局部开放。
- [ ] 6.6 增加bundled/Gateway-global/workspace SkillCatalog与VRN集成测试，验证固定优先级、所有工作区默认可用、metadata只含name/description、display URI scope正确、Gateway不写Session存储；验证URI不是capability/identity、恶意grammar在provider访问前失败、历史snapshot不按当前URI重解。
- [ ] 6.7 为设计表 R01–R09、E01–E07、D01、C01–C03、P01建立逐项 producer/domain-owner/mutation-intent覆盖矩阵；增加 ToolSet add/remove/visibility/policy hard rebase、outstanding call收敛、同 Turn多 epoch、Provider旧工具历史不兼容，以及 PromptReplay零残留、bridge无状态、reference错配、retry同 bytes/tools、bridge后 mutation拒绝和 LangChain/native/history一致性测试；专门覆盖`skill_load`及异步team source在并行tool call尚未全部result时只保持pending，下一request严格为assistant calls→全部paired results→source且物理append交错不改顺序。未登记生产 prompt差异必须 fail closed。
- [ ] 6.8 增加main/child capability、child无Goal、同child FIFO/ExecutionContextFence与queued root后旧execution继续tool-loop的隔离、Session内team source隔离与legacy board的`materialize_thread_copy`/freeze两种结果、公开`full_rollout_copy`仍创建独立Session/main、active source拒绝、发布前后crash recovery及正常API不可见staging，并覆盖普通`ThreadCreationRecord`后Session删除/delegation取消/revision漂移，以及`BoardMigrationRecord`逐target entry不产生独立creation record、不被普通worker单独发布、并发board mutation触发CAS失败、只定点清理且无record目录不吸收；增加fake-clock 29:59/30:00 idle临界与active lease阻断、cold history、lazy rehydrate、tracked activation snapshot恢复、late callback generation fence，以及Gateway-local/federated Session-link逐跳授权和send/read/wait测试。三个隔离Gateway进程组成真实loopback `B ⇄ A ⇄ C`：A通过SSH转发等价fixture建立到B/C的全双工WebSocket channel，B反向发起跨spoke请求且响应原路返回，C保留B真实source；不接受反向SSH、B/C直连、resolver stub或remote-backend直连。验证持久`gateway_id`/connection config ID与瞬时channel epoch/route分离且均不进入业务identity，并覆盖默认无规则可用全部核心操作、连接中撤权/恢复按实际使用点生效、ToolSet/stable prefix不变，以及错误origin/audience/path、过期/replay、credential失效、日志/模型泄露和零workspace副作用。communication send→wait不会假idle；wait用fake deadline timer推进真实60/300秒合同。由target进程外test harness持有`CommunicationAdmissionBarrier`，重启target后经内部test port重连，验证acceptance后原ID重试不重复注入；同ID不同payload/target冲突，无selector wait不订阅未来Job，模型schema没有`simulate_user`，read只形成调用方tool-result，目标只解析main，read/wait不注入或唤醒，跨Session状态字段被拒绝，卸载前后stable prefix和sealed bytes不变。
  - 6.8-A 增加source在target acceptance后、receipt提交前退出并恢复同一invocation的测试，以及target在Job create-or-get后、inbox binding前退出的测试；断言原send operation、communication、acceptance、admission、Job/Turn identity逐项不变。覆盖terminal正文回收后的相同ID命中/冲突与Session删除收敛，wait多对象聚合固定为`failed > cancelled > running > pending > completed`且与返回数组顺序无关。
  - 6.8-B 覆盖远端裸ID的origin envelope→hub transit discovery grant→完整operation grant两阶段授权，discovery grant禁止业务操作，`max_transit_gateways=1`/`max_gateway_hops=2`/总deadline/visited path且spoke无递归；未授权存在与不存在不可区分、ambiguity不泄露locator、stale route hint由target重验失败，hub和target nonce持久first-use registry跨重启拒绝replay/故障fail closed。覆盖channel epoch/route revision轮换不改变已冻结GlobalThreadAddress、communication或preimage且不可达不改投；默认policy允许核心操作，连接中撤销read/wait/transit后下一实际使用拒绝、恢复后立即允许，wait grant寿命不足、分页/恢复新grant和selector不可越权，已durable acceptance不因撤权回滚。
  - 6.8-C 覆盖team ledger fanout在零/部分/全部recipient投递点崩溃、cold recipient不唤醒、queue acceptance后/active slot前及active tool-loop两次model call间新增revision；默认turn在active slot冻结且同Turn不漂移，team_state配置model_call时下一安全call消费Registry新snapshot，连续pending合并、已提交revision不重写、recipient删除和其它recipient不重复。
  - 6.8-D 覆盖read分页期间目标新增Turn、rewind/view变化仍保持首个snapshot，第二页新source call通过cursor恢复同一observation/read series，逐页撤权、target/principal/policy/limit冲突、AEAD token篡改统一失败且不泄露、snapshot过期/loss、空页/终页revision一致和总预算不可绕过；覆盖read/wait observation冻结后source退出、同source call恢复相同page/selector/result、同identity不同preimage冲突、每调用单一tool result及record过期不静默重开，并覆盖相同clock origin进程重启和origin变化/回拨/clock不可用均不延长DurableDeadline。
  - 6.8-E 覆盖公开`full_rollout_copy`和board `materialize_thread_copy`的source capture/delete双顺序及多source顺序capture：冻结revision后并发append/view变化不进入target，删除先关闭fence时不创建可见target/child并整批abort，shared guard先行时删除只等到`SourceCopySnapshot`与manifest/bytes durable且handles关闭，之后source可删除而target从冻结副本完成。注入partial capture及captured-marker到operation-CAS崩溃，验证只按预登记locator定点abort/恢复且不扫盘、补齐或换revision。以lock-order instrumentation断言coordinator准入、单个source capture、target staging/publication互不重叠，任意时刻最多一个Session gate/guard和一个数据库事务，且释放guard后不访问隔离source节点。
  - 6.8-F 覆盖本地pinned claim与source删除双顺序、target committed前后崩溃、abort与target删除release saga、preparing claim冲突/缺record fail closed和detached无claim：证明capture前已有唯一claim，target提交后不补建，target/source gate从不重叠，释放后source删除可继续。federated link/grant调用fork必须明确拒绝且远端catalog、retention、context零副作用，既有send/read/wait仍通过。
  - 6.8-G 覆盖copy attachment claim在source owner release和GC竞争下保持单一blob，target/child未发布时reserved owner不可访问、发布后可用；注入preparing、owner_reserved和publication后/committed前崩溃并验证定点继续/释放。另让copy finalizer与target/coordinator删除竞争同一gate，验证finalizer先行把copy/board record推进成功终态`committed|published`，删除先行释放reserved claim/ref或验证committed claim归属后持久释放target owner ref，再推进删除终态`target_deleted|coordinator_deleted`，且恢复不重建/保留owner或复活target。required blob/claim缺失阻断copy，非required unavailable历史ref保持明确不可用；多copy/同digest claim不串用且不产生重复正文。
  - 6.8-H 为fixture composition增加按operation/phase寻址的`SessionLifecyclePhaseBarrier`与`CopyCaptureBarrier`/observer，生产绑定立即返回no-op；覆盖deletion journal/fence/final rename、source revision freeze、attachment claim/captured marker/owner_reserved、target/board publication及attachment finalization。barrier只暂停并ack，不改业务状态/identity/time/result或写业务库；pytest从进程外终止/重启，验证并发operation不串扰且不存在生产test路由、热开关或backend自杀hook。

## 7. 产品回归与交付门槛

- [ ] 7.1 更新Agent factory/deep stack/Resource platform/VRN/Skill/AGENTS/config/compaction、main-only Goal、child-thread subagent、target-main session messaging/read/wait、Session内部team、terminal精确callback、runtime idle manager、retry/checkpoint、ToolSelectionStore/ToolService/execution step/ToolSet registry、rollout context integration、sealed dispatch bridge/Provider projection和recovery fixture；删除PromptReplay测试、旧反向捕获预期、旧config/Skill/AGENTS独立watch/read路径、`.boxteam`模型Skill挂载/read白名单、`monitor_session_agent_end`、模型工具`simulate_user`及正常runtime跨Sessionteam状态，禁止任何旧入口兼容层、v1 fallback、直接内部message注入、Turn开始后永远沿用旧Agent工具集或第二checkpoint writer。
- [ ] 7.2 运行修改 Python 文件对应的 Ruff、compileall和 targeted pytest，并保留退出码、测试计数和失败诊断；未验证项不得勾选。
- [ ] 7.3 扩展并运行`uv run pytest tests/e2e/clients/web/test_basic_chat_tool_loop.py`：保留基础两轮精确时长/item数量/展开顺序/刷新无重复断言，并在该唯一Python E2E owner内验收多Session main、用户消息提供本地/远端Session ID或link后的周报、同Session child右侧对话与串行队列、Session内team、fake-clock cold runtime、fake-deadline wait、跨workspace/server send→wait/read/report、hub transit grant和失败零副作用，以及target-accepted但未execution-bound时backend restart后按同一communication恢复且只投递一次。cold/restart前后必须逐字段核对持久GraphBinding四元组与实际factory revision，新runtime generation不得改用latest graph。pytest外置`E2ETestControlHarness`提供独立residency/wait时间域与barrier，test backend仅由fixture composition连接；生产不注册test client、HTTP/model入口或热开关，harness不写业务状态。barrier释放后必须先在source未调用read/wait时由只读communication/trace证明InboxAdmissionWorker主动建立binding，再调用wait。允许调用helper，但pytest场景编排与PASS/FAIL gate不得仅存在于Node脚本；联合检查DOM、网络/通信账本、thread history/SSE和runtime trace。A/B/C使用测试目录内各自隔离BOXTEAM_HOME、peer credential和policy snapshot组成`B ⇄ A ⇄ C`三个进程；A主动建立到B/C的loopback全双工channel，B在该channel反向发起请求，覆盖默认允许、连接中撤权/恢复、响应原路返回及source不冒充。不得读取或写入用户正常全局配置、访问真实Provider、以反向SSH、B/C直连、resolver stub/remote-backend直连或进程内barrier冒充通过。
  - 7.3-A 同一E2E模块还要覆盖target已accept但source未持久化receipt时的source backend重启，以及Job已create-or-get但inbox未binding时的target重启；恢复后按原send operation/communication/admission只得到一份target inbox、ambient item、Job和Turn。
  - 7.3-B 同一E2E模块以用户消息提供远端裸ID触发真实hub有界exact-ID discovery，证明origin channel绑定、hub transit discovery grant只做target spoke本地lookup、唯一target后另发绑定origin/path的operation grant；覆盖多候选要求qualified link、未授权不泄露、hub/target nonce跨重启防replay及registry故障零副作用、WebSocket/SSH重连轮换channel epoch/route revision但原outbox不改target/communication/preimage或重新裸ID选路、wait grant寿命不足，以及运行时撤权/恢复只影响下一实际调用或披露且不改ToolSet/stable prefix；不得把预先解析好的remote backend地址直接喂给workspace冒充通过。
  - 7.3-C 测试composition用pytest fixture注入确定性UUIDv4-profile IdentifierFactory，所有Session/child继续走正常Gateway/Web API；ModelStream按动态identity精确匹配。active child第二条用户消息在第一条execution继续tool-loop期间保持queued，Provider request/history/DOM共同证明ExecutionContextFence不泄露未来root且刷新后Turn因果顺序正确。模块标记独占serial并以PID/start-time验证的E2EProcessLease租用避开8010–8016的端口，禁止杀未知监听者、抢占有效lease或共享开发home，teardown只关闭本次owner并验证端口释放。
  - 7.3-D 周报至少分页读取一个远端Session，并在页间制造目标新增Turn/view变化；断言报告只使用首个AEAD ReadContextSnapshot，cursor每页重授权，撤权、过期、参数冲突及token篡改不泄露内部字段、不混入新revision或产生目标副作用。
  - 7.3-E 同一E2E模块把两个child加入单个Session team并从右侧侧边栏核对role/task/coordinator；在child排队期间及active tool-loop两次model call间修改team状态，分别验证默认turn snapshot不漂移及显式model_call配置在下一安全call追平且revision不重影、cold child不被fanout唤醒，并断言跨Session member attach或消息内team/task/Goal状态被拒绝且无部分ledger/context mutation。
  - 7.3-F 在target冻结远端read page或无selector wait baseline后、source保存response/tool result前重启source；恢复同一source call/tool invocation必须返回同一RemoteObservationRecord/PageRecord结果，期间新增Turn/Job不混入且该调用只有一个tool result；read第二页使用新source call但继续cursor绑定的同一observation/read series。wait的相同origin进程重启及origin变化都不能延长原DurableDeadline，不安全恢复显式失败；过期显式`operation-retry-expired`。
  - 7.3-G 在同一child的第一条execution等待tool result时提交第二条queued用户Turn/root，再让第一条继续追加reasoning/tool/final item，刻意形成两个Turn物理item sequence交错；history API、live DOM和刷新DOM必须分别按`turn_item_projection.logical_item_ordinal`得到各自准确的identity/order/`item_count`/`elapsed_ms`，并证明`first_item_sequence`/`last_item_sequence`之间夹入的另一个Turn item不会被计数、展开或计时。
  - 7.3-H 为专用Session在创建目标Turn前通过正常workspace配置/ToolSet API启用`compact_conversation`，再在基础工具循环的独立Turn中让确定性模型调用该真实产品工具触发一次context compaction；禁止test-only压缩开关、绕过正常ToolSet流程或预写projection。断言canonical `compaction_summary`以同identity的独立可展开history response part出现在正确logical ordinal，相对于该Turn其它可展开成员（包括压缩操作自身的tool_call/tool_result）只额外计1且elapsed由后端给出；live、终态history和刷新DOM均不得把它伪装成thinking/final、重复carrier或按DOM节点重算。
  - 7.3-I 在同一E2E模块用barrier让child创建/发布、team mutation/fanout、target inbox acceptance/execution binding及独立`skill_load`/ToolSet control分别与Session删除竞争；每类都验证gate决定唯一顺序：操作先行时存在捕获旧generation的可恢复lease且删除等待terminal，删除先行时workspace无新thread/context/inbox/ambient/wakeup/Job/attachment副作用。另让已开始的cold history shared guard阻塞删除取得topology exclusive并提交catalog deleting，reader完成后删除才线性化；在catalog已deleting、部分local fence仍active的注入崩溃点重启backend，断言catalog metadata可观察但新thread history/detail及mutation明确`session_deletion_pending`、恢复继续同一删除且无重新active或late callback写入，并验证排空期间受许可旧lease可短时重取gate而不死锁。
  - 7.3-J 让target read/wait的`RemoteObservationRecord`建立与Session删除竞争：删除先行时不得产生snapshot/baseline/subscription，observation先行时删除按原lease完成冻结读取或返回明确target-deleted并等到terminal；覆盖最大300秒wait被删除取消、source恢复时每个source call只收到一个tool result。catalog tombstone后普通resolver不可打开隔离节点，但同一source call在恢复窗口内以新授权可定点重放同一terminal envelope；窗口结束才物理清理并返回`operation-retry-expired`，不得提前清理或静默重开observation。
  - 7.3-K 同一E2E模块继续覆盖本地copy/source snapshot、pinned retention/release、copy attachment claim/GC、target删除/finalization及federated fork拒绝；所有竞态和崩溃点使用按operation/phase寻址的lifecycle/copy barrier，pytest收到ack后从进程外重启。断言生产composition只有no-op observer且无test HTTP/model入口，barrier不改业务事实；详细场景、DOM/history/API/trace交叉证据与唯一PASS/FAIL owner以itemized change的8.13-K至8.13-O为准。
  - 7.3-L 同一E2E模块在不重启开发服务的情况下修改AGENTS和tracked Skill：验证ResourceRegistry先发布revision、默认turn的当前tool-loop保持旧snapshot且下一Turn生效、显式model_call配置在下一安全call生效、请求链路零文件I/O；工具结果/history只显示安全`boxteam://.../resources/...` URI，通用read提交URI或旧`.boxteam` Skill路径明确失败，刷新后resource provenance不重复、不泄露物理locator。
  - 7.3-M 与itemized 8.13-Q复用唯一Web E2E模块：在目录pending move/rename及202已接受但worker未提交期间，让同一Session main/child继续model tool-loop、history冷读与checkpoint恢复，断言owner binding、CSM/ToolSet revision、sealed prefix bytes、catalog locator均不随乐观树改变；排队删除前已提交active仍允许有lease的业务，整树deleting commit后即使Web事件延迟或部分local fence仍active也零新业务副作用。纯导航成功/失败/回退只影响UI父链，不产生source diff、epoch或重复assembly。
- [ ] 7.4 对真实 Provider request log、封存 frames/tools、stable-prefix epoch/reason/manifest、desired/applied ToolSet revision、checkpoint state、assembly selection和 history projection做端到端 provenance audit。
- [ ] 7.5 运行OpenSpec strict validation、54项未完成任务计数、git diff/status和架构/import audit；确认本change与`add-itemized-rollout-context`的ResourceActivationSnapshot/ResourceProvenance、SessionThread/GraphBinding、main/child capability、runtime residency、Session内部状态和跨Session无状态协作边界清晰后才进入apply/implementation。
