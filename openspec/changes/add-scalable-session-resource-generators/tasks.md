## 1. Gateway 工作区组织

- [x] 1.1 新增 Gateway 工作区导航 DTO、原子持久化服务和依赖装配
- [x] 1.2 新增工作区文件夹/引用的创建、更新、移动、排序、删除、breadcrumb API 与校验测试

## 2. 可扩展会话目录

- [x] 2.1 修改 AGENTS 存储契约，新增物理节点命名、folder manifest 与集中式 SessionPathResolver
- [x] 2.2 新增旧扁平目录和旧逻辑文件夹关系的带锁、带账本、校验后归档迁移
- [x] 2.3 将会话创建、读取、移动、重命名、删除、导出和全部会话附属存储切换到 resolver
- [x] 2.4 从物理树重建目录索引，提供根节点/子节点分页、breadcrumb、名称搜索、显式刷新和 unhealthy 诊断 API
- [x] 2.5 新增 Gateway 跨工作区轻量目录搜索、离线/过期状态和定位结果

## 3. 通用 Session Generator

- [x] 3.1 新增 Generator 类型注册、定义、触发器、挂载、命名、上下文、策略和运行 DTO
- [x] 3.2 新增 Gateway GeneratorDefinition 与 GenerationRun 原子持久化、CRUD、预览和手动触发 API
- [x] 3.3 新增工作区生成执行 API，完成 new_per_run、continue_existing、fork_new_and_report_back 与幂等
- [x] 3.4 将 placement、目录模板和会话名称模板接入物理目录创建，确保预览模板与落盘共用规范化规则且运行结果返回实际路径
- [x] 3.5 新增跨工作区控制面挂载、执行环境分离拒绝、离线/能力缺失、挂载失效、并发与回写状态测试

## 4. Web 会话资源管理器

- [x] 4.1 新增 Gateway 导航、会话目录、搜索和生成器浏览器 API 类型与状态控制器
- [x] 4.2 将会话侧栏改为工作区文件夹 + 按需会话树，提供加载、分页、搜索定位、高亮和错误反馈
- [x] 4.3 新增生成器创建/预览/运行入口和三种 session strategy 的状态展示

## 5. 质量与独立审查

- [x] 5.1 运行 Python 静态检查、单元/集成测试和 `bun --cwd src/web run build`
- [x] 5.2 编写并执行隔离的 Gateway、会话目录、生成器和真实浏览器 E2E
- [x] 5.3 完成项目架构 subagent 审查并修复 findings
- [x] 5.4 完成 real-user-ux-auditor Web 审查并修复真实用户体验问题
- [x] 5.5 验证旧布局迁移、手工移动/重命名、递归删除、索引重建及 Windows 路径边界

## 6. 物理子会话与资源树交互修正

- [x] 6.1 扩展 SessionPathResolver 扫描、创建、移动、迁移和稳定 ID 解析，支持会话 `children/` 物理子树并校验 `parent_session_id`
- [x] 6.2 将绑定、解绑、上下文分叉、父会话移动、级联删除和导出接入物理子树语义，拒绝跨工作区或循环绑定
- [x] 6.3 把会话与会话文件夹低频操作迁入右键菜单，增加剪贴板移动并将文案统一为“绑定为子会话”
- [x] 6.4 补充路径解析、服务、迁移、目录 API 与 Web 交互测试，完成静态分析、Web 构建和隔离真实浏览器验证

## 7. 拖放组织与多层工作区文件夹

- [x] 7.1 整合 Gateway `parent_workspace_id` 与多层虚拟工作区文件夹的拖放展示和测试，确保不触碰工作区物理目录
- [x] 7.2 扩展会话目录移动 API，支持文件夹进入文件夹或会话 `children/`、节点回到工作区根，并校验同工作区与循环边界
- [x] 7.3 为 Web 资源树增加工作区、工作区文件夹、会话文件夹和会话的受约束拖放，成功或失败后均刷新后端权威状态
- [x] 7.4 将已有工作区文件夹的新建子项、重命名和删除收进右键菜单，以树内编辑替代重命名弹窗
- [x] 7.5 补充前后端交互测试，运行 Python 静态分析、相关测试与 `bun --cwd src/web run build`
- [x] 7.6 保留导航变更前的用户展开态，消除叶子会话伪“空文件夹”，并为工作区文件夹提供明确的根级拖放入口和浏览器回归
- [x] 7.7 将工作区文件夹根改为右键标题，把会话文件夹创建入口迁入工作区/会话菜单，支持安全递归删除虚拟工作区文件夹，并统一会话资源暖色子窗口
