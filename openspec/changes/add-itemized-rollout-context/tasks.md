## 1. Canonical item 与格式合同

- [x] 1.1 在 schema/type 层冻结 `CanonicalItemRecord`、`TurnRecord`、`ExecutionRecord`、`ModelCallRecord`、`ContextRef`、`storage_commits` 以及 v1/v2 envelope 的字段名、枚举、hash 和 status 约束；v2 核心必填非空字段固定为 `format_version`、`record_type`、`item_sequence`、`item_id`、`semantic_kind`、`payload_kind`、`status`、`producer_ref`、`payload`、`content_hash`、`created_at`、`metadata`，`content_hash` 固定为 payload_kind+payload 的 `sha256:jcs:v1` JCS/SHA-256 小写 hex，`status` 固定为 `completed|partial|incomplete|cancelled|failed|unknown` 且只允许单向终态化，`payload_kind` 固定为 `text|structured_content|tool_call|tool_result|summary|attachment_ref|opaque|extension`，`extension_schema`/`extension_version` 为非空 namespaced schema/version 字符串，并落实未知值处理，`turn_id`/`turn_scope`/`message_group_id`/`wire_role` 按语义可空，并落实 root/member/ambient/pending notice 约束；区分 item `status`、`tool_outcome` 与 `ControlOutcome=completed|completed_empty|failed|interrupted|cancelled|execution_lost|unknown`，全局不得引入带 outcome 前缀的 unknown 状态别名；禁止引入含义重叠的通用 `kind` 或把 `assistant_text` 当作 canonical kind
- [x] 1.2 定义 `ContextRef` 的 `ref_type=canonical_item|request_only` 与统一 `ref_id` 两种引用形态，确保 `request_only` 不出现在 canonical item 字段或持久化 item catalog 中；`ToolSetRef` 作为 selection source union 的独立 `ref_type=tool_set` 分支，不扩展 ContextRef 枚举；在 ContextRef/selection 中，`ref_kind` 和 `request_only` boolean 仅可在一次性 `legacy_import_v1_to_v2` ingress adapter 内部归一化，wire/persistence/registry/selection 一律使用 `ref_type`/`ref_id`，并落实 canonical item、plan-draft request-only 与 sealed assembly selection 的互斥解析约束。`ContextRef` 不携带 assembly discriminator：最终合同要求它以 `(session_id,thread_id,item_id)` 或 `(session_id,thread_id,plan_id,ref_id)` 解析，assembly binding、最终 `detail_ref` 和 `plan_ordinal` 只能由 sealed `ContextSelectionEntry`/manifest 提供；跨 session fork 必须创建 target-local session/thread/plan/ref/detail identity，source ref 只保留在 fork lineage/audit，不得作为 target operational lookup。`ContextContribution.request_only=true` 是允许持久化的贡献本体不变量，不能复制为 ContextRef/selection discriminator。原勾选只证明旧session-only阶段的实现，新增thread-qualified重验收归Section 8
- [x] 1.3 实现 v1 message-line/v2 item-line 的显式 format dispatch：SQLite `rollout_format_version`、JSONL `format_version`、v1 `record_type=message` 和 v2 `record_type=item` 必须一致且禁止混写
- [x] 1.4 为 v2 item、content part、producer/provenance relation、Turn、execution、model call 和 assembly 建立确定性 fixture 与 schema 校验；逐行覆盖九个 semantic kind 的 compatibility matrix（允许 payload_kind、允许 item status、tool_result 的 tool_outcome marker 和其它 marker 禁止规则），覆盖表外组合的 `item-schema-incompatible`、unknown kind/unsupported extension 和 immutable JSONL recovery；Execution/ModelCall/assembly/storage/control 的结果字段固定为 `outcome`，不得与 item `status` 混用。fixture 还必须冻结并校验 `ContextRef` 的 canonical/request-only 字段、`ToolSetRef` 的 `ref_type=tool_set`/`ref_id=tool_set_snapshot_id`、plan/assembly scope、protection/availability，且 ContextRef/selection 不得出现 `ref_kind`/request_only boolean；允许且必须校验 `ContextContribution.request_only=true` 这一贡献本体不变量。对 `ContextSelectionEntry`，只有 `included=true` 才强制 `source_revision`、逻辑 `content_length` 和恰一个 `content_hash|redacted_stable_digest`；included request-only 才强制同 assembly 的最终 `detail_ref`，included contribution-backed source 才分配 `contribution_ordinal`，included canonical/tool_set 分别来自 item catalog/ToolSetSnapshot manifest 且不携带 detail/contribution binding。`included=false` 仅用于 optional omission，必须保留 tagged ref、`plan_ordinal`、`omission_reason`、`loss`、`availability` 和可得 source identity，正文 length/hash、detail_ref、contribution_ordinal 可为 NULL/未分配，已知 metadata 必须与 manifest 一致；projector/restore/history 跳过 omitted 正文/工具定义，不回退当前 source。还必须校验 ContextRef 在 draft 只按 session/plan registry 解析、仅 sealed `ContextSelectionEntry` 生成 `assembly_id`/`plan_ordinal`/最终 detail_ref，以及 selection 的 tagged-union ref、selection/loss 字段和 plan/snapshot 持有关系。还必须验证 unsealed plan 的 `plan_id`/空 selection/NULL assembly 与 Saver seal 后的 assembly_id、ContextRef/ToolSetRef registry 和各自 manifest 完全一致，union/selection_kind 不匹配、重复或不一致返回 `plan-order-integrity`/`source-mismatch`/`detail-unavailable`，`tool_set` 只归属 ToolSetSnapshot registry 且不占 item/contribution ordinal。验收必须调用真正的 RFC 8785 JCS 实现，禁止以 `json.dumps(sort_keys=True)` 冒充；fixture 必须覆盖 IEEE-754/ECMAScript 可表示有限浮点、`-0`、指数形式、Unicode 以及 UTF-16 code-unit key 排序和非法数字，且为 `content_hash`、`plan_hash`、`request_hash`、幂等键及 legacy seed 提供跨语言 golden vectors，证明重启/跨语言恢复使用同一 preimage

1.4 的 ToolSetRef 验收还必须把 `tool_set_refs[]` 的 manifest identity（`tool_set_snapshot_id`、`source_revision`、`content_length`、`content_hash|redacted_stable_digest`、`tool_set_schema`/`tool_set_schema_version`、`tool_policy_version` 和绑定的 `tool_policy`）纳入 `context-plan-hash:v2` preimage；工具 schema/config、policy 或 source revision 变化必须触发 `plan-hash-mismatch`/新 plan，跨 provider 仅允许 request_hash 因 wire 编码不同而不同。`ContextContribution.contribution_kind=tool_set` 不属于该 fixture 的合法输入，必须返回 `contribution-kind-unsupported` 或进入 legacy quarantine，不得生成 Provider tool definitions。

1.4 的 selection fixture 还必须验证 `ContextSelectionEntry.contribution_id` 的显式映射：included 且 contribution-backed 的 request-only/overlay entry 必须以非空 `contribution_id` 唯一解析同一 `(session_id, thread_id, plan_id)` 的 `contribution_manifest`，并逐字段匹配正文/detail ref、source revision、logical length、hash token 和 `contribution_ordinal`；普通非 contribution-backed request-only 只校验其 detail/source manifest，不要求 contribution_id/ordinal；`ContextRef.ref_id=plan_item_id` 不得被当作 contribution identity。canonical_history/tool_set entry（包括 omitted）必须拒绝非空 contribution_id/contribution_ordinal/detail_ref；只有 omitted request_only/overlay entry 可保留已有且一致的 contribution_id，不得新分配 contribution_id/ordinal 或读取正文/detail，没有既有映射则为 NULL。该映射、重复/缺项和三投影恢复证据已纳入本任务验收；thread-qualified部分仍由Section 8重验收
1.4 还必须按唯一矩阵校验 `canonical_history->canonical_item`、`request_only->request_only`、`overlay_base|overlay_delta->request_only`、`tool_set->ToolSetRef(tool_set)`；矩阵外 union/selection_kind 组合在 source lookup 前返回 `plan-order-integrity`，omitted entry 也不能改变 tag。canonical/tool_set 的 contribution_id、contribution_ordinal、detail_ref 必须为 NULL，overlay included 才校验 base/delta role、source_overlay_epoch 和 diff chain；restore/projector/history 对 omitted entry 只保留 omission/loss 并跳过对应正文、detail 或 tools

1.1–1.4 的旧勾选只证明当时带通用 `metadata` 的实验 v2 形态；本轮新字段合同由以下未勾选任务重新验收，不能沿用旧 PASS 宣称完成。

- [ ] 1.5 建立唯一的核心字段清单与 owner/type/中文业务注释/序列化名/SQLite 列或 manifest/API 投影/失败码映射：至少覆盖 item 投影归组、message/model-call/content-part/reasoning identity、tool-result 身份和执行确认、source revision/可见性/保护、supersession/fork关系，以及 contribution 的 selection role、replacement policy、typed source/overlay binding。将 `CanonicalItemRecord.metadata` 改为仅含 namespaced/versioned 非决策数据的 `extensions`，定义并校验 `ProjectionGroup`、`ItemProjectionIdentity`、`ToolResultEvidence`、`ItemSourceManifest`；`ContextContribution` 使用 typed `selection_role`/`replacement_policy`/`source_binding`/`extensions`，owner-thread registry返回 `RegisteredContribution` 并独占分配/恢复 `source_ordinal`，不在 contribution 或 ledger 内补号。逐个审计 domain、writer、checkpoint codec、fork/remap、history/provider projection 中决定身份/排序/选择/替换/结果/恢复/安全的 `metadata.get`/字符串索引，迁往 typed owner或显式 relation；新扩展默认 opaque，只能经新增 typed capability 参与决策。直接删除旧实验 v2 字段读取/双写/alias；旧 v2 数据不自动迁移，schema-incompatible 时原件不动，v1 一次性 importer仍独立。
- [ ] 1.6 在正式 schema fixture、JSONL/SQLite round-trip、registry/ledger/plan seal、checkpoint/fork/history/provider 与重启恢复测试中验证 1.5：`projection_group` 全组/缺员/重复ordinal、纯文本tool result无身份、source slot replace与多assembly ordinal、typed source binding与selection冲突、扩展伪造 `selection_only`/`replaceable_source`/`source_ordinal`、未知扩展保留但不驱动核心、旧 v2 metadata拒绝、SQLite约束/JSONL分歧失败和跨投影相同 identity/order。静态审计禁止核心路径再按自由 metadata key 分支；记录逐项 PASS/FAIL，不以当前 1.1–1.4、3.x 或 6.x 勾选代替。

## 2. Turn、execution 与 item 生命周期

- [x] 2.1 在 acceptance-time 以`(session_id, thread_id, accepted_ingress_id)`和`(session_id, thread_id, acceptance_idempotency_key)`原子创建或恢复 `TurnRecord`、真实用户 `user_input` root 和 `initial_execution_id`，为两类 identity 建立各自 owner-thread-local 唯一约束及 key/ingress/payload/branch 冲突语义，不得从 wire role、LangChain 类型或首个物理 item 推断 Turn；原勾选只证明旧session-only阶段，thread-qualified验收归Section 8
- [x] 2.2 实现 `TurnExecutionLink`、`ExecutionRecord`、`ModelCallRecord` 和 canonical output item 的 identity/关系模型，覆盖同一 Turn 多次 execution/model call 与 `final_item_id` 状态约束
- [x] 2.3 实现 provider retry 的新 `model_call_id`/attempt 和 execution resume/restart 的新 `execution_id`，禁止复用已提交 output item identity
- [x] 2.4 实现 `turn_finalize` 与 `final_item_id` 的 terminal convergence 原子合同；冻结 `Turn.status` 为 `open|active|completed|completed_empty|interrupted|cancelled|failed|unknown` 及 design/spec 中的逐状态转移表：仅显式 `resume_turn` 允许 `interrupted -> active` 或 reason=`execution_lost` 的 `unknown -> active`，且必须创建新 execution；`completed_empty` 是唯一无 canonical output 的正常终态名称，`completed|completed_empty|failed|cancelled` 不再转移，普通 `cancelled` 与 full rollout copy 的 cancelled historical 都永久不可恢复；`history_replay` 不创建 execution，绑定原 Turn 的 `dispatch_replay` 对 cancelled 返回 `turn_not_resumable`，独立的 `replay_as_new_turn` 才能创建新 Turn/root/acceptance/initial execution 并带 `replay_of_turn_id`，不能在同一 API 语义中同时返回错误并创建新 Turn；`partial` 只属于 `CanonicalItemRecord.status`，partial item 不得成为 final item，也不扩展 Turn.status 枚举
- [x] 2.5 覆盖真实用户 root、synthetic runtime notice、provider retry、无新用户 resume 和 finalization failure 的恢复测试

## 3. Rollout JSONL、SQLite 与提交边界

- [x] 3.1 实现 v2 item-line JSONL writer/reader，使 canonical item 正文只以不可变 JSONL 记录为事实源，raw provider chunk 只进入实时 draft/delta；只允许 `draft -> completed|partial|incomplete|cancelled|failed|unknown`，提交后禁止更新/删除/覆盖/重排，未知 payload/status 不能静默降级
- [x] 3.2 根据 schema review 冻结 item catalog 的最终表名和类型归属：`item_catalog`、`item_relations`、`item_projections`、`item_parts`、`operation_anchors`、`context_assemblies`、`assembly_item_refs`、`storage_commits`；不复用 `messages` 作为 canonical item 表。Python 归属落在 `app/domain/itemized/`、`app/services/infrastructure/rollout_context/` 和 `app/services/mapping/itemized/`，保持最小 catalog、关系/投影/parts/anchor 按需建立的边界；旧 `app/core/itemized_rollout.py` 聚合入口和临时 import shim 已删除
- [x] 3.3 实现 sealed-before-dispatch 与 terminal convergence 两阶段 commit、JSONL durability barrier、`commit_kind`=`acceptance|assembly_sealed|item_convergence|terminal_convergence`、正交 `commit_mode`=`item_bearing|metadata_only`、SQLite committed offset/index transaction 和启动恢复；一个 SQLite 事务只写一条 storage commit，可覆盖多个 item；冻结 `database_meta.committed_jsonl_offset` 为单一权威，要求其与每条 `storage_commits.jsonl_offset_after` 等值、按 commit 链单调一致，冲突时停止恢复
- [x] 3.4 建立 Turn root、`(turn_id, item_sequence)`、view logical ordinal、`history_view_revision`、source overlay lineage 和 semantic kind/wire role 的快速索引，避免从 JSONL 头部线性扫描
- [x] 3.5 实现 canonical item、Turn finalization、assembly terminal outcome 与 checkpoint/view projection 的收敛提交、commit idempotency/conflict 校验和一致性校验。分别闭合：3.5-A JSONL durability/offset barrier 与 `database_meta`/`storage_commits` 冲突、链断裂和越界；3.5-B metadata-only 不推进 offset 且能原子记录空输出/失败/执行丢失；3.5-C item-bearing terminal convergence、Turn/final item/assembly outcome 与 checkpoint/view projection 的同事务一致性；3.5-D commit idempotency、崩溃恢复和“同一 terminal outcome 不拆成两种 mode 或多 item 单 commit”

## 4. 实时 provenance 与 ContextAssemblySnapshot

- [x] 4.1 实现内存 `ItemDraft`、`ContextContribution`、`ContextRequestPlan` 和 provenance graph，区分 `producer_ref` 与 middleware/tool/memory 的 influence/transform relation
- [x] 4.2 将 static system prompt、skill、environment、memory 和 tool definitions 建模为 request-only contribution/独立 `ToolSetSnapshot` registry，并为已进入 request 的可变 source 记录 source revision；工具定义通过 `ToolSetRef` 进入 selection 和 Provider tools/tool-config，不作为 ContextRef 或 canonical history；`ContextContribution` 的 `request_only` 持久化字段必须存在且恒为 `true`，它是贡献本体不变量而不是 ContextRef/selection discriminator；`ContextContribution.contribution_kind` 只允许 `prompt|overlay_base|overlay_delta|notice`，`tool_set` contribution 非法，legacy 记录只能 quarantine；禁止 middleware 原地编辑 canonical history
- [x] 4.3 实现 pending runtime notice 的 ambient/pending-next-turn 语义，确保它既可被当前或下一请求引用，又不进入 normal Turn root/member 范围
- [x] 4.4 冻结 `sha256:jcs:v1` 的 content/plan/request preimage、敏感字段 redaction、跨 provider `plan_hash` 和 projector-specific `request_hash` 合同；运行时直接接入 RFC 8785 JCS serializer，并以浮点、`-0`、指数、UTF-16 key ordering、非法数字拒绝、跨语言 golden vectors 以及 content/idempotency/legacy seed 正式测试完成验收，禁止以 `json.dumps(sort_keys=True)` 替代
- [x] 4.5 在 provider dispatch 前封存 `ContextAssemblySnapshot` 的 `history_view_revision`、`source_overlay_epoch`、item/request-only refs、ToolSetSnapshot、compiler/provider 版本、loss/redaction 和 hash 字段；ToolSetRef registry/manifest、稳定 `contribution_ordinal`/`plan_ordinal` 与 Saver-owned 统一 selection/order 已由 1.4/6.5/6.6 验收闭合
- [x] 4.6 固化 `RolloutCheckpointSaver` owner boundary，业务 service、middleware adapter 和 projector 只能消费 Saver 提供的已提交 plan/snapshot，不得旁路访问 RolloutStorage、AppendWriter 或内部 context reader
- [x] 4.7 在 session/thread catalog/path resolver 定位的真实thread节点下，以精确相对路径 `rollout/context-plan-details/<assembly_id>/<detail_id>` 实现受保护 `ContextPlanDetailStore`，完成 required/optional detail failure、权限、敏感字段、retention/GC、tombstone 和 detail-unavailable 错误；`detail_id` 是 assembly 内 target-local 的不可变物理叶名，`detail_ref` 是解析为 `{session_id, thread_id, assembly_id, detail_id}` 的逻辑 typed reference，不是调用方可拼接的路径。included detail/ref 必须同时保存非空 `source_revision`、逻辑 `content_length` 和恰好一个 `content_hash`/`redacted_stable_digest`，供 ContextRef/selection 恢复校验；optional `included=false` omission 仍保留 tagged ref、`plan_ordinal`、`omission_reason`、`loss`、`availability` 和可得 source identity，但 detail_ref、content_length、hash token、contribution_ordinal 可为 NULL/未分配，已知 metadata 必须与 manifest 一致，且 projector/restore 不得回退读取正文。跨 session/thread materialization时必须同时创建 target-local detail_id/detail_ref，禁止共享 source detail。`sensitive=true` 时普通文件不得落盘原文，必须使用 redaction 或独立受保护/加密存储；从 resolved thread root 到 assembly/detail target 的每一级路径组件都必须通过 `lstat`/等价检查拒绝 symlink，并以 realpath containment 拒绝越界。`required_detail=true` 若 detail 缺失、不可读、source/content hash 不匹配或仅有 redacted marker 而无允许的 protected body，必须拒绝 seal/dispatch；optional omission/loss 必须进入 plan/diagnostics。以下四个相互独立的门槛均已取得旧阶段正式测试证据，但thread-qualified路径/identity仍归Section 8重验收：
  - 4.7-A 路径/权限门槛：session/thread catalog 定位、`rollout/context-plan-details/<assembly_id>/<detail_id>` 物理归属、逐级 no-follow/lstat、realpath containment、访问控制和跨 session/thread target-local resolution。
  - 4.7-B 内容保护门槛：included detail 的正文、`source_revision`、`content_length`、`content_hash|redacted_stable_digest`、sensitive redaction/protected backend、retention/GC/tombstone 的持久化与恢复；optional omitted detail 只需持久化 tagged ref、omission/loss/availability 和可得 identity，不分配正文完整性字段。
  - 4.7-C seal/dispatch 门槛：required detail 缺失、不可读、保护级别不足或 manifest mismatch 必须在 seal 前 fail-closed；optional `included=false` omission/loss 必须进入 plan/diagnostics，允许缺少 detail_ref/content_length/hash/contribution_ordinal，不能变成空正文或伪造 included entry。
  - 4.7-D projection/fork 门槛：sealed binding 能向 LangChain/native Provider/history 显式传递 detail availability/loss；普通 included request-only 只通过同 assembly `detail_ref`/source manifest 恢复，included contribution-backed request-only/overlay entry 才通过非空 `contribution_id` + `contribution_ordinal` 唯一绑定 contribution/detail manifest，为 fork 建立 target-local detail ref；必须同时拒绝 `canonical_history` 非 canonical_item、request_only/overlay 非 request_only、tool_set 非 ToolSetRef 的 union mismatch，并让 omitted entry 按矩阵保留 tag/loss 后跳过正文/detail/tools；canonical/tool_set 的 contribution_id、contribution_ordinal、detail_ref 永远为 NULL，optional omitted entry 可为空且只保留 omission/loss
- [x] 4.8 实现 assembly persistence failure 拒绝 dispatch、provider call 后提交前崩溃标记 unknown/execution_lost，以及成功结果与 canonical/Turn outcome 的原子收敛
- [x] 4.9 旧阶段已实现缓存保持型 source overlay 的base/delta、`source_overlay_epoch`、provenance和幂等原语；该勾选只证明旧原语，不证明“source失效即可物化”的旧生命周期仍是目标合同，stable-prefix/合法epoch修订由未完成的8.3-B重新验收
- [x] 4.10 旧阶段已实现区分history view与source overlay的`ContextReconciliation`骨架；该勾选不证明rewind可跨cutoff重新注入旧delta，tracked/snapshot/untrack与合法epoch语义由未完成的8.3-B重新验收

## 5. Content part、stream 与操作边界

- [x] 5.1 将 normalized provider block/delta 映射到可终态化的 item draft/content part，保持 item identity、part order、tool identity 和 reasoning protection
- [x] 5.2 实现最小 `content_part` anchor：稳定 item/part identity、part semantic kind、ordinal、content/prefix hash、before/inclusive、view/branch 和 recovery capability；冻结 part 正文位于父 JSONL payload，`item_parts` 只作带父 line hash 校验的 locator 派生索引，detail store 不承载 canonical part
- [x] 5.3 明确 token/character/raw chunk 仅在具备独立稳定 identity、长度/hash 和可恢复布局时才能作为 fragment anchor，否则返回不可操作错误
- [x] 5.4 将interrupt优先绑定内存cursor/draft，将durable rewind/replay/compaction/fork绑定已提交item/content-part anchor；request-only、pending notice和未物化source delta不得成为历史边界。该旧阶段勾选仅证明anchor/view原语；rewind/compaction的新prefix epoch与source恢复/物化边界由8.3-B重新验收
- [x] 5.5 覆盖 partial stream、用户中断、跨重启恢复、stale/unreachable anchor 和跨 branch/view anchor 拒绝场景
- [x] 5.6 固化 fork/replay 的全局 Turn identity 与 view-local logical ordinal：同 owner session 的 `history_replay` 是唯一复用 source Turn/root 的历史 projection，不创建 execution；显式 `resume_turn` 仅按 Turn.status 表复用原 Turn 并创建新 execution；独立的 `replay_as_new_turn` 与新用户输入都必须在 active view 创建新 Turn、新 root、新 acceptance/initial execution 和新的 view-local `logical_turn_ordinal`，并带 `replay_of_turn_id` lineage；`replay_as_new_turn` 可以复制或引用 source history 作为上下文前缀，但 source Turn/root 不能成为新 Turn 的 root；它不是原 Turn 的 `dispatch_replay`，view copy 不隐式执行
- [x] 5.7 固化跨 session fork 的 `(session_id, thread_id, entity_type, local_id)` 复合引用和 source→target target-thread-local identity mapping，覆盖 root/item/tool/execution/model-call/assembly/view/branch/anchor、source overlay epoch/base/delta、detail ref、`accepted_ingress_id`/`acceptance_idempotency_key`、target offset/ordinal、source lineage 与 retention；原勾选只证明旧session-only阶段，thread-qualified验收归Section 8
- [x] 5.8 为 `context_fork`、`history_prefix_fork`、`full_rollout_copy` 分别验证复制范围、target active view、source offset 仅作审计坐标、新输入/new execution 规则，以及 active execution/未终态 assembly 的模式化语义。分别闭合：5.8-A `context_fork` 的 prefix/运行态 preflight 拒绝、目标不存在与 source lineage；5.8-B `history_prefix_fork` 的 anchor 截止、view-local ordinal 与同一 Turn 历史复用；5.8-C `full_rollout_copy` 的 v2 target-local copy、cancelled historical、offset/retention/detail/overlay 语义；5.8-D cancelled Turn 的 `resume_turn`/原 Turn `dispatch_replay` 错误边界，以及独立 `replay_as_new_turn` 创建新 target Turn/root/acceptance/initial execution/logical ordinal 的规则。source history 只能作为前缀/lineage，不能与新 Turn/root 混同

## 6. LangChain、Provider 与历史 projection

- [x] 6.1 旧阶段已通过 `RolloutCheckpointSaver` 实现从已提交 canonical/request-only plan 到 LangChain message 的临时 projection及独立ref/provenance/loss，并按base→delta顺序投影overlay、不把diff写回既有message；当时的system/developer合并证据只可用于首次initial root，不证明post-user source可合并，后者及stable-prefix最终合同由未完成的8.3-B重新验收。以下四个旧阶段门槛均已闭合：
  - 6.1-A plan 输入门槛：Saver 返回含 `plan_id`、`plan_state`、可空 `assembly_id`、`refs[]`、`tool_set_refs[]`、`contributions[]` 和 `selection[]` 的正式结构；未 seal plan 的 selection 必须为空且不得进入 projector，sealed snapshot 才能提供 assembly-scoped selection。
  - 6.1-B source integrity 门槛：每个 `included=true` 的 `ContextRef`、`ToolSetRef` 和 `ContextContribution` 重新解析并校验 source revision、content length、`content_hash|redacted_stable_digest` 与 registry/detail manifest；普通 included request-only 只要求同 assembly `detail_ref`/source manifest，included contribution-backed request-only/overlay 才必须通过非空 `contribution_id` + `contribution_ordinal` 唯一解析同一 `(session_id, thread_id, plan_id)` contribution manifest，并校验 detail_ref/body locator，不能用 `ContextRef.ref_id` 猜测。selection kind 必须按固定矩阵绑定 ref type，矩阵外 union mismatch 在 source lookup 前返回 `plan-order-integrity`。optional omitted entry 只校验对应矩阵的 tagged ref、plan ordinal、omission/loss/availability 和已知 identity metadata，不回退解析正文；缺失 canonical ref、source mismatch、detail-unavailable、重复或 union 不匹配必须分别 fail-closed。原勾选不替代Section 8的thread-qualified重验收。
  - 6.1-C 旧阶段wire编码门槛：included request-only正文按selection/overlay顺序进入LangChain message；只有首次initial-root contribution的system/developer合并证据可沿用，post-user source必须保持独立user-role item且由8.3-B重验收。included ToolSetRef只进入Provider tools/tool-config，不生成canonical message；omitted canonical/request-only/tool_set只报告omission/loss并跳过正文/detail/tools，不得以metadata摘要、当前source或空正文伪造成功。
  - 6.1-D 旧阶段重启/overlay门槛：source overlay base/delta、sealed selection和omission/loss可显式恢复，projector不得自行扫描storage、重新排序或覆盖旧message；rewind后的tracked完整恢复与cutoff语义不由该勾选证明，归8.3-B
- [x] 6.2 实现 `assistant_output`/reasoning content part 到有序 `AIMessage.content` 的投影，以及 tool call/result 到结构化 LangChain tool fields 的投影
- [x] 6.3 更新 LiteLLM/provider adapter，使 v2 canonical item/content part 不依赖最终 AIMessage 反向推断；任何 v1 message/AIMessage reader 只允许作为一次性 `legacy_import_v1_to_v2` staging 输入，不是正常 provider/history/checkpoint/runtime adapter，完成 v2 切换后不得保留旧运行路径。已完成 provider 响应收敛、纯 history/request mapping 拆分和旧入口删除；生产 v2-only import audit 与核心 provider/tool-loop E2E 均有通过证据
- [x] 6.4 实现 history response 的 `assistant_text`、thinking、tool summary、tool call/result 与 `final_response` projection，禁止创建第二个 assistant_text canonical 事实
- [x] 6.5 验证 LangChain restore、native provider item request 与 Web history 使用同一 item selection/order，并显式报告 provider capability loss。完整任务拆为四个独立门槛：
  - 6.5-A selection schema 门槛：只能消费 Saver seal 冻结的 `selection`，每个 entry 具备 `assembly_id`、`plan_ordinal`、tagged-union ref、selection/loss、visibility/protection/availability、base/delta role 和 overlay epoch；唯一兼容矩阵为 `canonical_history->canonical_item`、`request_only->request_only`、`overlay_base|overlay_delta->request_only`、`tool_set->ToolSetRef(tool_set)`，矩阵外组合返回 `plan-order-integrity`。只有 `included=true` entry 才具备并强制 source revision、逻辑 content length、恰一个 hash token，included 普通 request-only 只具备最终同 assembly detail_ref，included contribution-backed request-only/overlay 才具备非空 `contribution_id`、最终 detail_ref 及 contribution ordinal。optional `included=false` entry 仍须满足 tag/type 矩阵，保留 tagged ref、plan ordinal、omission/loss/availability 和可得 identity，正文完整性字段、contribution_id、detail/contribution binding 可为 NULL/未分配。
  - 6.5-B manifest 门槛：included ContextRef/contribution/detail 与 ToolSetRef/tool_set_manifest 逐字段一致；普通 request-only 只校验 detail/source manifest，included contribution-backed request-only/overlay 的非空 `contribution_id` + `contribution_ordinal` 必须唯一解析对应 contribution manifest，并校验同 assembly detail binding；`canonical_history` 的 contribution_id/contribution_ordinal/detail_ref、`tool_set` 的 contribution_id/contribution_ordinal/detail_ref 必须为 NULL，`request_only`/overlay 只按矩阵允许字段绑定。omitted request_only/overlay entry 的已知 identity metadata（包括已有 contribution_id）若存在也必须与同一 manifest 一致，不得新分配 contribution_id/ordinal 或补造正文/detail binding；没有既有映射则 contribution_id 为 NULL。omitted canonical_history/tool_set entry 的 contribution_id/contribution_ordinal/detail_ref 必须始终为 NULL。重复、缺失、union/selection_kind/ordinal/contribution mapping 错误返回 `plan-order-integrity`，included source/hash/length mismatch 返回 `source-mismatch` 或 `detail-unavailable`；ToolSetRef 不绑定 contribution_id/ordinal，omitted canonical/request-only/tool_set 分别不投影正文、detail 或工具定义。
  - 6.5-C 三投影顺序门槛：LangChain restore、native provider request、Web history 只使用同一 selection，禁止按 `created_at`、`contribution_id`、物理邻接或各自 dict 顺序排序；ToolSetRef 仅进入 Provider tools/tool-config，history 不生成工具定义 message。
  - 6.5-D loss/能力门槛：三路径对 omission/loss、reasoning protection、provider capability loss 给出一致且可审计的结果；正式 projection/API/Web 测试覆盖 request-only 正文、tool-set manifest、缺失 canonical ref、omission 与 loss
- [x] 6.6 验证 LangChain 与原生 Provider projector 在相同 source overlay epoch、不同 history view revision 下共享可复用的 base/delta selection。完整任务拆为四个独立门槛：
  - 6.6-A overlay manifest 门槛：每个 `included=true` base/delta entry 持久化 source revision、content length、`content_hash|redacted_stable_digest`、base/delta role、source overlay epoch、assembly-bound `contribution_id`/`contribution_ordinal` 和 selection `plan_ordinal`；overlay_base/overlay_delta 只能绑定 `ContextRef.ref_type=request_only`，`contribution_id` 必须唯一解析该 plan 的 contribution manifest，不能从 base/delta ref 或 ordinal 猜测，delta 另校验 `from_revision`/`to_revision`/`diff_hash`。矩阵外 union 返回 `plan-order-integrity`。optional omitted base/delta 仍须保留 request-only tag、plan ordinal、omission/loss/availability 和可得 source identity；不新分配正文完整性字段、`contribution_ordinal` 或 `contribution_id`，若已有 `contribution_id` 则可保留但必须与同一 manifest 一致，且不得触发正文/detail 读取；没有既有映射则为 NULL，恢复不得应用该 delta。
  - 6.6-B ordinal/restart 门槛：`contribution_ordinal` 在 `(assembly_id, contribution_ordinal)` 内稳定且可重启恢复，selection 按 `(assembly_id, plan_ordinal)` 恢复；不得按 `created_at`、`contribution_id`、物理邻接或 dict insertion order 猜测。
  - 6.6-C 旧阶段history/source separation门槛：相同source overlay epoch可在只读history projection间复用base/delta；该勾选不证明rewind继续复用旧epoch，rewind合法新epoch及tracked/snapshot/untrack行为归8.3-B。
  - 6.6-D projector/loss 门槛：LangChain/native Provider/history 共享 selection，先按 `selection_kind/ref_type` 固定矩阵拒绝 union mismatch；普通 request-only 仅按同 assembly detail_ref/source manifest 恢复，只有 contribution-backed request-only/overlay 才按 contribution_id/ordinal 恢复；缺失 source、hash mismatch、detail-unavailable 和 omission/loss 显式失败或报告，omitted canonical/request-only/tool_set 分别跳过正文、detail、tools，overlay omission 不应用；不得把 request-only refs/contributions 无条件 prepend 到 canonical messages
- [x] 6.7 将 Turn 可展开活动收敛为后端权威逻辑 Item 投影：SQLite 返回稳定 item identity/order/range/time 与完整 item_count，按 producer/tool identity 解析 checkpoint 重影；detail 只按索引补正文。Web 仅在 live 阶段临时计算并在终态与 history 对比，收到 history 后整体替换 response parts，禁止前端按正文去重或重排。验收覆盖相同正文不同 identity、多 tool call 共享物理 item、tool call/result 分别计数、零 Item Turn、live/history mismatch 显式诊断，以及 `e2e-basic-chat-tool-loop` 的精确时长/Item/展开 DOM
- [x] 6.8 修复最终 checkpoint reasoning carrier 重影：按最终 `content_part_refs.id` 与既有 canonical reasoning `metadata.block_id`/provider item identity 对齐，同一逻辑 Item 只投影一次；相同正文但持久 identity 不同仍分别保留。验收覆盖真实开发历史 artifact、确定性 catalog 回归、完整 history 组合和正式 `e2e-basic-chat-tool-loop`
- [x] 6.9 收敛历史详情与实时流的请求和内存边界：history page 为每个 detail 返回同 revision 权威 summary，Web 不再为历史 Turn 请求 `message.v1` snapshot；前后端限制终态 stream、乱序事件、block/tool 正文、跨会话 timeline 和详情正文驻留，淘汰时只恢复后端 summary。验收覆盖后端重启/缓存/大正文、Web LRU/详情重载、协议生成与 build、正式 `e2e-basic-chat-tool-loop` 和当前 8011 开发链路

6.x 的验收证明 LangChain、native Provider 和 history 从同一 selection 消费上述 ToolSetRef manifest，且 replay 先做 manifest/source 校验再做 `plan_hash` 比较；不能把独立的 logical tool contract hash、当前 tool registry 或空 tools 当作 ToolSetRef manifest 的替代。

## 7. Legacy migration、分阶段启用与验证

- [x] 7.1 实现仅供一次性 `legacy_import_v1_to_v2` migration/import 命令调用的 v1 reader：以 `message_sequence` 仅作可审计 user-root window 边界，不能依赖非 user 记录的物理邻接；再按既有 `turn_id`、每窗口恰一个 user message、缺失/重复/冲突 ID 规则生成 `legacy-turn:<turn_id>` 或 `legacy-missing-turn:<legacy_message_hash>` candidate，并对 `legacy_turn_group_ambiguous`、`legacy_multiple_user_messages`、`legacy_orphan`、`legacy_unsupported_role`、`legacy_identity_conflict` 明确拒绝或标记。assistant/tool/function 只能成为 Turn member；system/developer 逐行保留为 `legacy_request_context`，不进 Turn；带可信 internal/checkpoint metadata 的 `system_reminder` 才映射为 `turn_id=NULL`、`turn_scope=pending_next_turn` 的 Turn 外 runtime_notice；unknown/其它 role、无法解释的 role/payload 或不可信 system_reminder 必须整体标记 `legacy_unsupported_role`、保留原行到 migration report/quarantine 后拒绝，不能按物理邻接改派。user message 映射为 root candidate、AIMessage 映射为 assistant_output/content parts、tool identity 保留、未知来源标记为 legacy/unknown_source，并定义基于 source coordinate+payload hash 的 `legacy_seed_hash`、synthetic accepted_ingress/acceptance idempotency/initial execution identity，不把展示临时 identity 当真实 acceptance；该 reader 不得被正常 history/provider/checkpoint/runtime 或 context compiler 导入
- [x] 7.2 使用已冻结的 v1/v2 manifest、envelope 和 dispatch 字段实现一次性 `legacy_import_v1_to_v2` migration report 与 staging abort/rollback audit；跨 session/full_rollout_copy 的 target 固定 dispatch 为 v2，source v1 坐标只进 legacy mapping，rollback 不得重新启用 v1 runtime/history/provider/checkpoint 读取，也不得改变 v2-only dispatch 与禁止混读合同
- [x] 7.3 实现临时 v2 artifact migration、JSONL/SQLite/view/Turn/assembly 校验和原子安装，保留 v1 原件并在不可逆字段上标记 loss/partial/raw extension；普通 `full_rollout_copy` 遇到 v1 source 必须返回 `v1_migration_required`，由显式一次性 `legacy_import_v1_to_v2` 先完成 migration，再按 v2 full-copy 创建 target-local identity/offset；只有明确 legacy final marker 才生成 final_item_id，否则 unknown/空 final
- [x] 7.4 验证 manifest/envelope mismatch、未知版本、结构损坏、一次性 migration 失败、重启恢复、source overlay base/delta 恢复和 v1 原始 artifact 保留/rollback audit 均显式报错或可审计；不得把 v1 runtime fallback 当作回滚路径
- [x] 7.5 完成 item storage、projection、history、branch/recovery、provider/tool loop、legacy migration 的分层测试与 OpenSpec verify。完成判定拆为互不替代的门槛：7.5-A item/commit/recovery；7.5-B ContextRef/ToolSetRef/selection/overlay/detail 与三类 projection；7.5-C Turn/fork/replay/rewind/compaction；7.5-D 一次性 v1 migration/import、format dispatch、staging rollback audit；7.5-E provider/tool-loop 与 OpenSpec verify；7.5-F v2-only import/runtime 审计、删除旧聚合职责和临时 import shim、确认无 dual writer/dual projector/双 schema/双事实源，并完成大文件职责拆分。7.5-F 还必须证明 `RolloutStorage` 只剩薄 facade，storage 不 import `langchain_core` 或 `app.services.mapping.itemized`，mapping 无 I/O；`plans.py`、`assembly/store.py`、`checkpoint/saver.py`、`checkpoint/owner.py` 和 `storage/service.py` 的职责已按 design 拆到单一 owner。任何新增或重构源码文件超过 800 行、混合两个以上领域职责，或目标目录直接源码文件超过 20 个，必须在 7.5-F 前完成拆分，或提供包含文件清单、职责/owner 映射、import graph、行数统计和复核结论的架构审查证据；仅 helper、局部静态检查、长期 compatibility shim 或局部 E2E 不得通过该门槛。每个门槛都必须记录实际命令、退出码和覆盖范围；核心 E2E 或 strict validate 不能替代缺失门槛，删除旧运行代码是 change 完成必要条件，不得留作后续讨论

## 8. SessionThread、GraphBinding 与持久化复用（本轮新增，未实施）

本节是Section 1–7中所有session-only owner、唯一键、路径和引用描述的规范替代。旧勾选项仅保留当时实现证据；凡缺少`thread_id`或仍把Session当rollout/ContextStore owner的旧合同，都必须由8.x迁移后重新验收，不得据旧勾选状态视为完成。

- [ ] 8.1 定义Workspace `navigation/session-catalog.sqlite`为Session/Folder node父链、显示名、状态、不可变UTC创建日/受检`sessions/YYYY/MM/DD/{session_id}` locator和唯一main pointer的唯一权威；folder无物理目录，session.json无可变父节点/标题，Session本地session-control持唯一匹配main row、thread catalog、collaboration ledger与fence/lease。统一两级resolver、NavigationTopologyGate→SessionLifecycleGate→至多一个SQLite写事务的准入锁序、UUIDv4-profile Session/Thread validator与完整path预算；main仍`threads/{main_thread_id}`，非main仍`threads/YYYY/MM/DD/{thread_id}`。逻辑导航移动不搬磁盘、不改fork/delegation lineage/Session kind或已封存context；执行中Session可移动。Gateway只消费受控catalog export，不读Workspace DB/locator；旧JSON index与物理父子目录仅作显式migration输入。
  - 8.1-A 在同一workspace `session-catalog.sqlite`中create-or-get SessionCreationRecord、冻结UTC日期/parent generation/ID/locator/preimage并预留唯一ID/路径；`.boxteam/sessions/.staging/<operation_id>`完整写入Session+main+fence/ContextStore并durably flush后rename至`YYYY/MM/DD/{session_id}`，重取topology exclusive后同一SQLite事务CAS父节点仍active且generation未漂移并一次发布node/locator/main pointer及record terminal。copy target保留prepublished CopyAttachmentSettlementRecord；覆盖同key/冲突、父移动/删除、staging/rename/SQLite commit/response崩溃、定点orphan恢复、缺main/双main/fence损坏、并发catalog更新与Gateway零业务补写。
  - 8.1-B 实现同库NavigationSubtreeDeleteRecord：Session删除含逻辑后代、Folder recursive删除从SQLite递归CTE冻结精确node/Session集合、locator和revision，topology exclusive下逐Session短时预检retention，再同一SQLite事务CAS全树active→deleting并保存batch manifest；commit关闭新准入与逻辑可见性。随后逐Session关闭local fence、等待guard/旧lease/通信/附件收敛、定点隔离日期目录并持久进度，全部完成后一次提交整树不可复用tombstone；崩溃保持全树deleting且从record定点继续。覆盖单/批删除、catalog已deleting但部分fence active、并发move/create/pin、隔离中断、GC、response重试、无关/目标revision漂移、双端晚到callback及绝不部分可见；不持锁等待worker或网络，不打开独立delete journal/旧JSON index。 对含大量后代的递归删除记录topology锁等待/预检耗时，验证默认不因规模阈值禁用核心删除且绝不拆成部分deleting提交。
  - 8.1-C 冻结SessionOperationLease/SessionLifecycleFence schema、holder generation/fencing token、operation identity/preimage、nonterminal索引和崩溃恢复；旧lease跨库提交后才terminal，不以墙钟过期。增加Workspace NavigationTopologyGate跨进程shared/exclusive锁，所有新Session准入先shared topology再Session exclusive gate，导航move/create/delete取topology exclusive；cold read在topology shared下取得fresh locator和Session shared guard后释放topology，guard持续到handle关闭。锁文件不unlink/recreate，验证多进程读写/崩溃释放、无反向锁序、无双Session gate/双DB写事务；catalog deleting后即便local fence active也不得新建lease。
  - 8.1-D 将pinned ForkRetentionClaim预检放在整树catalog deleting提交前且与claim准入竞争topology gate：claim先行返回具体blocker且全树active，batch先行则claim零副作用失败；覆盖active/preparing/released claim、不同子Session中的blocker、删除/claim双顺序及commit前后崩溃。不保留“独立journal提交后、本地fence前补pin”的旧窗口。
  - 8.1-E 实现SQLite-only Folder/Session导航节点的create/rename/move/assign/empty-delete/recursive-delete、parent索引、breadcrumb/search/cursor revision与受控Gateway catalog export；拒绝自环/祖先环、缺失/跨workspace/deleting parent、同名兄弟冲突及target revision漂移。删除旧Folder manifest、物理children树、`parent_session_id` manifest回写和移动时强制descendant Job idle逻辑；活跃Session移动不得影响thread路径、运行中handle、已seal assembly、fork/delegation lineage或Session kind。同步修订`AGENTS.md`中物理子树/JSON权威的旧指令，不能让文档与新存储合同并存。
  - 8.1-F 为workspace导航SQLite启用foreign key、唯一约束、schema version与启动integrity/locator/date/operation-record校验；迁移切换前保留旧索引/目录/SQLite可校验备份，后续用SQLite online backup/等价一致性快照记录generation/checksum。测试活动WAL备份、catalog损坏、备份落后于已提交操作与未登记日期目录：只能进入维护模式核对并保留原数据，不能扫盘补active node、旧JSON fallback或GC未知目录。
  - 8.1-G 定义并实现workspace业务typed`/api/v1/session-catalog/operations:enqueue`批量协议、同库`NavigationMutationRecord`/queue_seq/dependency/Folder ID reservation/terminal idempotency tombstone及202 durable receipt；gateway_id/actor只能来自认证路由，入队短事务不持topology gate做树预检。跨进程fenced worker按workspace FIFO执行，普通命令在topology exclusive+单一catalog事务原子提交node/revision/terminal/event outbox，递归删除复用NavigationSubtreeDeleteRecord并把logical committed与physical settlement分开；原同步目录mutation API/写路径不能继续作为第二writer。覆盖相同ID同/异preimage、批量原子入队、跨批依赖和client_ref映射、同node序列化与无关node并行意图、另一个客户端修改/删除冲突、worker crash/接管、背压不丢命令、乱序/重复调用及无双gate/双写事务。
  - 8.1-H 定义revision-pinned目录snapshot/分页、按ID持久operation状态查询与独立navigation事件channel/cursor，事件outbox和目录状态同SQLite事务；Gateway只代理已提交状态/事件，旧response或export不得覆盖新revision。Web实现按gateway/workspace/principal分区的IndexedDB outbox和“confirmed后端镜像+纯函数pending命令重放”的会话目录投影，操作同步立即更新内存pending UI，IndexedDB按序持久化成功后后台才有序批量入队，持久化失败撤销且报错；Folder client_ref/created_by_operation依赖、branch分页/has_children、breadcrumb/search及跨tab可见性不得制造重复node或污染后端cursor/total。明确拒绝时重读权威快照、移除失败及传递依赖后重放独立命令，保留可用焦点/展开并提示错误；超时/断线保留unknown并原ID查询/重试，刷新/重开恢复outbox，对账terminal及catalog revision后才清理。测试端到端连续操作的可交互性、失败回退而不覆盖其它客户端成功、cursor gap/旧响应、删除pending/committed不同状态，并遵守Web build/静态检查要求。
- [ ] 8.2 显式一次性迁移旧`session-catalog-index.json`及嵌套Session/Folder/children物理树：在workspace maintenance gate下quiesce execution、communication、attachment等mutation，先做可校验旧树/SQLite一致性备份，再冻结旧node ID/父链/locator/created_at映射与完整manifest，将Folder变成SQLite-only node、Session逐个staging到日期桶并保持canonical JSONL/item/Turn/assembly原bytes与ID；同时完成thread catalog/main/fence初始化、旧附件迁移与digest/owner ref校验。以带durable迁移journal/目录manifest的单一可恢复切换点启用新SQLite authority；失败保留旧树审计，不在正常v2 runtime双读旧JSON/旧路径alias或扫盘补node。非法旧ID/date、缺失parent、循环、locator冲突、正在删除/未登记lease一律quarantine/fail closed，不默认active。
  - 8.2-A 在相同maintenance gate和迁移journal中静态接线`add-agent-debug-tool-group`的debug-domain迁移步骤，不建立plugin registry：仅按冻结旧Session→main thread映射登记旧`debug/node/`的manifest/方案文件、bytes/hash、ID/revision/lineage，并在main thread目标节点staging与原子切换时校验；含调试文件但步骤失败不得声称完整迁移，原件保留供重试，不新增runtime旧路径alias或扫盘补缺。调试domain拥有方案语义/进程收敛，itemized拥有共享gate/journal/发布门槛。
- [ ] 8.3 让 thread catalog/storage resolver、ContextStore、CSM、ToolSet applied state、turn/execution/model-call、Job active slot/FIFO/interrupt、history/rewind/compaction 与 checkpoint config 全部以 `(session_id, thread_id)` 定位；实现workspace attachment content-addressed blob catalog，固定68-byte ASCII `blb_[0-9a-f]{64}`身份并直接使用`attachments/YYYY/MM/DD/{blob-id}`日期分桶；Session/thread上传在按session ID互斥的workspace `SessionLifecycleGate`内确认active并登记durable `AttachmentOperationPin`，再登记引用该pin的workspace `AttachmentIngestRecord`，durable hash后竞争唯一`BlobCommitClaim`，rename后再次持有同一gate复核Session active/pin并原子发布availability/attachment/owner ref，最后才释放pin。删除进入`deleting`也竞争同一gate，先删则拒绝未提交reference，先发布则删除按pin释放reference后再隔离。覆盖跨库saga各崩溃点、上传/Session删除双顺序竞态、跨日期同digest并发、定点恢复、去重、identity conflict、capability/view ownership和引用感知tombstone/GC。验证同一thread串行、sibling thread可并行，以及事务、offset、active view、source revision、队列和附件权限隔离，禁止扫盘和调用方拼接路径。
  - 8.3-A 实现持久thread-local ThreadExecutionQueue、不可重排admission ordinal、幂等entry和单active slot；每次用户/internal execution准入先在Session gate内建立捕获active lifecycle generation的queue/admission lease，为每个main/child active execution冻结ExecutionContextFence。busy时后续acceptance虽立即提交queued Turn/root，但旧execution的所有model-call plan按causal admission排除未来entry的root/seed/notice，旧execution在未来root后物理append的tool/final item仍按Turn/plan因果排序。独立ambient文件/Skill/team revision由ResourceRegistry持续发布而不丢失；默认`turn`边界在execution取得active slot时冻结ResourceActivationSnapshot并让整个tool-loop复用，只有显式`model_call`配置才在后续safe preparation激活新revision，均禁止请求路径读源。所有callback验证execution lease/generation，删除fence关闭后仅允许终结control outcome而不得追加业务item。前序终态后同事务推进下一entry visibility与新fence，再运行CSM/ToolSet reconciliation。覆盖entry/claim/start三个崩溃点、exactly-once启动、无越序、future entry不泄露、两种activation boundary、删除竞态/late callback、未admit取消不调用Provider、同thread串行与sibling并行。
  - 8.3-B 统一source overlay与stable-prefix合同：同prefix epoch的replay/restore/source edit只可精确复用旧wire bytes并尾部追加；只有首次组装、实际compaction、rewind重建和ToolSet hard rebase允许登记pending transition并在首个assembly成功seal时应用新epoch或物化。rewind/compaction可先提交view/transition；下一次model-call preparation把tracked当前完整user-role revision、首个新epochassembly与transition消费原子提交，失败保留view/transition但不产生半item/epoch/dispatch；snapshot/untracked不恢复。required detail/source/profile不兼容fail-closed，禁止用当前文件修复旧sealed request或跨cutoff复活旧delta。已有assembly后desired Provider/model profile变化在没有上述合法重建边界时返回`provider-profile-change-requires-rebuild`，不偷建第五种epoch、静默使用旧模型或伪造no-op ToolSet rebase。覆盖四种epoch reason、只rewind不继续、读取/seal失败重试、profile切换、旧assembly exact replay、post-user item不合并及Anthropic mid-system TODO未启用。
  - 8.3-C 在plan/seal前强制tool协议因果：assistant group的全部outstanding call先取得同一execution内匹配terminal result，期间由`skill_load`、team或runtime producer提交的source只保持pending；收敛后selection固定为assistant calls→全部paired results→source，禁止按物理item sequence、JSONL offset或提交时间把user-role source插入call/result之间。compaction、rewind、replay和fork anchor切入call group/result之间时返回`tool-protocol-boundary-conflict`，保持旧view且不创建目标或pending epoch，只给出无正文安全anchor；覆盖并行调用、部分result、失败result、重启恢复、物理append交错与只读partial history不得dispatch。
  - 8.3-D 实现`turn_item_projection(turn_id, logical_item_ordinal)`和不可变view logical membership segment/reference；queued Turn root与旧execution后续item物理交错时，history/context仍按显式membership与causal ordinal读取。冻结`expandable_activity`分类：可见reasoning/summary part、tool_call参数、tool_result和显式Turn-member compaction summary各按logical identity计1；聚合tool row、user/final、transport chunk、encrypted-only、request-only/CSM/隐藏runtime项及重复carrier不计。首项elapsed相对acceptance/root、后续相对前一计数成员，duration到terminal；时间异常显式失败。仅对证明无排除项的连续segment允许带count/endpoint/membership hash的range压缩；`first/last_item_sequence`只作诊断，不得推导Turn成员、item_count、elapsed或view。覆盖跨Turn物理交错、range hash冲突、rewind/fork切片及刷新DOM不重排。
- [ ] 8.4 重构 AgentFactory、工具和 middleware 的 session 捕获闭包为 invocation `ThreadRuntimeBinding`；持久化并校验 `GraphBinding`，只复用不捕获 thread 的 graph blueprint/topology，缺少精确 revision 必须 `graph_binding_unavailable`。
- [ ] 8.5 将 durable delegated subagent 创建/恢复/汇报改为同一 Session 的 child thread，保留短生命周期 per-invocation subgraph；child只接收显式task seed/reference/capability profile，默认不启用Goal，为同一persistent child建立持久FIFO和ExecutionContextFence：busy时新用户acceptance可立即持久化queued Turn/root，但旧execution所有后续tool-loop plan排除更晚admission，旧execution在queued root后物理append的item仍按Turn/plan因果排序，前序终态后才原子推进下一entry visibility。跨thread汇报保留parent/child/delegation/source provenance且不创建真实user Turn或复制全部child history。
  - 8.5-A 普通单child creation以`thread_creation_idempotency_key`去重，delegated child将`delegation_id`纳入preimage；在Session gate内、建staging前于`session-control.sqlite` create-or-get不进入catalog且承担operation lease的`ThreadCreationRecord(state=preparing)`，冻结key/preimage、child ID、最终/内部staging locator、GraphBinding/capability/seed/admission identity、artifact manifest/hash及Session lifecycle generation/catalog/collaboration precondition revision。按record完整staging并通过durability barrier后，原子rename到冻结且尚未引用的最终locator，再重取topology shared/Session gate，由thread catalog、需要时的collaboration ledger与该record在单一SQLite事务CAS验证catalog active和fence仍为捕获的active generation及delegation/parent/member状态后发布并标记`published`，最后由重新通过生命周期准入的幂等worker create-or-get初始execution。CAS失败不发布、不重基，只在定点清理record列出的目录后标记`aborted`。覆盖record提交/staging/fsync/rename/发布/terminal response/Job binding各崩溃点、创建/发布/Job准入与删除双顺序、取消delegation、revision漂移、orphan按预存record定点校验恢复且不扫盘吸收、无record目录不吸收、同key冲突、空child和重复Job；手动无seed child必须显式`initial_state=idle`。board migration batch明确不走该worker，而由8.11在gate内建立、捕获generation的record/entry整批发布。
- [ ] 8.6 调整 Session API/proto/Gateway/Web：普通 Session 聊天/历史/Goal显式解析main thread，thread级trace/history/message/operation使用精确ID；不合并child history，所有响应、SSE/cursor/cache key暴露并校验实际thread identity。
- [ ] 8.7 制定历史 delegated Session 的显式迁移/保留策略与跨 session fork 的 target-main-thread mapping；旧 delegated Session不得静默并入parent或继续作为team member，fork source/target引用必须升级为thread-qualified `GlobalEntityRef`并保持target-local identity。同Workspace方案矩阵固定为context fork仅当前活动方案、history-prefix不复制方案、full copy复制全部当前保存方案，migration-only child copy不自动复制；source read guard内将受检方案manifest/revision/bytes/hash列入同一SourceCopySnapshot，target staging中由debug owner冻结目标有效debug配置revision/hash并完整校验入口/工作目录/断点路径/profile/adapter/runtime，发布前复核配置revision且在fork journal写入target-local ID/lineage，坏方案或配置漂移使整个fork不发布。测试source并发方案更新、目标profile缺失/语义不兼容及配置漂移、断点路径失效、crash恢复、active指针/进程不复制及跨Workspace公开copy拒绝。
- [ ] 8.8 在目标`app/services/orchestration/thread_residency.py`实现统一ThreadRuntime admission、lease/generation fence、idle tracker、unload和lazy rehydrate；child默认连续30分钟无活动且无active/runnable/pending execution、未收敛model/tool/mutation或lease后由residency owner关闭该generation的进程内`LifetimeScope`，复用`add-context-injection-lifecycle`的唯一释放合同而不建立第二dispose manager。仅在scope自有task/句柄已排空后标记cold；失败显式报告、旧generation callback不得继续提交；进程级共享watch、跨Turn terminal/browser/MCP/Node调试和durable thread/ContextStore/CSM/GraphBinding/历史不随卸载删除或停止。cold list/history/detail不materialize runtime，新消息/内部wakeup按原GraphBinding恢复唯一owner，idle unload不新增item、epoch或上下文到期状态。E2E用trace同时验证scope drain、其它consumer的watch仍活、外部资源不受影响以及刷新前后stable-prefix bytes相等。
  - 8.8-A 将debug owner已核实的`launch_pending|starting|running|paused|stopping`进程claim及`reconcile_required`映射为精确thread的idle blocker：跨Turn process-instance claim独立于短期operation lease，活跃期间不累计闲置，进程核实终态/lease结清后重新起算30分钟；residency owner不据scope close、PID/端口重用或内存缺项推断进程停止。fake clock覆盖claim提交后spawn前、spawn后进程登记前崩溃、活动进程跨阈值仍resident、失败停止/重启仍阻断、终态后29:59/30:00转cold和不改变上下文字节。
- [ ] 8.9 定义不含credential/物理locator的Session目标引用，并建立中心Gateway hub通过现有SSH `-L`到各spoke、在隧道内连接`ws://.../api/gateway/federation/channel`的长期全双工WebSocket对等RPC channel；A主动连接B/C后，B仍能反向发起`B → A → C`请求并接收原路响应，不需要反向SSH或B/C直连，且只允许一个transit。以稳定`gateway_id`路由，现有持久`connection_id`仅作本地配置身份，瞬时`channel_instance_id`/epoch/seq/ack/route lease均不得进入link、`GlobalThreadAddress`或幂等key。hub从channel绑定真实origin并签发绑定origin/transit path/audience/operation/target/principal/request/nonce的discovery/operation grant，target以受信hub身份和最新local policy重新授权并解析main thread。实现`permissions.federation`默认允许全部核心discovery/send/read/wait/reply/transit、规则为空、hardening关闭的原子热发布snapshot；权限不改ToolSet/context/channel，在每个实际操作与披露点检查最新revision。身份、完整性、audience/path、防重放、target解析和幂等不可关闭。hub只保存peer/policy/replay registry/audit控制面状态，不保存业务消息；所有错误显式失败且零workspace副作用。
  - 8.9-A 裸ID只走source local workspace与唯一hub的有界fan-out，携带visited set、`max_transit_gateways=1`、`max_gateway_hops=2`和总deadline；hub只向active spoke转发一次，spoke仅查本地cold catalog且不得递归。channel-bound origin envelope与hub transit discovery grant不得执行send/read/wait或读取history，只统计最新policy允许的候选；未授权存在与不存在不可区分，多命中只返回count。route hint短TTL且由target重验Session/main pointer；hub/target first-use registry跨重启防replay且不可用fail closed。稳定ResolvedSessionMainTarget/GlobalThreadAddress与单attempt channel/route lease分离；outbox提交后WebSocket/SSH重连只刷新原target route，不重新裸ID改投。
  - 8.9-B wait grant剩余寿命覆盖effective timeout与时钟偏差；discovery、hub transit、target admission、read每页、wait状态/terminal、send重试和hub relay response返回均读取最新policy revision，cursor/selector不是capability。测试默认无规则时所有核心能力直接可用；连接中撤权立即阻止未accept send或停止披露，恢复后下一实际调用立即允许，ToolSet/stable prefix/channel不变；覆盖`grant_lifetime_insufficient`、`authorization_revoked`和已durable acceptance不因撤权或旧grant到期回滚。
- [ ] 8.10 将`send_message_to_session`、`read_context`和等待能力统一接入resolved target-main地址：模型侧send删除`simulate_user`，固定非空content、`kind=question|reply|progress|result`默认result及`delivery_policy=after_turn|after_tool_result|after_interrupt`默认after_turn；在source thread node保存outbox、target main-thread node保存inbox，以受认证receipt、本地事务和确定性dedupe持久化`GlobalThreadAddress`、immutable target、payload hash、acceptance、communication及job/turn binding；source outbox建立和target inbox acceptance分别在各自Session gate内建立communication-side lease，两个gate不同时持有，target acceptance、唯一ambient item和wakeup幂等键由该lease覆盖的owner事务提交，execution binding验证同一lease/generation，禁止分布式事务和Gateway业务owner。实现target workspace单一`InboxAdmissionWorker`，由acceptance事件和backend startup按状态索引恢复未绑定inbox，以claim/lease和原wakeup key幂等admit，不扫目录、不依赖wait/read或内存future、不建立第二writer；仅在target durable acceptance后返回成功，并校验question/reply direction。read走cold有界projection并只在调用方形成带source refs的canonical tool-result；以接受`communication_id|job_id|turn_id`、`until`及默认60秒/最大300秒timeout的有界`wait_for_session`替换`monitor_session_agent_end`，communication未绑定execution时先等binding，timeout返回可复用selector，无selector只观察准入时已有active/runnable/pending工作。wait使用可注入单调Clock/DeadlineTimer和目标状态订阅，持久DurableDeadline的UTC/monotonic origin，进程重启延续原预算、clock origin变化不延长；测试推进虚拟60/300秒而不缩短合同，deadline竞态按target已提交revision裁决。不得轮询数据库、假idle、订阅未来任意Job、materialize目标runtime或创建共享task board；删除先关闭任一端fence时该端零副作用，acceptance先行时删除必须收敛其lease；target重启和原ID重试不得重复注入，同ID不同payload/target必须冲突。
  - 8.10-A 首次route前由软件生成稳定且模型不可见的`send_operation_id`（模型工具绑定execution/tool invocation，UI/API绑定持久idempotency key），先以该identity在source事务中create-or-get outbox/communication并冻结request preimage；验证target acceptance后source丢失receipt再恢复时仍使用原communication，且同operation不同preimage冲突。
  - 8.10-B target inbox持久化稳定`admission_id`，JobService按`admission_id + preimage hash`create-or-get；验证Job已创建但binding未提交时重启只补写原binding。terminal正文回收后，在Session删除前保留最小dedupe/correlation/receipt tombstone；Session删除先收敛未终态通信并提交不可复用identity的deletion tombstone。
  - 8.10-C read首请求通过`(source GlobalThreadAddress, first_source_call_id)`唯一create-or-get并分配`observation_id/read_series_id`，签发版本化AEAD保护、opaque、可自验证且不写ContextStore的ReadContextSnapshot/cursor envelope，冻结该identity、resolved thread、view revision、item/Turn上界、projection/policy hash、总预算/offset/expiry；同call丢response重试必须找回同一snapshot。每页重新授权，解密/认证失败统一为`read-snapshot-invalid`且不泄露字段。为每次read/wait source tool/API调用增加软件生成且模型不可见的`operation_invocation_id/source_call_id`和source FederatedCallRecord；后续页用新的source call但由cursor恢复同一observation并建立按`(observation_id,page_ordinal)`去重且反向唯一绑定source call的RemoteObservationPageRecord，wait observation与单次source call一一对应并冻结selector/baseline/deadline。grant/response绑定source call和observation/page；同一次调用的网络retry换grant不换identity，同preimage create-or-get，source在response/tool-result前崩溃后恢复同一结果且每个调用最多一个tool result，过期显式失败。覆盖同ordinal不同preimage/同call不同page冲突、目标并发新增/rewind/view切换不混页、参数冲突、权限撤销、retention/key rotation过期、明确loss、篡改token、旧cursor只重读固定范围及分页不能绕过总预算。
- [ ] 8.11 将Goal固定为main-thread capability，并以显式migration operation整块处理旧跨Sessionteam/member/task/coordinator状态：materialize路径只从已终态/显式quiesce的legacy main checkpoint/view调用migration-only `materialize_thread_copy`，复用target-local mapping/校验但只在`BoardMigrationRecord`冻结的coordinator Session staging中生成goal-disabled child，不创建新Session/main、不写catalog/ledger、不自行发布或暴露owner；公开`full_rollout_copy`继续只创建独立target Session/main。source Session/历史保持不变；其它情况freeze/detach。创建任何staging前先在coordinator lifecycle gate内于`session-control.sqlite` create-or-get不改变catalog/ledger且承担operation lease的`BoardMigrationRecord(state=preparing)`，冻结operation/preimage、coordinator lifecycle generation、旧board/catalog revision，并为每个target内嵌包含ID/locator/GraphBinding/capability/source view/lineage/artifact hash的唯一`MigrationChildCreationEntry`；不得另建可由普通worker独立推进的`ThreadCreationRecord`。随后准备并durably flush，先把全部child原子rename到record冻结且尚未引用的最终locator，再由thread catalog、collaboration ledger与该record单一SQLite事务CAS验证fence仍为捕获的active generation及board/catalog/member/task precondition后发布全部locator、mapping并标记`published`。CAS失败不覆盖并发变更、不重基或部分发布，只在定点清理后标记`aborted`。覆盖两类copy不共享creation key/发布语义、普通worker无法认领batch child、record提交、逐child rename及发布/terminal response前后崩溃、迁移每阶段与Session删除双顺序、board mutation、orphan只枚举预存record定点校验恢复/清理、无record目录不吸收且正常reader分别只见完整旧/新board，不假设跨thread/workspace分布式事务。禁止复制active runtime、创建空child、部分发布及外部Session/本地child混合member。正常状态使用稳定`member_id → child_thread_id`，删除/拒绝跨Sessionteam attach与state同步，相关delta只进入需要感知它的精确thread。
  - 8.11-A 每次Session collaboration mutation先通过lifecycle gate，在同一ledger事务创建承担operation lease且冻结fence generation/revision/event hash/recipient集合的fanout record；worker按revision+recipient幂等提交pending resource observation，每次提交校验lease/generation且不唤醒cold runtime。fence关闭后未提交recipient只写terminal outcome，不得晚到append。Registry异步reconcile team snapshot；默认`turn`在recipient取得active slot时冻结不早于该时刻ledger revision的activation snapshot，整个tool-loop不重新捕获，只有显式`team_state=model_call`才在后续call激活最新已提交revision。测试mutation/fanout提交与删除双顺序、排队/active期间变化、两种activation boundary、请求路径零ledger I/O、部分投递崩溃、重启补齐、已完成不重复、recipient删除terminal outcome和required snapshot未就绪禁止dispatch。
  - 8.11-B 为公开`context_fork|history_prefix_fork|full_rollout_copy`和migration-only `materialize_thread_copy`实现source snapshot capture：对应journal/entry先预分配`source_snapshot_id`与内部locator；从source catalog解析到node/SQLite handle关闭并durably冻结期间持shared `SessionReadGuard`，在固定SQLite read snapshot/online backup中取得同一revision的view/checkpoint/control、`storage_commits`、各JSONL committed end offset，只复制offset内JSONL及通过hash/length/capability校验的thread-local detail，并原子发布带operation/preimage、lifecycle generation、database/逐文件/detail/attachment-claim manifest hash的`SourceCopySnapshot(captured)`。释放后禁止回读source，target/coordinator准入与publication必须分阶段另行取得gate。board先短持coordinator gate建立record/lease并释放，再按record顺序一次只capture一个source，最终另行CAS发布。覆盖source并发append/view变化、partial与marker-to-CAS崩溃、删除/capture双顺序、多个source、target/coordinator publication和锁序fail-closed；partial定点abort，完整snapshot可不依赖source恢复，不扫盘/补齐/换revision，任意时刻不同时持有两个Session gate/guard或两个数据库写事务。
  - 8.11-C 将fork/copy限定为同一workspace。为`pinned`模式在capture前先由target creation/fork journal预分配operation/preimage，再单独取得workspace topology shared/source gate创建绑定fork、两侧GlobalThreadAddress、target operation/preimage、source generation及view/detail范围的`ForkRetentionClaim(preparing)`/retention占位；删除先行则fork失败且零可见target，claim先行则删除在整树catalog deleting前返回明确retention blocker。target提交后只激活同一claim，不能首次补写；abort/target删除通过target durable `ForkRetentionReleaseRecord`、释放target gate、单独取得source gate释放claim、再确认target terminal的saga完成，preparing claim无墙钟过期且缺失/冲突fail closed。覆盖两端崩溃点、重复恢复、同时删除、无双gate/双事务，并拒绝federated link/grant执行fork或远端retention mutation。
  - 8.11-D fork/board source snapshot只冻结logical attachment/variant、digest/length/availability、source owner/item ref与attachment catalog revision，不复制workspace blob；同一capture用journal绑定的IdentifierFactory预分配target-local item/attachment identity和mapping。关闭source SQLite read事务后且释放guard前，在单个attachment catalog事务按copy operation/source ref/target owner ref建立`ForkAttachmentClaim(preparing)`并验证source owner/blob、阻止GC；全部成功才把有序claim清单/hash写入captured marker，中途失败按精确operation ID释放。target/child发布前全部required claim转`owner_reserved`并建立target owner ref，resolver仍要求target catalogs active；有required claim的公开copy target预置绑定operation/preimage/generation/publication preimage/claim hash的`CopyAttachmentSettlementRecord`，board record保存等价字段，publication后保持`published_pending_attachment_commit`非终态；无required claim的公开copy不建settlement record。finalizer与target删除竞争同一gate并以不重叠事务提交claim/record；删除先行时释放reserved claim/ref，或验证committed claim归属后持久释放其target owner ref，再进入`target_deleted|coordinator_deleted`而非成功终态，禁止恢复重建/保留owner或非终态record下隔离节点。abort/未发布按journal claim ID释放并进入`aborted`；source unavailable非required历史ref显式映射unavailable，required失败阻断整次copy。覆盖source/target删除、claim/owner-reserved/publication/finalization各崩溃点、target/board abort、同digest多ref、多copy隔离和无source identity复用/扫盘/重复blob/GC空窗/提前授权。
  - 8.11-E 冻结附件结算状态闭集：`CopyAttachmentSettlementRecord`只能为非终态`preparing|published_pending_attachment_commit`或终态`committed|aborted|target_deleted`；`BoardMigrationRecord`只能为非终态`preparing|published_pending_attachment_commit`或终态`published|aborted|coordinator_deleted`。无required claim时公开copy不建settlement record，board可见性事务直接写`published`；有required claim时publication只能写pending，全部claim/settlement完成后才写成功终态。已发布后删除抢先必须写对应删除终态，且结算不得改变已发布board成员集合。替代8.11主项中未区分attachment的`published`简写。
- [ ] 8.12 在主窗口右侧侧边栏实现当前Session的child-thread列表、`ThreadResidencySnapshot`状态/idle deadline/脱敏blocker、未读、cold history与直接对话入口；主会话区继续展示main thread，child不进入左侧Session导航，直接发送携带精确thread并只更新对应stream/history，汇报item可跳转到child原始历史。
- [ ] 8.13 在`tests/e2e/clients/web/test_basic_chat_tool_loop.py`这一唯一Web E2E owner中保留基础两轮精确时长/item数量/reasoning→tool→reasoning展开/刷新无重复断言，并增加Git/分析/实施/汇总Session、用户把本地/远端Session ID或link发送给汇总Session后生成周报、同Session child右侧直接对话和同child FIFO、Session内team、fake-clock 29:59/30:00及lease blocker/cold read/lazy rehydrate、late callback fencing、Gateway-local/federated Session-link send→wait/read/report、错误origin/audience/path、过期/replay、credential失效/不可达的零目标副作用、wait timeout恢复，以及target-accepted但未execution-bound时backend restart后按同一communication恢复且只注入一次和stable-prefix bytes不变验收。cold/restart前后逐字段核对持久GraphBinding与factory revision。pytest拥有外置`E2ETestControlHarness`，生产不注册test入口，harness不写业务状态。每个新增Turn从权威history取得item identity/order/count/elapsed并与live/refresh DOM对账。多workspace fixture放同名`workspace/`子目录，产物放`artifacts/`；A/B/C使用各自隔离BOXTEAM_HOME、peer credential和policy snapshot组成三个真实进程，A通过loopback SSH转发等价fixture主动建立到B/C的全双工channel，B反向发起`B → A → C`且响应原路返回。覆盖默认无规则可用所有核心能力、连接中撤权/恢复按下一实际调用生效且ToolSet/stable prefix不变；禁止反向SSH、B/C直连、resolver stub、remote backend直连或用户BOXTEAM_HOME。模型按SessionThread/model-call identity匹配，teardown关闭全部owner进程/端口。不得真实等待产品阈值、注册项目根、扫盘猜owner或另建重复E2E。
  - 8.13-A 在同一E2E模块增加target已accept但source尚未持久化receipt时重启source的故障点；恢复原tool/API invocation后断言`send_operation_id → communication_id`不变、target inbox/ambient item/Job/Turn均没有第二份，并覆盖Job已创建但inbox binding尚未提交的target重启。
  - 8.13-B 用户消息只提供远端裸Session ID时先以channel-bound origin envelope经唯一hub做有界exact-ID fan-out，再以绑定唯一target/origin/path的operation grant执行业务操作；断言discovery grant不能read/send/wait、spoke无递归、未授权不泄露、多候选要求qualified link，hub/target nonce跨重启防replay且失败零workspace副作用。重连轮换channel epoch/route revision但已提交outbox不改GlobalThreadAddress/communication/preimage或重新裸ID选路。用fake deadline覆盖grant寿命不足，并在WebSocket保持时热发布撤权/恢复，证明cursor/selector不能绕过最新授权、恢复后下一调用可用且已接受send不回滚。
  - 8.13-C 测试composition注入确定性且UUIDv4-profile合法的IdentifierFactory，生产保持随机factory；Session/child仍由正常Gateway/Web API创建，ModelStream fixture精确匹配动态ID。active child接收第二条用户消息时，fixture必须让第一条execution继续至少一次tool-loop，并从Provider request、history和DOM证明ExecutionContextFence不把queued root泄露给旧execution且刷新后Turn因果顺序不受物理append交错影响。模块进入独占serial组并以PID/start-time校验的E2EProcessLease租用避开8010–8016的端口；不得杀未知监听者、抢有效lease或共享开发home，teardown只清理本次owner并验证端口释放。
  - 8.13-D 周报场景对至少一个目标做多页read，在两页之间追加目标Turn并执行独立view变化；断言报告只包含首个ReadContextSnapshot上界，cursor重用仍重新授权，撤权/过期/参数冲突及AEAD token篡改显式失败且不会泄露内部字段、静默混入新revision或产生目标副作用。
  - 8.13-E 同一E2E模块把两个child加入单个Session team并通过右侧侧边栏核对role/task/coordinator；在一个child排队期间及其active tool-loop两次model call之间修改team状态，分别验证默认`turn`只在取得active slot时冻结一次且后续call不漂移、显式`team_state=model_call`才在第二次call激活新revision；两者各revision均不重影、请求路径无ledger读取、cold child不因fanout唤醒，并断言跨Session member attach或send携带team/task/Goal状态被拒绝且没有部分ledger/context mutation。
  - 8.13-F 在target已冻结远端read page或无selector wait baseline、source尚未保存response/tool result时重启source；恢复同一source call/tool invocation后必须沿同一RemoteObservationRecord/PageRecord返回相同page/selector/result，目标期间新增Turn/Job不混入且该调用在history只有一个tool result。另验证read第二页使用新的source call ID但cursor恢复同一observation/read series。对wait在相同clock origin进程重启及origin变化两种情况验证DurableDeadline不延长原60/300秒预算；不安全恢复返回`deadline-clock-unavailable`/timed_out。过期记录返回`operation-retry-expired`，不得静默重开。
  - 8.13-G 在同一child的第一条execution等待tool result时提交第二条queued用户Turn/root，再让第一条继续追加reasoning/tool/final item，刻意形成两个Turn物理item sequence交错；history API、live DOM和刷新DOM必须分别按`turn_item_projection.logical_item_ordinal`得到各自准确的identity/order/`item_count`/`elapsed_ms`，并证明`first_item_sequence`/`last_item_sequence`之间夹入的另一个Turn item不会被计数、展开或计时。
  - 8.13-H 为专用Session在创建目标Turn前通过正常workspace配置/ToolSet API启用`compact_conversation`，再在基础工具循环的独立Turn中让确定性模型调用该真实产品工具触发一次context compaction；禁止test-only压缩开关、绕过正常ToolSet流程或预写projection。断言canonical `compaction_summary`以同identity的独立可展开history response part出现在正确logical ordinal，相对于该Turn其它可展开成员（包括压缩操作自身的tool_call/tool_result）只额外计1且elapsed由后端给出；live、终态history和刷新DOM均不得把它伪装成thinking/final、重复carrier或按DOM节点重算。
  - 8.13-I 在同一E2E模块用barrier让child创建/发布、team mutation/fanout、target inbox acceptance/execution binding及独立`skill_load`/ToolSet control分别与Session删除竞争；每类都验证gate决定唯一顺序：操作先行时存在捕获旧generation的可恢复lease且删除等待terminal，删除先行时workspace无新thread/context/inbox/ambient/wakeup/Job/attachment副作用。另让已开始的cold history shared guard阻塞删除取得topology exclusive并提交catalog deleting，reader完成后删除才可线性化；在catalog已deleting、部分local fence仍active的注入崩溃点重启backend，断言catalog metadata可观察但新thread history/detail及mutation明确`session_deletion_pending`、恢复继续同一删除且无重新active或late callback写入，并验证删除排空期间gate可由受许可旧lease短时取得而不会死锁。
  - 8.13-J 让target read/wait的`RemoteObservationRecord`建立与Session删除竞争：删除先行时不得产生snapshot/baseline/subscription，observation先行时删除按原lease完成冻结读取或返回明确target-deleted并等到terminal；覆盖最大300秒wait被删除取消、source恢复时每个source call只收到一个tool result。catalog tombstone后普通resolver不可打开隔离节点，但同一source call在恢复窗口内以新授权可定点重放同一terminal envelope；窗口结束才物理清理并返回`operation-retry-expired`，不得提前清理或静默重开observation。
  - 8.13-K 在同一E2E模块通过test harness barrier分别让公开`full_rollout_copy`与board `materialize_thread_copy`停在source snapshot capture前/中间，并与source Session删除跑双顺序；capture固定revision后让source另提交Turn/view变化，证明target只含冻结SQLite snapshot、JSONL committed offsets与detail manifest。再从API、deletion/creation record、catalog、target history和trace证明删除先行时不发布target/child且整批board保持旧状态，shared guard先行时删除在capture结束前不能关闭fence，释放后source可删除且copy只靠`SourceCopySnapshot`完成。对partial capture和captured-marker到record-CAS崩溃分别断言定点abort/继续同一snapshot；多member board逐source capture，不存在coordinator/source或source/target gate重叠、扫盘补齐、换revision、部分child、删除后回读隔离节点或刷新后历史缺失。
  - 8.13-L 在同一E2E模块创建detached与pinned两种本地fork：用barrier验证pinned claim先于capture，source删除先行时fork无可见target，claim先行时删除在整树catalog deleting前显示具体retention blocker；target提交后的恢复只激活原claim。随后删除pinned target，验证`ForkRetentionReleaseRecord`跨两端崩溃恢复后释放source并允许删除，且全程没有双gate。另用federated Session link尝试fork，断言明确拒绝且远端catalog/retention/context零副作用；send/read/wait仍可正常工作。
  - 8.13-M 为source Turn附加available workspace attachment后执行公开copy和board materialization，在attachment claim提交后删除source并于target/board发布前触发GC；断言正文始终只有一个`blob-id`，claim阻止回收，reserved owner在target/child catalog发布前不可访问、发布后DOM/history/tool capability可用。分别在claim preparing、owner_reserved、publication后/claim committed前重启，验证只按record claim ID继续/释放，无悬空target ref、重复blob、提前授权或其它copy claim串用；required缺失使整次不发布，非required unavailable历史ref明确显示不可用。
  - 8.13-P 在唯一Web E2E模块跨两个UTC日期用受控Clock经正常API创建Session，验证SQLite catalog给出冻结`sessions/YYYY/MM/DD/{session_id}` locator、Folder多层导航无实体目录；让活跃main/child持续tool-loop时移动/重命名Folder和Session，验证breadcrumb/`parent_session_id`投影变化而physical locator、item/assembly ID、history/live/刷新、fork/delegation lineage和stable prefix均不变。再对含多个Session的Folder执行递归删除并在整树deleting后、部分日期目录隔离后重启：DOM/API不能看到半棵active子树，新准入全拒绝，恢复按同一batch终结所有tombstone且不扫盘。非递归删除非空Folder、环/跨workspace parent、Gateway stale export和旧JSON目录无runtime fallback都要有明确失败断言。
  - 8.13-Q 在唯一`tests/e2e/clients/web/test_basic_chat_tool_loop.py`中用真实Web DOM+Gateway/workspace回执+SQLite只读trace验收会话目录乐观协议：服务端分别阻塞enqueue回执和worker提交时连续创建Folder、立刻移入Session、移动多个Folder/Session及同node二次移动，断言前端在HTTP未返回时已更新且标pending、未给Gateway/CSM提交虚假状态；失败Folder使依赖move回退而独立命令继续；本地IndexedDB写失败必须立即撤销且Backend无入队副作用。另以双客户端冲突、202/终态SSE丢失、乱序事件/cursor gone、分页revision_changed、页面刷新/Backend重启恢复验证同operation ID无重复、只按最新后端snapshot回退失败链且不覆盖其它客户端成功。递归删除覆盖preflight blocker时恢复可见、pending隐藏不关闭已打开聊天，以及catalog deleting后排空崩溃时不可假回滚，检查pending/committed/repair状态、node计数/breadcrumb/search/分页和用户可定位错误；harness只暂停外部phase，不改业务事实，正式产物仍在同名E2E目录。
  - 8.13-N 扩展pytest外置`E2ETestControlHarness`：除独立residency/wait clock外，按operation identity/phase提供communication admission、Session lifecycle与copy capture barrier，覆盖deletion journal/fence/final rename、source revision freeze、attachment claim、captured marker、owner_reserved、target/board publication及attachment finalization。fixture backend只注入可暂停并ack的phase observer，生产只绑定立即返回no-op实现且无test路由/热开关；barrier不得改业务状态/identity/time/result。pytest收到ack后从进程外终止/重启，禁止backend test hook自杀；验证不同operation/phase不串扰且harness状态不进入业务库/checkpoint/canonical trace事实。
  - 8.13-O 在同一E2E模块让target/board已经发布但claim仍为`owner_reserved`时，由copy finalizer与target/coordinator Session删除竞争同一gate：验证finalizer先行会在释放gate前提交全部claim并把settlement/board record分别推进成功终态`committed|published`，删除随后正常释放owner；删除先行则排空流程释放reserved claim/ref，或验证committed claim归属后持久释放其target owner ref，再分别推进删除终态`target_deleted|coordinator_deleted`。重启恢复不得重建/保留owner、复活target/child、留下非终态record或GC泄漏。

8.3/8.4/8.7/8.13 的未完成验收同步覆盖新版ToolSet与source role边界：只有Provider可见直接工具或固定`invoke_extension_tool`信封形状变化才hard rebase；扩展目标目录增删改不能制造ToolSetRef/epoch。source owner typed`root_placement`必须在初次组装、实际compaction/rewind、Provider ToolSet hard rebase或fork目标首次assembly的新root编译中生效，同epoch变化一律独立user-role追加；校验新root去重、base/delta lineage、旧sealed bytes、tool call/result因果顺序与MCP指引默认`tail_only`。在唯一Web E2E模块和分层测试中分别验证默认Turn/显式model_call下MCP工具新增、改schema、删除、无通知server刷新、空目录信封恒定、旧in-flight调用、撤权、重连及错误candidate不半发布；前端live/history/刷新不能按binding重复计Item。

## 9. Resource activation snapshot 与虚拟资源 provenance（本轮新增，未实施）

本节只实现itemized rollout对资源激活结果的持久化、assembly binding、恢复和投影；Resource Observation Platform、SourceReconciler、ResourceDerivationGraph、ResourceRegistry、Virtual Resource Namespace、`skill_load`和activation policy由`add-context-injection-lifecycle`及其design 5.3目标目录拥有。本change不得复制monitor、loader、resolver或CSM状态机，也不得直接读取资源。

- [ ] 9.1 在`app/domain/itemized/`定义并冻结`ResourceActivationSnapshotRef`、`ResourceProvenanceRef`、`snapshot_kind=turn|model_call`、parent Turn snapshot、activation policy revision/hash、owner/session/thread/turn/model-call字段，以及每个binding的effective boundary、captured Registry generation、resource/facet/语义revision/ordinal、availability、语义length/hash、safe display URI、snapshot/detail typed ref、受保护`source_lineage_ref`与覆盖source_id/revision向量和derivation版本的`source_lineage_digest`；纳入破坏性schema清单与跨语言golden，不创建旧字段reader。`bindings_hash`/plan hash只含实际有序语义选择，不含来源raw revision/lineage；`activation_provenance_hash`额外包含source lineage digest及policy/capture/parent关系，parent ID由FK/binding复用校验，运行identity/captured_at不进内容hash；拒绝assembly单值boundary、provider locator、credential、绝对路径、旧路径与任何别名字段。
- [ ] 9.2 在thread-owned SQLite中增加activation snapshot catalog、resource binding manifest、assembly binding及owner/turn/model-call/语义revision定点索引；正文和source lineage manifest只进入现有受保护detail/snapshot body store，保存并校验lineage digest/ref。以显式一次性schema migration升级既有v2数据库，迁移不能从当前文件、URI、middleware状态或generic read记录补造provenance/lineage，缺失信息进入明确migration loss/quarantine；正常runtime只打开新schema并fail closed，不保留旧表读取、动态升级或双版本分支。
- [ ] 9.3 扩展`ContextRequestPlan`、Saver seal与`assembly_sealed`提交，使其只接受activation coordinator冻结的内存snapshot并原子绑定selection/plan hash：active slot冻结Turn内不可变policy与turn-bound binding；存在model_call override时生成parent-linked组合snapshot，逐字节复用turn-bound binding并只替换model-call-bound binding。required resource仍dirty/gap/unavailable时有界等待或失败，禁止Turn内policy漂移及seal/dispatch路径执行stat、scan、read、HTTP fetch、memory-provider lookup或当前Registry fallback。
- [ ] 9.4 扩展LangChain/native Provider/history API/Web response-model projection统一消费sealed resource refs；策略允许的history detail只携带安全`boxteam://` URI/revision/availability和opaque provenance ref，不泄露locator/credential/body，本change不新增专门的provenance UI。request-only resource binding不得计入Turn`item_count`、`elapsed_ms`或可展开item；live终态、刷新、重启和跨端读取必须使用同一identity/order，前端不得按正文、路径、URI或DOM推断/去重。
- [x] 9.5 让checkpoint restore、retry、rewind、compaction、fork和materialization保留精确activation snapshot/ref：旧assembly永不解析当前URI或Registry；target copy生成target-local operational identity并保留source lineage；retention导致body不可用时返回明确loss/error，不读取同名新资源、不改写stable prefix bytes。（注：materialization 面依赖 8.11 未实现的 migration-only materialize_thread_copy，待 8.11 落地后补验收；9.5 其余五面本轮已取证。）
- [ ] 9.6 删除ItemizedContextProjectionMiddleware反向捕获、PromptReplay残留、path-based resource provenance、generic read Skill推断、当前resource/Registry恢复分支及重复writer/projector；若framework必须保留接入点，只实现不持有状态且只转发sealed assembly reference的dispatch bridge。完成后执行AST/import/schema审计，确认无兼容adapter、字段alias、dual write、fallback projection或旧模型可见Skill路径。
- [ ] 9.7 增加domain/storage/assembly/restore/projection/retention分层测试，并在`tests/e2e/clients/web/test_basic_chat_tool_loop.py`现有唯一owner中验证：默认Turn、显式model_call及同assembly混合边界；Turn内policy热更新不生效而下个Turn生效；`skill_load`固定使用发起tool call snapshot；一源多Skill facet、多源一effective config、来源raw变化但选中facet不变、lineage损坏fail closed；异步变化但请求路径零源I/O、safe VRN、locator不泄露、Skill/AGENTS/team revision、rewind/compaction/restart精确恢复、零Item Turn不误计resource binding、live/history/刷新一致及旧路径拒绝。记录命令、退出码和产物，运行两份change strict validate且不得用旧E2E或文档校验勾选本节。

9.1–9.7 的未完成验收还必须增加typed`ExtensionCatalogBindingRef`及独立`extension_dispatch_binding_hash`的schema、SQLite/受保护snapshot、assembly seal/restore/fork/retention与golden；catalog semantic revision/hash、指引revision、target/schema identity、server generation验证ref和策略revision按同一ModelCallResourceSnapshot原子绑定，不能混进`context-plan-hash:v2`、Provider ToolSetRef或`request_hash`。历史binding丢失显式`extension-catalog-unavailable`，不从当前MCP目录或同名工具恢复；ToolSetRef仍只含直接工具+固定信封。Provider prompt/schema、AgentFactory、重试reminder、配置模板、Web显示及测试中旧`invoke_extension_tool`全部改为`invoke_extension_tool`，不留兼容别名；改动只勾选实际验证完成的原任务。

## Architecture decomposition plan（不新增任务计数）

以下是 3.2 冻结归属时确定的生产拆分验收清单。A0-A11 已由 7.5-F 的最终 v2-only import/owner/规模审计全部闭合，不把单独文件移动、临时 import shim 或局部静态检查作为完成依据：

- A0（已完成）：为 `app/domain/`、`app/domain/itemized/`、`app/services/infrastructure/rollout_context/` 及其 `runtime/`、`storage/`、`assembly/`、`execution/`、`operations/`、`checkpoint/`、`provider/`、`migration/`、`app/services/mapping/itemized/` 创建目录时同步补齐各自 `AGENTS.md`，每份都必须包含“目录用途”“可修改内容”“不可修改内容”“规范”四部分；不得修改 `app/core/AGENTS.md`，通过迁移归属消除通用内核边界冲突。
- A1（已完成）：从 `app/core/itemized_rollout.py` 和当前 `app/domain/itemized/plans.py` 提取 `schema.py`/`enums.py` → `refs.py`/`selection.py`/`request_plan.py`/`assembly_snapshot.py` → `serialization.py` → `validation.py`/`hashing.py`，目标为 `app/domain/itemized/`；`plans.py` 只能作为一次性拆分来源，完成后删除重复领域职责。旧格式 reader/report/quarantine 不进入 domain，而进入 `app/services/infrastructure/rollout_context/migration/` 并只由一次性 `legacy_import_v1_to_v2` 调用。提取期间旧路径至多是指向 v2 类型的临时 import shim，完成迁移后删除；禁止 domain 依赖 SQLite、路径 I/O、LangChain、Provider 或 Agent 流程。
- A2（已完成）：将 `app/core/rollout_storage.py` 及当前 `app/services/infrastructure/rollout_context/storage/service.py` 收敛为只做依赖组装、port 路由和事务边界的薄 facade；下沉 `storage/jsonl.py`、`catalog.py`、`commits.py`、`recovery.py`、`schema.py`，`checkpoint/view_anchor.py`，`execution/turns.py`、`executions.py`、`model_calls.py`，`assembly/manifest.py`、`detail.py`、`overlay.py`、`selection.py`，以及 `operations/fork.py`、`compaction.py`、`pruning.py`。history/projection 必须进入 `app/services/mapping/itemized/`，不得留在 storage。storage 只依赖 domain、session path resolver 和 I/O port，不得 import LangChain 或 mapping；`storage/migration.py` 只做 v2 schema/data migration，v1 reader/report/quarantine 必须另置于 `rollout_context/migration/` 且只能由一次性 `legacy_import_v1_to_v2` 调用。
- A3（已完成）：从 `app/core/itemized_context_runtime.py` 依次提取 `runtime/detail_store.py`、`ledger.py`、`composer.py`、`reconciliation.py`、`stream_accumulator.py`；detail/security 与 realtime ledger 属 infrastructure，不能回流为 `app/core` 通用能力，也不能复制 `assembly/manifest` 或 mapping 投影。
- A4（已完成）：将 `itemized_projection.py` 的无 I/O history/LangChain/selection 映射提取到 `app/services/mapping/itemized/`，Provider-specific request/tools bridge 置于唯一的 infrastructure/provider owner；从同一 Saver-owned selection 消费，不得自行扫描 storage 或排序。mapping 不得 import storage、SQLite、文件 I/O、Provider SDK 或 context reader。
- A5（已完成）：从当前 `checkpoint/saver.py` 与 `checkpoint/owner.py` 依次收敛出唯一 `checkpoint/context_owner.py` facade，并提取 `persistence.py`、`view_anchor.py`、`fork_compaction.py`；context owner 只拥有 plan/assembly/view port，persistence 只拥有 checkpoint/view/anchor durable commit，fork_compaction 只拥有 fork/rewind/compaction adapter，assembly manifest/detail/overlay 仍由 A2 的 assembly owner 负责。`saver.py`/`owner.py` 不得并行保留 owner 事实；旧路径若暂时保留只能作为删除门槛明确、只转发 v2 的 import shim，不得暴露 v1 reader 或双路径 checkpoint/history，所有生产 import 和相应 change gate 闭合后必须删除旧聚合职责与 shim。
- A6（已完成）：迁移顺序固定为临时 shim/AGENTS → v2 domain（含 plans 二次拆分）→ storage 薄 facade 与 JSONL/catalog/commit/recovery/schema → assembly manifest/detail/overlay → checkpoint/view/anchor 与 Turn/execution/model-call → runtime → projection/mapping/provider → Saver context owner/fork/compaction → 业务/orchestration 边界 → v2-only import/runtime 与架构规模审计 → 删除重复实现和 shim；v1 reader/report/quarantine 仅由一次性 migration/import 入口调用。每步保持唯一事实源、兼容表名和 owner boundary，不能以并行双写、双 projector、双 schema、storage→mapping/LangChain 跨层 import 或超大聚合 facade 缩短顺序。
- A7（已完成）：从 `app/agents/providers/litellm_content.py` 提取 provider response/content-block normalization 与 provider-level stream normalization，保留 wrapper 和现有 format-check 归属；history/LangChain 纯映射进入 `app/services/mapping/itemized/`，ToolSetRef→外部 provider wire 只由 `app/services/infrastructure/rollout_context/provider/toolset_request_bridge.py` 实现，禁止三处重复投影。`app/agents/providers/` 已有 AGENTS.md，不重复创建；新增 source subdirectory 才补四段式 AGENTS.md。
- A8（已完成）：从 `app/services/business/message_service.py` 提取 canonical reasoning merge、message/attachment DTO、agent-state/history projection 到既有 mapping 目标；cursor/page/service entrypoint 留在已有 `app/services/business/session_turn_history/`，business 只保留 session/message 业务规则和 Saver port 调用，不携带 item payload、storage reader 或 provider wire 事实。既有目录不重复创建 AGENTS，新建 mapping 子目录必须带四段式 AGENTS.md。
- A9（已完成）：从 `app/services/orchestration/agent_execution_service.py` 提取 step/control loop、retry/reminder policy、checkpoint/context adapter、tool execution/event transition；orchestration 只协调协议调用和顺序，不保存 canonical/storage/provider 事实。文件留在已有 orchestration 目录时不重复创建 AGENTS，新建子目录才补四段式 AGENTS.md。
- A10（已完成）：从 `app/services/orchestration/message_stream_runtime.py` 提取 trace observer、stream block/delta assembler、临时 tool-call registry、terminal/finalization state machine；assembler 只接收 provider 已归一化 block/delta，terminal 结果交给 Saver/storage adapter，不能直接成为 canonical item 或 provider content 第二事实源。既有 orchestration AGENTS.md 继续适用，新子目录必须带四段式 AGENTS.md。
- A11（已完成）：执行 provider response normalization、纯 mapping、external request bridge、orchestration、storage/domain/checkpoint/assembly 五类 import/owner 与规模审计，确保 `ToolSetRef` 只有一个 wire bridge，storage 不 import LangChain 或 mapping，mapping 无 I/O，infrastructure 不承载纯模型转换，orchestration 不持久化；逐项记录 >800 行文件、混合两个以上领域职责、目标目录直接源码文件 >20 的清单及拆分/审查证据。未完成前不得因单文件拆出 helper、临时 facade、局部 smoke 或核心 E2E 勾选任何新任务。

### v1 代码生命周期边界（不新增任务计数）

`app/domain/itemized/`、`app/services/infrastructure/rollout_context/`、runtime、projection、history、provider bridge 和 checkpoint owner 的 v2 实现是唯一生产运行时事实源。旧聚合文件中的重复 schema、writer、reader、projector、history/checkpoint 分支不得作为正常运行时路径；不得通过 import switch、配置回退或错误恢复重新打开 v1。

v1 reader/adapter 只能由显式、一次性的 `legacy_import_v1_to_v2` migration/import 命令调用，用于读取原始 artifact、生成 staging/report/quarantine 和 rollback audit。它不提供长期兼容 API，不参与正常 history/provider/checkpoint/runtime/context compiler，不与 v2 dual write、dual projector、双 schema 或双事实源并存。提取期间若确需保留旧路径，只能是仅转发 v2、禁止读取 v1 且带删除门槛的临时 import shim；当目标模块、调用方迁移、v2-only import/runtime 审计和一次性 migration 命令均闭合时立即删除，最迟在 7.5、主 spec 同步和 change 完成前删除。v1 原始 artifact 可以继续作为不可变迁移/回滚审计材料保存，但不构成保留旧运行代码的理由。

## Verification ledger

本台账首节记录Section 1–7上一次已闭合实现边界；后续带“阶段/历史”标题的段落保留实施过程快照。新增Section 8–9重新打开整个change，其20项未实施状态优先于旧“完成”快照，任何旧E2E或strict validate不得据此勾选8.x/9.x。

### 2026-09-09 Turn 逻辑 Item 权威投影增量

- PASS（6.9 请求与内存边界）：history page 与 detail 同 revision 返回后端权威 summary，Web 历史详情不再请求 `message.v1` availability/snapshot；后端终态 stream 固定为最多 16 条/16 MiB，单 block 256 Ki 字符、tool result/error 64 Ki 字符，重启只恢复未完成流；Web 乱序缓冲最多 256 条、全局终态流最多 8 个，详情最多保留 8 个或约 1M richness units，跨会话 timeline 使用 64-session LRU，淘汰只恢复后端原始 summary。扩大后端组合 `141 passed in 233.62s`，覆盖重启、缓存、大正文、历史索引与 projection；Web 9 文件组合 `117 pass`，覆盖 LRU、详情重载、stream 截断/乱序和 DOM。
- FAIL→PASS（6.9 Provider/tool-loop 时序）：正式 E2E 扩大验证先后暴露 model-call 在外层 `on_tool_start` 前开始而清空工具注册表，以及 live stream/checkpoint carrier 因正文 hash 不同被投影为两份 assistant。runtime 改为整 Turn 保留工具身份；Provider projector 改为同 Turn、同 `tool_call_id` 集合的组级替代，保留完整 checkpoint carrier，不再按正文/hash 去重；最终 assistant checkpoint 通过显式 `supersedes_message_id` 排除旧 carrier。专项 projection/runtime/reasoning `32 passed`，最终 `uv run pytest -q tests/e2e/clients/web/test_basic_chat_tool_loop.py` 退出码 0，`1 passed in 64.90s`，四次模型请求 role chain 不再包含重复 assistant。
- PASS（6.9 当前 8011 开发链路）：通过 worktree transient unit 完整 stop/start，当前 `boxteam-dev-e2fc883effdf1153.service` active/running，主进程 PID `2518496`，`BOXTEAM_HOME=/data/hyf/20260629_agent/vscode-graph-agent-opencode/out/development-runtime/boxteam-home`。真实 Chromium 打开 `http://127.0.0.1:8011/`，同源 `/api/gateway/users/current`、`/api/gateway/health`、`/api/gateway/workspaces`、`/api/v1/workspace` 均为 200 且响应头/体 request_id 一致；composer 可见、HTTP 404 流提示为 0，page error、console error、failed request 均为空。证据保存在 `out/tests/temp/chat-history-memory-bounds/artifacts/verify-8011.json`。
- PASS（6.9 生成物/静态）：`bun run check:protocol`、`bun run --cwd src/clients/web build`、修改 Python（排除 protobuf generated）的 Ruff、compileall 均退出码 0；自动生成协议未手工格式化。扩大回归曾发现两项旧断言错误地把 acceptance message 算入终态 commit 或要求跨事务回滚，已改为终态 commit 仅拥有 sequence 2、失败保留已提交 acceptance，定点 `2 passed` 后完整 141 项通过；Web API 测试也改为显式声明无 initializer 时的 `/api/gateway/users/current` 屏障。
- PASS（6.8 最终 checkpoint reasoning carrier）：开发历史 `ses_904c3c5c038446c08296b39f8837e3b9/job_106f48dc300f4fcd94ea505bde6997c5` 中，final item `content_part_refs.id=part_66f...` 与 canonical reasoning `metadata.block_id=part_66f...` 的同一持久 identity 已在后端合并；重启后的 8011 history API 返回 `item_count=5`，最终 `Confirming test tool success with results` 只保留 canonical sequence 12 一次。新增确定性 catalog 回归单项 `1 passed in 13.09s`；最终组合 `30 passed in 202.13s`；正式浏览器 E2E `1 passed in 65.41s`。
- PASS（6.7 后端 identity/order/count/time）：最终组合 `uv run pytest -q tests/unit/services/infrastructure/rollout_context/test_turn_activity_projection.py tests/unit/services/mapping/test_turn_response_parts.py tests/integration/backend/sessions/test_session_history_loading.py --tb=short` 退出码 0，`27 passed in 199.95s`；覆盖零 Item、逐 Item elapsed、相同正文不同 identity 保留、复用 tool_call_id 的不同 source coordinate、provider/checkpoint producer identity 解析、detail 只补工具 payload、终态未知结果和完整 history API。此前核心变更后的独立 history 集成为 `18 passed in 197.14s`，随后暴露并修复 detail+tool_summary 过滤后已纳入最终组合。
- PASS（6.7 Web history/live 边界）：`bun test src/clients/web/src/state/tests/turnTimeline.test.ts src/clients/web/src/state/tests/conversations.test.ts src/clients/web/src/state/tests/messageStream.test.ts src/clients/web/src/components/chat/ChatTurn.test.tsx` 退出码 0，`97 pass`；新增终态 live/history mismatch 显式诊断后相关三文件复验 `54 pass`。Web 收到 history 后整体采用后端 response parts，不按 message/text 重排去重；live 按 stream identity 临时计算。
- FAIL→PASS（6.7 正式 Web E2E）：首次 `uv run pytest -q tests/e2e/clients/web/test_basic_chat_tool_loop.py --tb=short` 为 `1 failed in 84.67s`，暴露 detail 模式下 `tool_summary` 错误过滤 canonical tool parts；修复后同一正式入口通过，并在最终 source-coordinate identity 修正后再次复验为 `1 passed in 64.99s`。最终两轮 DOM 分别为 `耗时 7.5s · Item 4 项`、`耗时 8.9s · Item 4 项`，刷新恢复保持首轮 7487ms/4 Item；展开均为 reasoning→聚合工具→reasoning 三块。history API 额外验证每轮四个 activity part 均携带 `item_id/item_sequence/part_ordinal/created_at/elapsed_ms`，坐标后端有序，页面/console/请求失败诊断为空。正式产物位于 `out/tests/e2e/clients/web/test_basic_chat_tool_loop/artifacts/basic-chat-tool-loop-result.json`。
- PASS（6.7 生成物/静态/规范）：协议与 OpenAPI 已重新生成；修改 Python 的 Ruff/compileall、`bun run check:protocol`、`bun run --cwd src/clients/web build`、`git diff --check` 和 `openspec validate add-itemized-rollout-context --type change --strict --no-interactive` 均退出码 0。

### 2026-09-08 当前边界最终闭合台账

- PASS（7.5-A item/commit/recovery）：`uv run pytest -q tests/integration/backend/sessions/test_rollout_commit_recovery.py tests/integration/backend/sessions/test_rollout_context_semantics.py tests/integration/backend/sessions/rollout_context`，退出码 0，`833 passed, 2 warnings in 390.17s`。覆盖 item-bearing/metadata-only terminal convergence、Turn/final item/view、partial/restart、SQLite/JSONL commit recovery、显式离线损坏库恢复和真实 checkpoint persistence owner。
- PASS（7.5-B ContextRef/ToolSetRef/selection/overlay/detail/三投影）：`uv run pytest -q tests/e2e/clients/web/test_basic_chat_tool_loop.py tests/integration/backend/agents/test_turn_message_stream_runtime.py tests/integration/backend/sessions/test_schema4_terminal_convergence.py tests/integration/backend/sessions/test_itemized_projection_contract.py tests/unit/services/business/test_reasoning_checkpoint_service.py tests/unit/services/orchestration/test_step_model_call_adapter.py`，退出码 0，`116 passed in 137.69s`。覆盖 owner/ref union、omission/loss、detail protection、source overlay、LangChain/native/history selection/order、并发 stream、Provider retry sealed binding 和 Web tool loop。
- PASS（7.5-C Turn/fork/replay/rewind/compaction）：上述 833 项 storage/recovery 组合与 `uv run pytest -q tests/integration/backend/sessions/test_itemized_migration.py tests/integration/backend/sessions/test_itemized_migration_integrity.py tests/integration/backend/sessions/test_itemized_migration_semantics.py tests/integration/backend/sessions/test_rollout_fork_migration.py tests/integration/backend/sessions/test_rollout_fork_modes.py tests/integration/backend/sessions/test_rollout_fork_plans.py tests/integration/backend/sessions/rollout_context/test_itemized_migration_legacy.py tests/unit/model_stream/test_assets.py` 均退出码 0；后者为 `148 passed, 1 warning in 91.50s`。覆盖 target-local fork identity、history replay/replay-as-new-turn、stale anchor、partial recovery、rewind/compaction 与终态收敛。
- PASS（7.5-D 一次性 migration/import/dispatch/rollback）：同一 148 项命令覆盖 v1 staging/quarantine、manifest/envelope mismatch、显式 migration 后 v2 full-copy、source overlay 恢复、原件保留、失败回滚和 v2-only dispatch；普通 full-copy 对 v1 source 返回 `v1_migration_required`，不隐式打开 legacy reader。
- PASS（7.5-E provider/tool-loop/Web/OpenSpec）：`uv run pytest -q tests/e2e/system/agent/storage/test_checkpoint_persistence.py -x` 退出码 0，`1 passed in 40.69s`；三次模型回放按首轮、带一轮历史、重启后带两轮历史的不同 role chain 严格匹配，证明 checkpoint 保存、续聊投影与进程重启恢复，且不访问外部 Provider。上述 116 项组合包含真实 Chromium/Gateway/workspace backend 的两轮 tool-loop E2E。OpenSpec status/instructions/strict validate 另行执行；strict validate 只作为 artifact 结构证据，不替代实现测试。
- PASS（7.5-F v2-only/import/owner/规模审计）：`rg`/Python AST/`find`/`wc -l` 审计退出码 0；无旧 `app.core.rollout_*`、`migration.projections` 或旧 storage message-projection 引用；v1 reader 仅位于一次性 migration 边界；storage 不 import LangChain/mapping，mapping 不执行 I/O。`app/domain/itemized` 直接 Python 文件 18 个；runtime/storage/assembly/execution/operations/checkpoint/provider/migration/fork 分别为 12/17/10/9/1/19/2/9/17，均不超过 20；最大相关源码 `fork/remap_prepare.py` 为 739 行，无新增或重构文件超过 800 行。checkpoint projection 的 SQLite locator/materializer 与纯 mapping 分层，旧 dual projector 和临时 shim 已删除。`remap_prepare.py` 为同一 full-copy preparation 职责，是 P2 maintainability 残余风险，不构成当前门槛阻断。
- PASS（模型 fallback 缺陷回归）：`StepModelCallAdapter` 只有在上一 model-call 的 `failed` 终态成功提交后，才允许 Provider fallback 复用同一 sealed assembly/execution；completed/cancelled/interrupted/unknown、过期回调或终态提交失败都保持 fail-closed。专项 `42 passed in 8.67s`，并已纳入 116 项组合。
- PASS（静态与卫生）：最终修改文件的 Ruff、`uv run python -m compileall -q`、`git diff --check` 与 OpenSpec strict validate 均要求退出码 0；正式测试产物保留于对应 `out/tests/`，未提交 Git、未创建 PR、未归档 change、未同步主 spec。
- PASS（损坏 SQLite 显式恢复）：在线 restore 保留 live-reader/same-inode 合同；完全损坏 target 只允许显式 offline restore，在安装前验证备份，隔离原 target/sidecar，原子安装并写入 hash manifest，失败回滚。普通 startup/read 返回 `recovery_required`，禁止从 JSONL 重建或静默覆盖。

### 2026-09-08 schema4 正式生命周期接线阶段历史证据

- 用户要求在当前导入来源清单边界收尾后暂停继续实施；整体仍为 27/43，不把本阶段边界完成当作整个 change 完成。当前没有启动新的 middleware/Turn 或损坏 target 恢复实现。
- 当前边界实现完成：`context-plan-source:v1` 保存独立 target-local refs/无正文 contributions，不持有虚构 draft 创建键、selection 或最终 assembly/ordinal；`context-plan-import:v2` hash 同时绑定 provenance、来源清单与 sealed snapshot/detail。runtime 与 imported 恢复入口互斥；import 的来源参数必填，没有从 snapshot 自动补造来源的兼容路径。schema3 仅在显式升级中提取冻结旧 registry，fork 从已验证的 runtime draft 或 imported manifest 重新映射；omitted 贡献继续不读取正文、不分配最终绑定。
- PASS（导入与 fork 边界最终专项）：`uv run pytest -q -n 0 tests/integration/backend/sessions/rollout_context/test_context_plan_imported.py tests/integration/backend/sessions/rollout_context/test_context_plan_import_bindings.py tests/integration/backend/sessions/rollout_context/test_context_plan_import_sources.py tests/integration/backend/sessions/test_rollout_fork_protected.py tests/integration/backend/sessions/test_rollout_fork_plan_registry.py tests/integration/backend/sessions/test_rollout_fork_reader_locks.py tests/integration/backend/sessions/test_rollout_fork_import_sources.py --maxfail=3 --junitxml=out/tests/integration/backend/sessions/test_rollout_fork_import_sources/artifacts/import-v2-owned.xml`，退出 0，`151 passed in 93.29s`，JUnit 0 failure/error/skip。包括原先两个 unknown omitted binding 失败用例、合法 direct/alias/overlay base/delta 来源的连续两次真实 full-copy 与新 Saver 恢复、禁止 detail read/write、真实 SQLite authorizer 禁止读取旧 session contribution 表、隐私/schema/hash/index 篡改拒绝和源文件不变。首轮 source 专项 `39 passed/1 failed` 是 fixture 拦截不存在的方法，已改为真实 SQL authorizer，未删除正向断言。
- PASS（本边界静态与结构核对）：主线对 plans、schema_v4、full_copy、schema_plans 和本边界 tests 执行 Ruff/compileall，退出 0；AST 核对四个生产 import/hash 调用点均显式传 source_manifest。三个目录直接 Python 文件为 11/5/8，AGENTS 均在；触达 source/import/plan 模块最大 339 行，无新增连接、事务提交或正文 writer。此限定审查不替代整个 7.5-F。
- 结构残余风险：既有 `fork/remap_prepare.py` 本轮也接入来源 identity 映射，仍为 739 行，保留 identity/JSON remap 与 JSONL 准备的集中职责；Ruff/compileall 和上述 fork 专项通过，不等于其大文件职责拆分已完成。按用户要求不在暂停前扩大重构，7.5-F 继续未勾选。四个 subagent 已结束并关闭；本边界没有运行中的测试 handle，没有新临时产物，仅保留正式测试目录的复查产物。
- PASS（来源清单 DDL 的 runtime 隔离）：新增 `context_plans.source_manifest_json`，runtime 必须 NULL、显式 imported 必须非空；恢复也拒绝绕过 SQL CHECK 后给 runtime 注入 provenance/source manifest。主线 registry 专项 `32 passed in 11.79s`；同本节五文件 schema4 组合最新 `183 passed in 26.88s`，退出 0。imports 的 port、migration/fork 来源提取与对应回归另行验收，不以 runtime 通过代替。
- PASS（来源清单下的显式升级）：`uv run pytest -q tests/integration/backend/sessions/rollout_context/test_schema_v4_upgrade.py --tb=short --junitxml=out/tests/integration/backend/sessions/rollout_context/test_schema_v4_upgrade/artifacts/source-manifest.xml` 为 `42 passed in 36.64s`；`uv run pytest -q tests/integration/backend/sessions/rollout_context/test_schema_upgrade.py tests/integration/backend/sessions/rollout_context/test_schema_upgrade_entrypoint.py tests/integration/backend/sessions/rollout_context/test_schema_v3*.py --tb=short --junitxml=out/tests/integration/backend/sessions/rollout_context/test_schema_upgrade/artifacts/source-manifest-old-matrix.xml` 为 `73 passed in 83.57s`；均退出 0，无排除/跳过。known omitted 从冻结旧 registry 提取而不是反推 selection，伪造/缺失/不一致来源拒绝；禁止 detail 正文读取，原 snapshot/JSONL/detail 保留。非空 schema1 仍不支持。
- PASS（来源清单 DDL 后的真实 Saver caller）：`uv run pytest -q -n 4 --dist loadfile tests/integration/backend/sessions/rollout_context/test_context_dispatch.py tests/integration/backend/sessions/rollout_context/test_turn_execution_recovery.py tests/integration/backend/sessions/rollout_context/test_schema4_checkpoint_seal.py tests/integration/backend/sessions/rollout_context/test_schema4_checkpoint_retry.py --tb=short --maxfail=3`，退出 0，`85 passed, 1 warning in 26.71s`。warning 是既有 Pydantic usage 序列化告警，native HTTP 使用本地 Provider 桩，属于 Integration。
- PASS（schema4 业务与调度最新复验）：`uv run pytest -n 4 --dist loadfile -q tests/unit/services/business/test_message_service_checkpoint.py tests/unit/services/business/test_reasoning_checkpoint_service.py tests/unit/services/orchestration tests/integration/backend/sessions/test_session_history_loading.py tests/integration/backend/sessions/test_checkpoint_message_groups.py --tb=short --disable-warnings --maxfail=3`，退出 0，`230 passed in 214.68s`。此结果补齐上阶段 orchestration 在正式 schema4 下的验证。
- PARTIAL（备份恢复新发现）：同 inode SQLite backup 恢复已修复在线 reader 的一致性，`test_schema4_restore_reader.py` 22 项通过；与 checkpoint Saver/commit recovery 组合为 `92 passed, 1 failed in 40.68s`。未闭合项是既有 `test_sqlite_backup_restores_authoritative_checkpoint_state`：完全损坏的 target 返回 SQLITE_NOTADB，不能使用在线 backup 覆盖。没有删除或放宽此断言；原件保留，安全损坏库恢复不纳入当前暂停前边界。
- 完成计数仍为 27/43。以下证据替代旧“全局 schema3/测试手工建表”阶段状态；不替代公开 Saver 三入口、显式非空升级、full-copy 和最终 Web/Provider 验收。
- 全局 `ROLLOUT_SCHEMA_VERSION=4`，正式初始化由 storage schema owner 建立 context_plans/ref/contribution/failure registry 和 nullable ToolSet binding。移除 `test_context_plan_registry.py` 的 fixture DROP/CREATE DDL，改为断言真实初始化的 schema4/active；没有在普通旧库启动/读取中补表或回填数据。
- 真实 storage seal 强制已注册 plan、非空 seal key 和规范 JCS seal_input_hash；同一 BEGIN IMMEDIATE 覆盖预检、一个 metadata-only assembly commit、snapshot、selection、ToolSet NULL→assembly 与 plan unsealed→sealed。删除旧“工具行缺失即 INSERT”的封存分支；不同 key/input/snapshot 的重试明确拒绝。runtime seal_hash 同时绑定调用输入摘要和最终 snapshot/detail，恢复不能独立篡改 seal_input_hash。
- `assembly/manifest.py` 改为 plan-scoped registry 权威，删除 session `context_contributions` 查询与 source_ordinal 容错；SQL 比较使用实际 JSON 序列化边界，仍保留严格类型/逐字段检查。补上 omitted contribution 不能新造 unknown ID 或借用另一 ref 映射的校验，不读取 omitted 正文/工具定义。
- PASS（正式 schema4 主线组合）：`uv run pytest -q -n 4 --dist loadfile tests/integration/backend/sessions/rollout_context/test_context_plan_registry.py tests/integration/backend/sessions/rollout_context/test_context_plan_registry_toolsets.py tests/integration/backend/sessions/rollout_context/test_context_plan_registry_privacy.py tests/integration/backend/sessions/rollout_context/test_schema4_seal_transaction.py tests/integration/backend/sessions/rollout_context/test_schema4_assembly_manifest.py --tb=short --maxfail=3`，退出 0，`181 passed in 27.46s`。覆盖 registry30、ToolSet31、privacy78、真实 SQL seal15、assembly manifest27。此前 176 passed/1 failed 是伪造 header fixture 未补新必填 input hash，补齐伪造输入后仍验证缺 snapshot 拒绝；不是放宽数据库约束。
- FAIL→PASS（真实 SQLite 锁缺陷修复）：独立 `test_schema4_crash_reader.py` 首次 5 passed/3 failed，发现 storage `_connect` 裸文件 open/read(16)/close 导致同进程 SQLite POSIX SHARED lock 丢失，旧 WAL/SHM 描述符残留，子进程 COMMIT 后错读 unsealed；SQLite 官方 howtocorrupt 第2.2明确描述该情形。删除该裸读取，格式校验交给原 SQLite schema 查询；不通过关闭合法父读连接隐藏问题。
- PASS（修复后的真实进程退出与恢复组合）：`uv run pytest -q -n 4 --dist loadfile tests/integration/backend/sessions/rollout_context/test_schema4_crash_reader.py tests/integration/backend/sessions/rollout_context/test_schema4_seal_transaction.py tests/integration/backend/sessions/test_rollout_commit_recovery.py --tb=short --maxfail=3`，退出 0，`54 passed in 12.22s`（8/15/31）。包含保留 reader 的内核锁/inode 证据、COMMIT 前后 os._exit、重启/幂等重试和既有 JSONL recovery；没有用 Python 异常回滚冒充硬退出，也不代表断电/Windows 运行时验收。
- PARTIAL（正在接线）：新增 runtime/schema3_import/fork_import 来源合同；import 只允许 sealed，creation/draft/input-hash 字段 NULL，来源与原 snapshot 指纹显式绑定。Saver 显式升级已注入 schema3→4 SQL-only prepare/verify port，公开三入口与 imported/fork owner 正在组合复验；旧 schema3 一 plan 多 assembly 的明确重映射尚无验收，不选择最新记录或自动合并。
- PASS（schema4 显式升级主线复验）：`uv run pytest -q -n 0 tests/integration/backend/sessions/rollout_context/test_schema_v4_upgrade.py --tb=short --maxfail=2`，退出 0，`35 passed in 30.90s`。来自冻结 schema3 DDL，覆盖非空 2→3→4 公共 Saver 串联、3→4 SQL 确定性/只读准备、失败 journal 保留和重试、COMMIT 前后硬退出、旧 snapshot/JSONL/detail 原件不变与 omission；旧 schema1/2 的全测试矩阵仍由迁移 owner 继续迁移，不能以此宣告所有 migration gate 完成。
- PASS（新 schema4 下 domain/core 扩大回归）：`uv run pytest -q -n 4 --dist loadfile tests/unit/domain/itemized tests/unit/core/test_itemized_contract.py --tb=short --maxfail=3`，退出 0，`1075 passed in 110.27s`，无 xfail/skip。该结果限于此命令的 domain/core 合同，不替代 Provider/浏览器或 fork。
- 本阶段修改批次执行 Ruff/compileall，正式测试产物留在同名 out/tests；未提交 Git、同步主 spec、归档或改写用户会话。

### 2026-09-08 草稿注册、严格迁移重试与跨链路阶段历史证据

- 完成计数仍为 27/43。以下是当前增量证据；旧 typed detail/schema3 初期记录移至后面的历史阶段。没有因局部通过勾选 1.2/1.4/4.7/5.x/6.5/7.5，未同步主 spec 或归档。
- 实现 `assembly/plans/` 的 manifest、registry、索引一致性、seal CAS 和 storage facade；Saver 暴露 create/revise/read registration。create key 绑定精确初始 preimage（不只比较 plan_hash），相同 key 返回原持久 plan owner；修订使用显式 revision，不覆盖初始创建摘要。draft registry 不复制贡献正文、不分配 selection/detail/assembly ordinal；ToolSet 的 NULL draft binding 已有 schema4 DDL。新目录包含四段式 AGENTS；主线 AST 审查显示 assembly/plans 六个源码文件分别为 1/89/188/134/140/59 行，assembly/checkpoint/storage 直接源码文件为 10/19/20，无该范围内新增 >800 行源码或缺失 AGENTS。
- PARTIAL（schema4 激活前）：全局 `ROLLOUT_SCHEMA_VERSION` 仍为 3；上述 registry 的测试在各自新建隔离 SQLite 中显式安装未来 schema4 DDL。正式 3→4 升级、既有 sealed plan 的显式来源审计、三个生产 seal 入口注册接线、同一个 assembly commit 内最终 plan 绑定尚未完成，不得把 fixture 建表或独立事务 port 当作生产生命周期闭合。测试保留 TODO，后续改由真实升级入口建立 schema4。
- PASS（草稿 registry 最新专项）：`uv run pytest -q -n 0 tests/integration/backend/sessions/rollout_context/test_context_plan_registry.py --tb=short --maxfail=3`，退出 0，`27 passed in 9.61s`。覆盖 Saver create/read/revise、连接重开、创建冲突、revision CAS、索引丢失/篡改拒绝、写入失败回滚、SQL NULL CHECK、seal control、snapshot 缺失拒绝以及工具 binding/单 plan 不得第二次 seal 的事务 port。两个 plan 的同名贡献采用 plan-scoped 内存正文；sealed projection 不再读取当前 source cache，仍强制读取同 assembly detail。新增重复 JSON key 的规范 JCS 校验，并持久保存无正文 creation_json，所有 revision 都重新校验最初创建 preimage，修正审查发现的 revision>0 可篡改 creation_hash 漏洞。前一轮 23 passed/1 failed 是 fixture 缺少显式 source alias metadata，已补齐生产合同并复验，不修改 domain 的 contribution identity 规则。
- 实现严格失败迁移重试：保留 failed journal 原行，仅同 from/to/name 和最终 audit-bound SQL checksum 的显式 artifact retry 可以继续；未解决失败仍阻断普通 runtime。只有后续同合同 completed journal 才解决该失败，不将 failed 改写为 completed，不清除审计；无匹配失败的 recovery_required、script/name/version/status/state 篡改均拒绝。COMMIT 后仍保持 migrating，artifact 审计通过后才 active。
- PASS（主线严格重试/恢复专项）：`uv run pytest -q -n 4 --dist loadfile tests/integration/backend/sessions/rollout_context/test_schema_upgrade.py tests/integration/backend/sessions/rollout_context/test_schema_upgrade_entrypoint.py tests/integration/backend/sessions/test_rollout_commit_recovery.py --tb=short --maxfail=3`，退出 0，`55 passed in 14.54s`。包括连续两次 precommit 失败后成功、原 failed 行和已发布文件字节保留、普通读写隔离与所有上述拒绝路径。
- PASS（主线最新扩大组合）：`uv run pytest -q -n 4 --dist loadfile tests/integration/backend/sessions/test_rollout_commit_recovery.py tests/integration/backend/sessions/rollout_context/test_rollout_checkpoint_saver.py tests/integration/backend/sessions/rollout_context/test_turn_execution_recovery.py tests/integration/backend/sessions/rollout_context/test_context_dispatch.py tests/integration/backend/sessions/rollout_context/test_schema_upgrade.py tests/integration/backend/sessions/rollout_context/test_schema_upgrade_entrypoint.py tests/integration/backend/sessions/rollout_context/test_context_plan_registry.py tests/integration/backend/sessions/test_itemized_projection_contract.py tests/integration/backend/sessions/test_itemized_dispatch_contract.py tests/integration/backend/sessions/test_itemized_typed_detail_contract.py --tb=short --maxfail=3`，退出 0，`256 passed, 11 warnings in 69.53s`。warning 为既有 langchain-community deprecation 和 Provider usage 序列化；不代表真实外部 Provider 或 schema4 已通过。
- PASS（迁移 worker 全量最新回传）：`uv run pytest -q tests/integration/backend/sessions/rollout_context/test_schema_v3*.py --tb=short --junitxml=out/tests/integration/backend/sessions/rollout_context/test_schema_v3_artifact_upgrade/artifacts/final-junit.xml`，退出 0，`48 passed in 40.87s`，无排除/skip。覆盖随机密文的认证复用、真实进程退出、暂存/已发布文件篡改拒绝和 SQL 回滚后公共入口重试；此前 10 passed/8 failed 的 capability/failed-journal 依赖已闭合。旧不可逆 redacted marker 缺少可信类型仍明确拒绝并保留原件，不假造可恢复正文。
- PASS（protected full-copy 主线复验）：`uv run pytest -q -n 0 tests/integration/backend/sessions/test_rollout_fork_protected.py --maxfail=3 --tb=short --junitxml=out/tests/integration/backend/sessions/test_rollout_fork_protected/artifacts/main-protected-fork.xml`，退出 0，`26 passed in 13.16s`。源正文认证、目标独立 HMAC/AAD 重加密、target-local identity、私有 staging 安装和保留 lineage 已落地；optional 审计 header 可以显式 unavailable，但 included request source/required header 仍强制可用。
- PASS（protected capability/full-copy 最新主线组合）：`uv run pytest -q -n 4 --dist loadfile tests/integration/backend/sessions/test_rollout_fork_protected.py tests/integration/backend/sessions/rollout_context/test_prepared_detail_verification.py tests/integration/backend/sessions/rollout_context/test_protected_detail_upgrade.py --tb=short --maxfail=3`，退出 0，`140 passed in 15.93s`（fork 28、prepared 56、legacy capability 56）。新增两项反向测试证明 required header unavailable 和 included source 即使 required 标记被篡改也拒绝恢复/fork。prepared capability 校验 typed manifest/AAD/hash/length/session HMAC，认证后直接复用原随机密文，不重新加密、不写文件、不返回原文。不能以该组合替代 schema4 fork 和全部 5.x/7.5 门槛。
- REVIEW GATE（schema4 接线前）：只读审查复现 omitted ToolSet 与多 ToolSet 的 JSON 往返可能被当前 draft/sealed registry 比较错误拒绝，已交 projection owner 修复；其最终测试结果待回传。旧 assembly seal 的 ToolSet“缺行 INSERT/要求提前 assembly binding”和恢复中的 session 级 contribution lookup，必须在 schema4 激活时一并替换为 plan registry/同事务绑定，不能只调用新 port 继续沿用旧 authority。修订后的 creation preimage 问题已由上面的 27 项专项闭合。256 项扩大组合早于追加的重复 JSON key/creation 两项测试，后者以最新 registry 专项为准。
- PASS（业务/codec worker 最终组合）：`uv run pytest -n 4 --dist loadfile -q tests/unit/services/business/test_message_service_checkpoint.py tests/unit/services/business/test_reasoning_checkpoint_service.py tests/unit/services/orchestration tests/integration/backend/sessions/test_session_history_loading.py tests/integration/backend/sessions/test_checkpoint_message_groups.py --tb=short --disable-warnings`，退出 0，`230 passed in 212.38s`。此前真实 phase/source/parent 和 tool text/reasoning 丢失已修复，17 项 JSONL/group 覆盖同 commit/locator/重启/回滚；最后旧 carrier 断言按现行 spec 改为 reasoning_content，保留 exact 正文、顺序、agent-state 并增加无 tool calls 断言，非降低断言。额外 tool reasoning 分类定向复验 3 passed/4.19s。后续 orchestration 架构拆分仍需新快照复验。
- PASS（Web worker 真实浏览器 Integration）：`uv run pytest -q tests/integration/clients/web/itemized_context/test_itemized_context_browser.py --tb=short`，退出 0，`1 passed in 46.38s`。真实点击、分页、滚轮、manifest、重载与切换会话得到 9 selection/2 omitted/2 history/3 loss，14 次 context 请求均 200，Gateway 路由及 request_id 一致；普通聊天保留新 assistant 输出。模型为本地 HTTP 桩，严格分类 Integration。产物位于同名 out/tests 路径的 artifacts/browser-result.json、context-network.json、rich-loss.png 和 browser-trace.zip。API+query 专项 19 passed/48.79s，前端两文件 9 passed/19 assertions，Web build 退出 0；schema4 尚需复跑。
- PASS（OpenAPI 同步 worker）：`bun run gen:openapi` 退出 0；`uv run pytest -q tests/contracts/api/test_itemized_context_openapi_contract.py tests/contracts/api/test_sse_openapi_contract.py --tb=short` 为 10 passed/11.15s；`bun test tests/contracts/api/test_itemized_context_openapi_contract.test.ts` 为 15 passed/41 assertions；`bun run --cwd src/clients/web build` 退出 0（15.49s）。runtime/OpenAPI/TS 侧离线快照的 assembly/assemblies enum 一致，Proto 仍为公开 DTO 来源。默认 tsconfig 的两处既有 Array.at/ES2021 错误未改；显式 `--lib ES2022,DOM,DOM.Iterable` 类型检查通过，不等同原配置检查通过。统一生成包含当前工作树已有 config pending/events 变更，未重写其源配置。
- PASS（本轮静态/格式）：主线每批 Python 修改执行 Ruff 与 compileall，新 registry 文件执行 Ruff format；`git diff --check`、OpenSpec strict validate 均退出 0。仅保留正式测试路径内产物，没有创建临时诊断工作区、修改用户会话或提交 Git。
- PASS（审查修正后的最终主线组合）：`uv run pytest -q -n 4 --dist loadfile tests/integration/backend/sessions/test_rollout_commit_recovery.py tests/integration/backend/sessions/rollout_context/test_rollout_checkpoint_saver.py tests/integration/backend/sessions/rollout_context/test_turn_execution_recovery.py tests/integration/backend/sessions/rollout_context/test_context_dispatch.py tests/integration/backend/sessions/rollout_context/test_schema_upgrade.py tests/integration/backend/sessions/rollout_context/test_schema_upgrade_entrypoint.py tests/integration/backend/sessions/rollout_context/test_context_plan_registry.py tests/integration/backend/sessions/rollout_context/test_context_plan_registry_toolsets.py tests/integration/backend/sessions/rollout_context/test_context_plan_registry_privacy.py tests/integration/backend/sessions/test_itemized_projection_contract.py tests/integration/backend/sessions/test_itemized_dispatch_contract.py tests/integration/backend/sessions/test_itemized_typed_detail_contract.py --tb=short --maxfail=3`，退出 0，`371 passed, 11 warnings in 79.85s`。包含最新 registry 30、ToolSet 往返 31、privacy 78 项，以及新的 orchestration 导入/装配后的存储和投影回归；warning 仍为上述既有两类。旧 256 项快照之后的修改已被该组合重新覆盖，schema4 正式生命周期仍未激活。
- PASS（registry 隐私与数据库约束审查闭合）：write/restore 共用凭据拒绝校验，metadata/api_key、Authorization、Proxy-Authorization、X-API-Key/api-key、Cookie/Set-Cookie（含大小写/嵌套）必须由 producer 给出完整 domain redaction marker，拒绝原文且不静默改 hash；不把 JSON Schema 的同名参数误判为认证配置，未知敏感文本仍由 producer 显式分类。恢复同时拒绝 draft_json/creation_json 中 hash 正确的 inline body；ToolSet 新增 session/plan parent FK。privacy 独立 78 passed/24.36s，main registry 独立 30 passed/12.91s。ToolSet 按 tagged identity 校验、按 selection 保序，omitted 比较不读取工具定义；独立 31 passed/12.87s，worker 与 main registry 合跑 61 passed/22.11s。这些仍是显式 fixture schema4/真实 SQLite port 证据。
- PASS（orchestration 重构后 worker 全量）：上面的同一 230 项业务/group/orchestration 命令再次退出 0，`230 passed in 212.59s`。新增 execution_step 七模块、event_stream 六模块和四段 AGENTS，移除 step mixin/service 私有字段透传；Agent 构建集中到一个 factory。根目录直接源码 21→13，26 模块/39 条顶层依赖边无环、全部 ≤800 行；旧八个入口删除且不留 shim。主线对 app/tests/scripts 的 Python 旧 import 搜索无命中，scoped Ruff/compileall/diff check 通过。新后端的正式 browser/API 复跑已交 Web owner，结果待回传；不把本局部拆分自动视为整个 7.5-F 完成。
- PASS（orchestration 重构后的全新 Web/API 运行面）：`uv run pytest tests/integration/clients/web/itemized_context/test_itemized_context_browser.py tests/integration/backend/sessions/test_itemized_context_api_contract.py tests/unit/services/business/test_session_context_query_service.py -q --tb=short`，worker 回传退出 0，`20 passed, 3 warnings in 92.82s`（browser 1、HTTP API 11、query unit 8）。全新 backend/Gateway 的 14 次 context 请求均为 200、route/request_id 一致，真实选择/4 页分页/重载/会话隔离/active history 通过，9 selection/2 omission/3 loss 与零 page error；14 个测试数据库为 schema3/active。Web build 退出 0（14.88s），测试后 32800/32820/31000 端口均关闭，正式 artifacts/process-owner.json、browser-result.json、context-network.json 保留。仍使用本地 Provider HTTP 桩，属于 Integration，不证明 schema4 或真实外部 Provider。

### 2026-09-08 typed detail、草稿解析与 artifact 升级阶段历史证据

- 完成计数仍为 27/43；本节记录实现增量与限定验证，不替代未勾选任务的完整合同。下方 owner/schema2 阶段结果只代表对应历史快照。
- typed identity 已贯穿 `ContextRef.source_ref`、sealed selection、SQLite detail key/manifest、runtime detail store、GC 和 fork。`ContextRef` 不再携带最终 `detail_ref`；最终引用固定为 `DetailRef(session_id, thread_id, assembly_id, detail_id)`，普通 leaf 无 `.json` 后缀。seal 前统一核对 owner、source revision、逻辑 length、visibility、detail kind、retention、protection 与 availability；不一致不产生 assembly commit。 该记录中的旧实现证据不替代Section 8的thread-qualified迁移验收。
- PASS（主线领域/草稿/dispatch/升级/Turn 恢复组合）：`uv run pytest -q -n 4 --dist loadfile tests/unit/domain/itemized tests/unit/core/test_itemized_contract.py tests/integration/backend/sessions/rollout_context/test_context_dispatch.py tests/integration/backend/sessions/rollout_context/test_schema_upgrade.py tests/integration/backend/sessions/rollout_context/test_turn_execution_recovery.py --tb=short --maxfail=3`，退出码 0，`1143 passed in 112.69s`。覆盖新增 42 项严格 draft serde、禁止草稿 selection/final detail/跨 owner、same-assembly seal 的 selection 幂等冲突、final detail mapping 的 included request-only 限制。原扩大回归 `315 passed, 1 failed` 中把 `DetailRef` 强转字符串的旧恢复测试已修正，该文件随后 `43 passed in 14.72s`。以上通过快照早于正在实施的多 item codec port，不宣称其已经通过。
- 架构实施：原 `assembly_serialization.py` 的解析职责移入 `app/domain/itemized/serde/`，assembly/registry/draft 分别为 321/327/65 行，共用严格 parser，移除旧入口且补齐四段式 AGENTS。ToolSetRef 草稿明确序列化 `assembly_id=NULL`。这只完成草稿类型恢复，尚未实现完整 plan-scoped 持久 registry、create/seal 幂等键和 draft→sealed SQL 事务，因此 1.2/1.4 继续未勾选。
- PASS（主线公共非空升级入口首批）：`uv run pytest -q -n 0 tests/integration/backend/sessions/rollout_context/test_schema_upgrade_entrypoint.py --tb=short --maxfail=3`，退出码 0，`3 passed in 3.44s`。通过 Saver 显式入口完成 schema2 非空 public detail/assembly 升级，新进程恢复精确正文，canonical JSONL 与旧 detail 原件保持不变；重复升级只有一条 completed schema journal；COMMIT 前验证失败回滚为 schema2/recovery_required，无假 completed artifact audit；缺少 protected key 在发布前拒绝。该结果不证明 protected 全量 artifact 升级。
- 升级入口当前进一步接入 prepare/publish/verify-migrated/verify-committed/abort 与显式 audit resume。artifact SQL COMMIT 后保持 `database_state=migrating`，审计核验后才解除读写隔离为 active，避免 pending audit 期间新业务写入破坏恢复基线。新增两项真实 `os._exit` 测试覆盖 COMMIT 后 audit 前、audit 后激活前崩溃；该增量首次组合运行未执行到这两个断言：`uv run pytest -q -n 0 tests/integration/backend/sessions/rollout_context/test_schema_upgrade_entrypoint.py tests/integration/backend/sessions/rollout_context/test_schema_upgrade.py --tb=short --maxfail=3` 返回 `3 errors in 1.67s`，因 codec 已切换 `items_for_message` 而 persistence 仍调用 `item_for_message` 的实施过渡态。该失败保留为阶段性记录，接齐 caller 后的专项复验见下一条。非空 schema1 升级、旧 redacted-only marker 无可信 value_type 等输入仍明确拒绝并保留原件，不能当作完整迁移支持。
- PASS（主线升级隔离与崩溃恢复最新专项）：`uv run pytest -q -n 0 tests/integration/backend/sessions/rollout_context/test_schema_upgrade_entrypoint.py --tb=short --maxfail=1`，退出码 0，`6 passed in 7.48s`。两种真实 `os._exit` 均保留 schema3/migrating，拒绝普通 assembly 读取与新 Turn 写入；仅显式重试核验已有备份、SQL journal、新旧文件后写 completed audit 并激活。已发布 typed 文件被篡改时保持 migrating、没有 completed audit、canonical JSONL 不变。上述 3 setup error 的基础 port 过渡态在此专项已消失；业务语义组和完整 commit/checkpoint 组合仍需另外复验。
- PASS（多 item codec 接线后的主线 commit/checkpoint 恢复组合）：`uv run pytest -q -n 4 --dist loadfile tests/integration/backend/sessions/test_rollout_commit_recovery.py tests/integration/backend/sessions/rollout_context/test_rollout_checkpoint_saver.py tests/integration/backend/sessions/rollout_context/test_turn_execution_recovery.py tests/integration/backend/sessions/rollout_context/test_context_dispatch.py tests/integration/backend/sessions/rollout_context/test_schema_upgrade.py tests/integration/backend/sessions/rollout_context/test_schema_upgrade_entrypoint.py --tb=short --maxfail=3`，退出码 0，`145 passed, 1 warning in 24.94s`（既有 langchain-community deprecation）。先前 `143 passed, 2 failed` 的两个断言按实际 JSONL 迁移：每 Turn 的 tool-call 伴随文本现在是独立 assistant_output，同 message group、同 commit；messages 仍为 4 而 canonical items 为 5。测试明确断言 semantic 顺序、sequence、正文恢复与 rewind 后旧 JSONL 字节前缀完全不变、只追加一个新 output，不通过放宽行数或删除断言掩盖重复写入。独立 checkpoint 文件复验为 `40 passed, 1 warning in 21.26s`。
- REVIEW GATE：protected 非空升级发布后未 COMMIT 的重试需要专门证据。新 AES-GCM 密文使用随机 nonce，重新 prepare 可能与既有 staged 密文冲突；迁移 owner 正在验证并实现经过认证的暂存复用，不能使用固定 nonce，也不能用 public-only 重试通过代替 protected 证据。
- PASS（API worker 限定交付）：`uv run pytest -q tests/integration/backend/sessions/test_itemized_context_api_contract.py tests/integration/backend/sessions/test_itemized_typed_detail_contract.py tests/integration/backend/sessions/test_itemized_projection_contract.py tests/integration/backend/sessions/test_itemized_dispatch_contract.py tests/unit/services/business/test_session_context_query_service.py tests/unit/services/business/test_gateway_context_query_service.py tests/unit/services/infrastructure/test_gateway_session_context_client.py --tb=short`，worker 报告退出码 0，`113 passed in 130.23s`，12 条既有 usage serialization warning。真实 HTTP `/api/v1/context/read` 支持 `#assembly=<id>`/`view=assembly` 冻结 selection/omission/loss、分页与重启；普通聊天保持 active view。真实 Web UI 消费尚未完成，6.5 保持 PARTIAL。
- PASS（protected upgrade capability worker 限定交付）：`uv run pytest -q -n 0 tests/integration/backend/sessions/rollout_context/test_protected_detail_upgrade.py --maxfail=3`，worker 报告退出码 0，`56 passed in 12.35s`，零 xfail。可信历史旧 blob golden 验证、旧 AAD 一次认证、session digest 校验与新 typed manifest/密文准备已通过；不替代完整 SQLite artifact 发布/rollback 或 protected 跨 session fork。
- FAIL（业务复验，真实语义丢失）：`uv run pytest -q tests/unit/services/business/test_message_service_checkpoint.py tests/unit/services/business/test_reasoning_checkpoint_service.py tests/unit/services/orchestration tests/integration/backend/sessions/test_session_history_loading.py --tb=short --disable-warnings`，worker 报告退出码 1，`208 passed, 5 failed in 196.53s`。实际 JSONL 缺 phase/source/parent 语义及 tool-call 伴随 text/reasoning；已授权 codec port、checkpoint persistence、catalog grouped read 改为有序多 canonical item 同事务写入/恢复。不得通过降低业务断言、填塞 tool_call payload 或恢复单 item shim 消除失败。
- PASS（主线修改批次静态）：对 domain/serde、composer、storage schema upgrade/migrations、Saver 与新增/修改测试执行 `uv run ruff check`、`uv run python -m compileall -q`，退出码 0。正式测试产物保留在同名 `out/tests/`；没有修改用户会话、提交 Git、同步主 spec 或归档。

### 2026-09-08 owner/schema2 阶段历史证据

- PASS（本轮主线 owner/schema/dispatch 组合复验）：`uv run pytest -n 4 --dist loadfile tests/unit/domain/itemized tests/unit/core/test_itemized_contract.py tests/integration/backend/sessions/rollout_context/test_schema_upgrade.py tests/integration/backend/sessions/rollout_context/test_context_dispatch.py -q --disable-warnings --tb=short --maxfail=3`，退出 0，`973 passed in 113.80s`，零 xfail/skip。按文件调度解决下方默认 xdist 的正式 workspace 重建冲突；未改全局 harness、未放宽断言。该集合覆盖 domain owner 与 schema/真实 Saver 详情恢复，不替代 Provider HTTP、Web 产品和 fork 完整验收。
- PASS（本轮 history 测试入口迁移后的扩大回归）：`uv run pytest -q tests/integration/backend/sessions/test_rollout_commit_recovery.py tests/integration/backend/sessions/rollout_context --tb=short --maxfail=5`，退出 0，`137 passed, 1 warning in 55.25s`。下方阶段性结果的两项私有入口错误已解决；仍保留 >32 thinking blocks 与失败关闭 snapshot 的验证，不恢复旧 history facade。
- PASS（本轮 domain worker owner 合同交付）：`uv run pytest -n 4 tests/unit/domain/itemized tests/unit/core/test_itemized_contract.py -q --disable-warnings --tb=short`，worker 退出 0，`960 passed in 46.98s`，零 xfail。新增 85 项 session/plan 必填、跨 owner 拒绝、included/omitted 与往返恢复边界。主线另行执行两个 Bun verifier，分别通过 32 合法/11 拒绝/11 preimage 和 6 redaction golden；同时相关 Ruff 与 compileall 通过。typed detail/draft registry 仍未闭合，不等价于整个 1.4/4.4 完成。
- FAIL（本轮默认 xdist 混合集合调度）：domain 与两个正式 integration 文件按默认 `-n 4` 混跑，退出 2，`964 passed, 5 errors in 50.66s`；多个 worker 的 integration autouse fixture 同时重建同名正式 workspace，触发 `OSError: Directory not empty`。该结果不算通过；改用 `--dist loadfile` 将同一文件固定到同一 worker 复验，不修改全局 harness、不跳过用例。
- PASS（本轮显式 schema 升级）：`uv run pytest -q tests/integration/backend/sessions/rollout_context/test_schema_upgrade.py --tb=short --maxfail=3`，退出 0，`10 passed in 4.27s`。普通读路径拒绝 schema1；显式 `Saver.upgrade_rollout_schema` 执行 schema1→2，保留备份和原 canonical 字节，重建 contribution 的恰一个 hash token 约束。修复了升级写锁内获取独立读 snapshot 的自锁；SQL 脚本逐条执行、不能隐式 COMMIT 或逃离外层事务。独立进程在 DDL 完成后直接退出，验证原 schema/items/contributions 未变、无半成品表，随后可显式重试。尚未验证 Windows 进程/文件锁，不据此关闭所有迁移门槛。
- PASS（本轮恢复与一次性迁移组合）：`uv run pytest -q tests/integration/backend/sessions/test_rollout_commit_recovery.py tests/integration/backend/sessions/test_itemized_migration.py tests/integration/backend/sessions/test_itemized_migration_integrity.py tests/integration/backend/sessions/test_itemized_migration_semantics.py --tb=short --maxfail=3`，退出 0，`113 passed in 43.86s`。startup 不再尝试清空未安装的迁移 target；保留原 JSONL/SQLite 并拒绝普通读取。此结果不替代 full-copy 的 lineage/remap 验收。
- PASS（本轮 session owner 集成）：`uv run pytest -q tests/integration/backend/sessions/rollout_context/test_context_dispatch.py --tb=short --maxfail=3`，退出 0，`3 passed in 3.02s`。新增两个 session 使用相同 plan/source ID 的隔离用例，验证 canonical/request refs owner、plan manifest/hash、封存后新 Saver 从 detail 恢复的 native wire 只含本 session 正文；这是生产 Saver/SQLite/detail/native projector 集成，不是 Provider HTTP 或产品 E2E。
- PARTIAL（本轮引用身份与架构）：ContextRef 及 ToolSetRef 已强制 session_id，request-only ContextRef 强制 plan_id、canonical 禁止 plan_id；Plan/assembly/restore 校验引用 owner，omission 不能跳过 owner 检查。Saver composer 不再使用共享 `__default__` ledger，plan/hash/manifest 显式携带 owner。`request_plan.py` 从 793 行拆为 604 行，独立 `plan_hash.py` 为 206 行，不保留旧 hash 实现。完整 draft registry、typed detail ref/path、跨 session fork 与新的 domain golden 组合复验仍需闭合，1.2/1.4/4.4/4.7 不因此勾选。
- FAIL（本轮扩大 storage/checkpoint 集合的阶段性结果）：`uv run pytest -q tests/integration/backend/sessions/test_rollout_commit_recovery.py tests/integration/backend/sessions/rollout_context --tb=short --maxfail=5`，退出 1，`2 failed, 134 passed, 1 warning in 53.01s`。两项引用已被拆分的 `RolloutHistoryReader._thinking_blocks`/`_read_indexed_history_snapshot`；由 history owner 将测试迁往真实新入口，保持 >32 thinking blocks 与失败关闭 snapshot 的断言，不恢复 shim。该运行早于上面的 owner 隔离增量，尚未最终组合复验。
- PASS（storage/恢复/checkpoint/逻辑操作）：`uv run pytest -q tests/integration/backend/sessions/test_rollout_commit_recovery.py tests/integration/backend/sessions/test_rollout_context_semantics.py tests/integration/backend/sessions/rollout_context --maxfail=3 --tb=short`，退出码 0，`134 passed, 1 warning in 53.71s`。覆盖损坏 meta/commit/catalog 时先验证再截断、未提交尾部回收、拒绝自动回填 catalog、最小 catalog 允许无可选 projection、摘要不打开 JSONL、64 KiB 摘要边界、Turn/checkpoint 恢复、overlay 新请求/重启恢复、sealed source detail 的 retention 保护，以及 pruning 前后 canonical 字节/sequence/locator 不变。
- 已删除 `storage/column_migrations.py`、`storage/compat_migrations.py` 和启动 catalog/projection backfill；不再通过重新计算 commit 计数或补 source revision 隐藏损坏。`storage/catalog/integrity.py` 负责正文 manifest 校验，summary snapshot 保留 SQLite commit/catalog 校验但不扫描 JSONL 正文。删除旧 `compact_jsonl_offline` API/实现、物理回收 schema/journal 自动恢复和幂等 metadata 容错；历史非空 physical-compaction journal 只报 `recovery-required`，保留原文件供显式恢复。逻辑 compaction/rewind/pruning 继续存在。
- 当前目录归属：catalog/item/part/view membership/index 查询集中到 `storage/catalog/`，不保留旧 import shim；`assembly/detail_retention.py` 拥有 tombstone 与 sealed selection 引用保护。`test_turn_execution_recovery.py`、`test_rollout_checkpoint_saver.py`、`test_context_plan_detail_store.py` 已从 `tests/unit/core/` 迁入 `tests/integration/backend/sessions/rollout_context/`；新增目录均有四段式 AGENTS。此条是当前实施状态，不单独关闭 7.5-F。
- PASS（各修改批次静态检查）：对修改的 storage、assembly、checkpoint 与集成测试执行 `uv run ruff check` 和 `uv run python -m compileall -q`，退出码 0；目录移动后 import 排序由 Ruff 修正并复核。没有用该证据替代运行测试。
- PASS（GC 提交顺序与引用保护）：`uv run pytest -q tests/integration/backend/sessions/test_itemized_projection_contract.py tests/integration/backend/sessions/rollout_context/test_context_plan_detail_store.py tests/integration/backend/sessions/rollout_context/test_context_dispatch.py -k gc --tb=short`，退出码 0，`3 passed, 51 deselected in 1.98s`。证明删除失败前已提交双字段 tombstone、重启能重试删除、sealed selection 详情不能被过期候选误删；不替代 typed detail identity/全 retention owner 的 4.7 门槛。
- PASS（规划格式与补丁卫生）：`openspec validate add-itemized-rollout-context --type change --strict --no-interactive` 和 `git diff --check` 均退出 0；不代表实现完成。
- PASS（扩大 projection 矩阵的当前结果）：`uv run pytest -q tests/integration/backend/sessions/test_itemized_projection_contract.py --tb=short --maxfail=3`，退出码 0，`42 passed in 28.86s`。先前 `3 failed, 67 passed` 集合中的 omitted ToolSetRef restore/overlay binding 已由 domain owner 修复；随后暴露的 digest-only contribution SQLite 约束和 composition 混入明文问题也已修复。覆盖真实 Saver/SQLite/detail、独立进程恢复与三投影 helper，但 native 生产 dispatch/Web API history 尚未闭合，不据此勾选完整 6.1/6.5/6.6。
- FAIL（扩大既有历史 fixture 验收）：`uv run pytest -q tests/integration/backend/sessions/test_rollout_fork_modes.py tests/integration/backend/sessions/test_rollout_context_semantics.py tests/integration/backend/sessions/test_real_rollout_fixture.py tests/integration/backend/sessions/test_session_history_loading.py --maxfail=3 --tb=short`，退出码 1，`3 failed, 24 passed in 44.10s`。两个旧 fixture 直接把 v1 artifact 送入 v2 runtime，另一个测试未注入 MessageCodec；分别由 migration 与 business/orchestration owner 迁移测试入口，不恢复 v1 fallback 或 storage→mapping import。
- PARTIAL（domain worker 交付，尚未最终组合复核）：跨 Python/Bun golden 与 compatibility matrix 已补到 `tests/unit/domain/itemized/`、`tests/fixtures/itemized/`。worker 报告 `790 passed, 6 xfailed`，Bun 32 合法向量、11 拒绝向量和 9 preimage。6 个 xfail、敏感 hash marker 合同和 ContextRef owner identity 缺口未关闭；不能把 xfail 当作 1.4/4.4 完成证据。
- PARTIAL（business/orchestration worker 回传，尚待组合复验）：orchestration `164 passed`；message/reasoning business `21 passed, 5 failed`；session history 排除单独有界用例的运行 `10 passed, 2 failed, 1 deselected`。失败涉及 phase/source、tool reasoning/body、内部 reminder 可见性和定点工具查询误要求未加载 final；worker 继续修复 mapping/history owner。审计还发现 middleware 缺失 prepare 时的 Provider 旁路、Turn 可见性仍读取派生 wire role、若干 fork/history 大文件；不能据前述 storage/projection PASS 宣称 7.5-F 通过。
- 明确剩余：ContextRef 的 session/plan owner、typed detail ref 与精确 detail identity/path、完整 omission/loss 跨投影恢复、migration/full-copy 及最终架构规模审计。新 schema 已允许 contribution 采用 digest-only 并以 CHECK 强制恰一个 hash token；既有旧 v2 index 的约束仍需显式、版本化 schema migration，不得重新引入启动隐式回填。native 新投影入口的生产 dispatch 接线和真实 Web/API history 尚待验证。当前没有全产品/真实 Provider 新验收，也没有同步主 spec 或归档。

### 先前快照的历史台账

下列条目保留此前执行结果；其中“本轮”“最新”和旧模块路径只描述当时快照，不代表当前 checkout。现状以上面的日期化证据为准。

- FAIL/UNVERIFIED：`uv run pytest -q tests/integration/backend/sessions/test_rollout_context_semantics.py tests/integration/backend/sessions/test_rollout_fork_modes.py tests/unit/core/test_rollout_checkpoint_saver.py`，退出码 2；收集阶段仍引用已删除的旧 `app.core.rollout_*` 模块。旧测试未修改，失败不通过 v2-only 生产门槛，也不能据此恢复旧运行时路径。
- HISTORICAL FAIL（此前核心 E2E 回归）：指定 `basic-chat-tool-loop.e2e.mjs` 曾为 `1 failed in 78.47s`、Node returncode=`1`，产物为 `out/tests/e2e/clients/web/test_basic_chat_tool_loop/artifacts/basic-chat-tool-loop-result.json`；失败已通过生产 history projection 修复并由本轮官方 harness 重新验证。该历史记录保留用于审计，不改变其它任务结论。
- PASS（本轮限定静态检查）：`uv run ruff check app/domain/itemized app/services/infrastructure/rollout_context app/services/mapping/itemized && uv run python -m compileall -q app/domain/itemized app/services/infrastructure/rollout_context app/services/mapping/itemized`，退出码 0；覆盖 v2 domain、rollout context 和纯 mapping，不覆盖 tests，也不证明未完成的 fixture、恢复、跨 projector、migration 或架构拆分门槛。
- PASS（本轮生产收敛修正）：`uv run ruff check app/services/infrastructure/rollout_context/storage/service.py app/services/infrastructure/rollout_context/checkpoint/owner.py app/services/mapping/itemized && uv run python -m compileall -q app/services/infrastructure/rollout_context/storage/service.py app/services/infrastructure/rollout_context/checkpoint/owner.py app/services/mapping/itemized && git diff --check`，退出码 0。terminal convergence 现在只收敛同一 execution 的最新 model-call attempt，保留 retry attempt 的既有 `failed` outcome；provider ToolSetRef bridge 已从 mapping 移回唯一 infrastructure owner；activity/control outcome 的 unknown 命名已统一。该证据不关闭 3.5、4.4、6.1、6.5、6.6 或 7.5。
- FAIL/UNVERIFIED（相关 checkpoint persistence）：`uv run pytest -q tests/e2e/system/agent/storage/test_checkpoint_persistence.py`，退出码 1；生产 terminal convergence 冲突已消失，但旧测试随后读取 v1 `record["role"]`，对 v2 item envelope 抛出 `KeyError: 'role'`。不恢复旧 writer/compatibility shim，不修改测试；该结果不能证明 3.5/5.x/7.5 已完成。
- FAIL/UNVERIFIED（本轮全量相关既有测试集合）：`uv run pytest -q tests/e2e/system/agent/test_context_compaction.py tests/e2e/system/agent/storage/test_checkpoint_persistence.py tests/integration/backend/sessions/test_real_rollout_fixture.py tests/integration/backend/sessions/test_rollout_context_semantics.py tests/integration/backend/sessions/test_rollout_fork_modes.py tests/integration/backend/sessions/test_session_history_loading.py tests/unit/agents/providers/test_user_message_provider_projection.py tests/unit/core/test_checkpoint_config.py tests/unit/core/test_rollout_checkpoint_saver.py tests/unit/services/business/test_message_service_checkpoint.py tests/unit/services/business/test_reasoning_checkpoint_service.py tests/unit/services/business/test_system_reminder_checkpoint_service.py tests/unit/services/mapping/test_user_message_content_projection.py`，退出码 2；8 个模块在 collection 阶段仍导入已删除的 `app.core.rollout_*` 聚合入口。旧 compaction/fixture/saver/business 测试未修改，不能通过恢复长期 v1 shim 解决。
- PASS（仍可运行的相关既有测试）：`uv run pytest -q tests/unit/agents/providers/test_user_message_provider_projection.py tests/unit/core/test_checkpoint_config.py tests/unit/services/business/test_system_reminder_checkpoint_service.py tests/unit/services/mapping/test_user_message_content_projection.py`，`14 passed in 7.66s`，退出码 0；这只是 provider/配置/提醒/纯 mapping 子集证据。
- PASS（v2-only owner/import audit）：`uv run python` AST/import 审计确认生产代码无旧 `app.core.rollout_*`/itemized 聚合入口引用，v1 reader 仅由 `rollout_context/migration/` 导入，ToolSetRef bridge 只有 `app/services/infrastructure/rollout_context/provider/toolset_request_bridge.py` 一个实现，mapping/itemized 无 infrastructure/provider/SQLite/文件 I/O import；退出码 0。该证据不替代 A0–A6/A8–A11 及 7.5-F 的删除/分层验收。
- REVIEW GATE（`PARTIAL/UNVERIFIED`）：运行时 `app/domain/itemized/hashing.py` 已接入真正的 RFC 8785 serializer，且 JCS/ToolSet/selection/overlay/detail 的有限 smoke 已通过；4.4 仍未完成跨语言 golden vectors、非法数字/Unicode 边界的专门验收和完整恢复证明，因此保持未勾选。4.5 仍只表示基础 snapshot/provenance 字段；稳定 ordinal、统一 selection/order 的跨 LangChain/native Provider/history 验收仍未完成，不能据此勾选相关任务。
- KNOWN COMPATIBILITY MISMATCH（`PARTIAL/UNVERIFIED`）：`uv run pytest -q tests/unit/services/business/test_message_service_checkpoint.py tests/unit/services/business/test_reasoning_checkpoint_service.py --maxfail=3`，`2 failed, 24 passed`；两个失败都要求 final agent-state 只含 text，当前合同保留 reasoning+text carrier。未修改测试，未将其标记为通过，相关任务仍不得勾选。
- INTERRUPTED/UNVERIFIED（`PARTIAL`）：`uv run pytest -q tests/e2e/system/agent/test_context_compaction.py -x` 仅首个场景通过，随后因长时间无新输出触发 `KeyboardInterrupt`，退出码 130；`1 passed` 不代表 compaction change 通过，相关恢复/overlay 任务仍未完成。
- KNOWN UPSTREAM CONTRACT FAILURE（本轮最新 compaction overlay 报告）：`2 passed / 3 failed`；失败显示为旧 prompt-cache 上游契约，不作为 itemized context 的通过证据，也不改变本 change 的任务状态。
- PROJECTION REVIEW GATE（`PARTIAL/UNVERIFIED`）：当前生产 projector 已对缺失 canonical ref、source/hash/length mismatch、detail-unavailable 和 ToolSetRef availability fail-closed；本轮 storage/overlay/detail/provider smoke 已通过，但仍没有覆盖 LangChain restore、native Provider 与 Web history 三条路径的完整同一 selection/order/loss 证据，也没有完整 request-only wire/omission 端到端报告。因此 4.7、6.1、6.5、6.6 仍不得因核心 E2E 或局部 smoke 勾选。
- P1 DETAIL SECURITY FIX（生产修正，4.7 仍未完成）：`ContextPlanDetailStore.write(sensitive=True)` 已强制普通 JSON 仅保存 redacted/HMAC marker，增加敏感原文不得出现在待写字节中的安全后置检查；`_root`、`_target`、redaction-key parent 和 read path 逐级使用 `os.lstat`/no-follow 检查，并执行 realpath containment，读取最终文件使用 `O_NOFOLLOW`。隔离 smoke 已证明敏感原文不落盘且 `rollout` 父级 symlink 被拒绝，退出码 0；这不替代 protected backend、required/optional detail 全链路和跨 projector 验收，因此 4.7 保持未勾选。
- PASS（hash/ownership smoke）：`uv run ruff check app/domain/itemized app/services/infrastructure/rollout_context/runtime app/services/mapping/itemized && uv run python -m compileall -q app/domain/itemized app/services/infrastructure/rollout_context/runtime`，退出码 0；domain 统一持有 request hash，未发现 runtime/infrastructure/LangChain/I/O 反向依赖。该证据不关闭 4.4、6.1、6.5、6.6 或 A0-A11。
- PASS（最新核心回归）：`uv run pytest -q tests/e2e/clients/web/test_basic_chat_tool_loop.py`，退出码 0，`1 passed in 56.51s`；官方 pytest harness 内部 Node harness 退出码为 0。最新产物 session `ses_6c4eb780f8e04c3e930ebb894b068083` 含两轮 completed job、4 次 LLM request、预期 upstream role 序列，page/console/failed-request diagnostics 为空。该结果仍只覆盖核心 Web/tool-loop 链路。
- PASS（本轮 storage/fork/replay production audit）：隔离 workspace 的 `uv run python -` audit 在 storage acceptance checkpoint-origin 修正后退出码 0；`context_fork`、`history_prefix_fork`、`full_rollout_copy` 均完成 target-local mapping，full copy 包含 acceptance ingress/key mapping；`replay_as_new_turn` 首次创建新 Turn/root/acceptance/execution，重复 key 返回同一结果并标记幂等。该 audit 不是 5.6-5.8 的分层恢复测试，相关任务继续未勾选。
- ARCHITECTURE REVIEW/PARTIAL：当前 checkout 的 `wc -l` 为 `storage/service.py=792`、`assembly/store.py=135`、`checkpoint/saver.py=768`、`checkpoint/owner.py=643`；`app/domain/itemized/plans.py` 不存在。但 `storage/` 仍有 23 个直接 `.py` 文件，`service.py` 仍聚合连接/路径、schema/offset、快照/备份恢复和事务路由，Saver/owner 仍混合 checkpoint、Turn/execution、context、history 与 projection facade。该审计不把已有模块数量当作拆分完成证据，A0-A11/7.5-F 继续未完成。
- PASS（最新 OpenSpec）：`openspec validate add-itemized-rollout-context --type change --strict --no-interactive`，退出码 0；本轮未新增完成证据，按 1.2 的实现/恢复边界撤销不相称勾选，当前为 26/43。

当前勾选计数仍为46/66（不据部分实现勾选8.x/9.x）：Section 1–7的46项保留既有完成证据；Section 8的13项与Section 9的7项均未整体完成，但经只读审计（`out/tests/temp/openspec_8_9_gap_audit/artifacts/report.md`）确认并非“全部未实施”：Section 8已落地大量基础设施（8.1主体含权威/gate/resolver、8.1-B、8.1-C、8.5-A、8.8、8.8-A、8.3-C 已达到可验收深度），Section 9仍基本为零（仅9.1 domain冻结已落地）。旧核心E2E与strict validate只证明当时边界，不能覆盖SessionThread、federated Session协作、resource activation snapshot或扩展验收。

本change当前未完成且不得归档：Section 1–7的既有实现边界已闭合，Section 8–9仍无一项整体完成，整体仍为46/66。上一版台账把Section 8–9记为“0/20”属失真结论，已按上述审计证据纠正；勾选状态不变，纠正的是对现有实现深度的描述。另需注意两条同源失真的文档声明：`app/core/session_catalog_migration.py` 模块docstring声称“8.2-A 调试域步骤已作为同一journal phase接入”，但该文件中debug域命中为0，属虚假声明，须随8.2-A实现一并修正或删除。保留工作树中其它既有dirty改动；本次台账修订未同步主spec、未归档；后续历史失败/PARTIAL条目仅记录当时快照。
