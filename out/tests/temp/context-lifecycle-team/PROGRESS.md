# Context-Lifecycle 团队推进台账

- 目标：goal-bad828d2-9925-4fca-80f4-fc9228592e71（16 轮上限，已用满自然结束）→ goal-6d099dce-368f-423b-9967-ecd6c5abbfda（晚间会话 11 轮，R8 后误标 complete——用户质询"为什么停止"后确认目标本体是三份 OpenSpec 剩余改动，远未完成）→ goal-e2ce71e2-689b-46ba-a6cf-5528222288eb（深夜会话，R9 收口后经用户要求收口）→ goal-bdcf1036-632c-4c60-81b8-de373e668592（本 goal，40 轮上限）
- 交接文档：docs/plans/2026-09-15-agent-handoff-context-lifecycle.md；会话级交接：docs/plans/2026-09-15-session-continuation-handoff.md
- 团队约定：实施/审查 subagent 一律 provider=hyfsub2api（**2026-09-15 深夜更新：cyx 已不可用，用户指示改用 hyfsub2api**）；不继承主 agent。**模型实测（本会话开局）：hyfsub2api 目录仅 GLM-5.3-flash 可用，qwen3.8-flash / deepseek-v4.1-flash 均被拒绝（"child LLM route not allowed for this Session"）——回到单模型模式，审查与实施同模型，独立性靠流程保证（全新 agent + 磁盘/md5 裁定 + 变异抽查）；GLM-5.3-flash 空终止时同模型重试一次，再失败主 agent 接手并在报告如实标注。**
- 本 goal 轮次计划（按依赖顺序）：R9 8.4 GraphBinding 接线收尾（runtime 层透传+container 装配+集成用例，闭 R6a TODO 与审查 N6）→ R10+ 8.1 session-catalog.sqlite 基础设施（schema/NavigationTopologyGate/SessionLifecycleGate/fence/UUIDv4 validator/path 预算）→ 8.2 旧 JSON 索引一次性迁移 → 8.5-A ThreadCreationRecord → 8.5 child thread 完整实体与 delegate 改造 → ThreadResidency 真实 runtime 接入 → 2.4 CSM producer 缺口 → D1-fix 纵深防御。
- 审查规则：每轮实现完成后由未参与实现的独立 subagent（优先 qwen3.8-flash 以获得不同模型视角）审查 + 复跑定向测试，报告写 reviews/。
- 产物规则：所有报告/临时产物只写本目录；正式测试输出走 out/tests/<同名测试路径>/。

## 基线证据（2026-09-15 会话）

- `uv run pytest tests/unit/services/infrastructure/rollout_context/test_context_source_manager.py tests/unit/services/infrastructure/test_workspace_file_resources.py -q` → 9 passed
- `uv run pytest tests/unit/agents/test_skill_runtime.py tests/unit/agents/tools/test_session_history_tools.py -q` → 27 passed
- `uv run pytest tests/integration/gateway/test_gateway_workspace_routing.py -q`（清理残留状态后）→ 8 passed；未清理直接重跑 → 8 failed（跨运行状态泄漏，见 R1b）
- openspec CLI 不可用；全量 ruff 有约 780 个基线问题，禁止作为门槛。
- `uv run pytest tests/e2e/clients/web/test_basic_chat_tool_loop.py -q` → 1 passed in 77s（真实 Chromium 全链路，R7 扩展基线；Chromium 位于 /usr/bin/chromium）。

## 轮次状态

| 轮次 | 内容 | 状态 | 实施报告 | 审查报告 |
| --- | --- | --- | --- | --- |
| R1 | CSM 控制状态持久化与重启恢复 | ✅ 收口：实现→审查（有条件通过）→M1 修复→复核（通过）；47 passed | rounds/R1-impl.md | reviews/R1-review.md |
| R1b | Gateway 路由测试跨运行隔离修复 | ✅ 收口：审查通过（4 条非阻断建议已记录：报告补 3 处 hunk 归属、e2e Docker 用例与 AGENTS.md 张力根因=产品缺陷①、联邦用例依赖 force 重装副作用、reset_gateway_user_configuration 加 .resolve()） | rounds/R1-gateway-isolation.md | reviews/R1b-review.md |
| R3a | NodeDebugService thread 归属迁移 | ✅ 收口：实现→审查（有条件通过）→修复→复核（通过）；42+6 passed，ruff/compileall 绿 | rounds/R3a-impl.md | reviews/R3a-review.md |
| R3b | stopping 状态机 + launch_pending claim + reconcile_required（任务 3.2/3.5 服务层） | ✅ 收口：实现→审查（有条件通过）→F6/F7/F8 修复→复核（通过）；65 passed；复核残留（stop/restart 不取 owner 锁、spawn 前无 closing 守卫）已记录 | rounds/R3b-impl.md | reviews/R3b-review.md |
| R2 | 资源事件→CSM 事件驱动接线 | ✅ 收口：实现→审查（有条件通过：M1 溢出缺陷/M2 测试缺失/M3 报告失实）→修复→复核（通过）；77+20 passed；复核 3 条不阻断建议已记录（gap+已删除文件、gap 双重计数、sha256: 前缀合同显式化）；3.8/3.9 仅部分证据，未勾选 OpenSpec | rounds/R2-impl.md | reviews/R2-review.md |
| R4 | 16 个调试工具经 invoke_extension_tool 信封注册 | ✅ 收口：实现→审查（有条件通过：M1 step 释放并发缺陷/M2 报告勘误）→修复（精确释放+并发用例）→复核（通过）；192 单测+11 集成 passed；含主 agent 两处上游修复（StepRunner reactor owner key、claim 迟到错绑）；非阻断项（fail-closed 守卫无独立用例、release_matching 成 dead API 待 R8 清理）已记录 | rounds/R4-impl.md | reviews/R4-review.md |
| R5 | ThreadResidency / 30 分钟 idle fake clock | ✅ R5a 切片收口：typed node_debug_process lease 进唯一账本（握手成功 acquire、核实终态先 settle 后写 claim、reconcile_required 保持、恢复幂等；审查通过，变异独立复现）；30 分钟 idle blocker/fake clock 依赖 ThreadResidency 基础设施（代码留 TODO，报告如实记为未完成） | rounds/R5a-impl.md | reviews/R5a-review.md |
| R6 | Web 右侧侧边栏 child-thread 列表 | ✅ 本会话经 R6b（后端 API）+ R6c（Web 面板）交付（中间形态：delegated child session 即当前 child thread 物理形态；8.5 完整实体后可平滑升级） | rounds/R6b-impl.md、rounds/R6c-impl.md | reviews/R6b-review.md、reviews/R6c-review.md |
| R7 | 模拟用户前端操作 E2E 验收 | ✅ 本会话经 R7 交付（真实 Chromium + model stream replay 驱动真实 task 委派 → 面板断言 → 点击导航；两次通过覆盖产品两种请求形态；基线无回归） | rounds/R7-impl.md | reviews/R7-review.md |
| R8 | OpenSpec 收尾与交接文档更新 | ✅ 本会话收口（主 agent 亲自执行）：OpenSpec 任务勾选终核——三份均不满足全量勾选条件（各任务缺口在各轮报告"未完成事项"节如实记录），保持未勾选；会话级与技术交接文档均已更新至本会话末状态（剩余工作按依赖重排：8.1/8.2 → 8.5 → 8.4 接线 → ThreadResidency runtime 接入 → 2.4 producer → D1-fix 纵深防御）；D1-fix 非阻断 2 条处置：carrier_dedup fail-loud 留后续轮、E2E 第 4 interaction 保留（观察竞态复发） | docs/plans/2026-09-15-session-continuation-handoff.md、docs/plans/2026-09-15-agent-handoff-context-lifecycle.md | |
| R2b | EventChannelService 通用提取（OpenSpec 3.8-A/3.8-B/3.8-C 基础设施 + 3.9 可撤销 handle） | ✅ 收口：实施（GLM-5.3-Flash 完成代码后异常终止，主 agent 接手验证+报告）→ 审查有条件通过（M1/M2/M3）→ 主 agent 修复 → 复核（M2✅M3✅M1-R 残留）→ M1-R 修复 → 终验**通过**（探针 24 场景 22 走私全拒、38 passed、md5 零漂移）；584 passed 广回归绿；OpenSpec 3.8/3.9 未勾选（producer 缺口+2.4 依赖，如实记录） | rounds/R2b-impl.md | reviews/R2b-review.md |
| R3c | 旧 Session debug/node/ 目录显式迁移（任务 2.5 debug-domain 步骤） | ✅ 收口：实现（GLM-5.3-Flash）→ 审查**通过**（必改 0，非阻断建议 S1-S6：原子写 tmp 残留断言、伪新格式分支用例、already-new-format hash 清单、pending 字面量、noop 前解析、生产装配点）；10+33 passed，变异抽查 3 项（1 项测试缺口已记录：原子写降级需 fault injection 才能观察） | rounds/R3c-impl.md | reviews/R3c-review.md |
| R5b | ThreadResidency 基础设施（30 分钟 idle tracker/fake clock/blocker + R3b 残留收口） | ✅ 收口：实现→审查**通过**（必改 0，非阻断 8 条：N1 push 未知相位 release 方向、N2 sweep 整轮中止、N4 websockets.connect 无超时等，留 8.4/8.5 接入轮复查）；4 变异全检出还原；20/33/213/48 passed | rounds/R5b-impl.md | reviews/R5b-review.md |
| R6a | itemized 8.4 GraphBinding 基础设施（四元组 + factory registry + graph_binding_unavailable + 持久化） | ✅ 收口：实现→审查**有条件通过**（M1 报告失实：runtime 层 build_session_agent_runtime 缺 graph_binding_store 透传，参数链断链——主 agent 已更正报告 §5.1 措辞并修 N5 行号；代码本身无缺陷）；3 变异全检出还原；55 passed；非阻断 N1-N6（fsync TODO、tmp 并发锁、真实 builder 集成用例留接线轮） | rounds/R6a-impl.md | reviews/R6a-review.md |
| R6b | child-thread 列表后端 API（Web 右侧侧边栏数据面；中间形态：delegated child session 即当前 child thread 物理形态） | ✅ 收口：实现→审查**通过**（必改 0，非阻断 6 条：API 层 409 用例、降级路径不可达分支语义、latest_job_status 接线以 _list_loaded_jobs 为基础等）；4 变异全检出还原；9/222 passed | rounds/R6b-impl.md | reviews/R6b-review.md |
| R6c | Web 右侧侧边栏 child-thread 列表（「运行与连接」标签内 ChildThreadPanel + 5s 轮询 + openWorkspaceSession 导航） | ✅ 收口：实现→审查**通过**（必改 0，非阻断 5 条；1 测试缺口：useChildThreadLoader 无单测——竞态守卫直读确认正确）；3 变异 2 抓 1 缺口（M1 竞态守卫无测试覆盖）；build/tsc/9 pass/全量 431-21-1 四元组数字级吻合。**过程发现：bash 输出也会返回过期变体（md5 稳定）——后续审查须用 cp 副本重读+双提取互 diff+md5 快照裁定** | rounds/R6c-impl.md | reviews/R6c-review.md |
| R7 | child-thread 面板 Web E2E 验收（真实 Chromium + model stream replay 驱动真实 task 委派） | ✅ 收口：实现→审查**有条件通过**（M1 报告勘误 + M2 归因裁决——主 agent 已修：§1.2 补第 4 interaction 行、§2 改 4-interaction、§1.3 导航断言 0 用户气泡+45s 窗口、§5.5 归因改为 R6c 审查变异-还原周期的 mtime（内容 md5 已由该审查验证一致）、头部加日期约定；「7 月 28 日」磁盘不存在系审查通道过期变体）；复跑双形态各实测 1 次、基线无回归、3 变异按预期；非阻断 N1-N5 | rounds/R7-impl.md | reviews/R7-review.md |
| D1 | 产品缺陷排查：委派后下一次模型请求偶发缺尾部 tool 消息（R7 实测非确定性 + ~6.7s 延迟） | ✅ 排查收口（deepseek-v4.1-flash）：根因=装配/投影层双层去重叠加——①items.py `_select_existing_tool_result` 把带 projection_message_id 的 request 侧 carrier 等价复用成 live item（不产生新 commit）；②carrier_dedup 把 stream 组+唯一 live tool_result 判 shadow（假定 carrier 保留但已被①吞掉）→ 投影无 ToolMessage。竞态边距 0.16-0.27s。确定性复现+真实数据验证修复方案（~6 行：carrier 不被等价复用）。~6.7s 延迟=seal→派发 4.3-8.3s（awrap_model_call 同步 _prepare 阻塞事件循环，父/子 job 串行放大）。未改生产代码 | rounds/D1-investigation.md | |
| D1-fix | 修复 D1 缺陷（items.py carrier 不被等价复用 + 测试） | ✅ 收口：实施（GLM-5.3-Flash 完成核心后异常终止，主 agent 接手验证收尾+补报告 §2-§6）→ 审查（**审查 agent 连续 3 次异常终止后由主 agent 亲自执行**，归属如实标注：3 变异全检出还原——删 guard/条件取反/移位到等价校验前分别被 test 1/1/4 精确捕获；4/66/588 passed；md5 bfce552c 前后一致）；非阻断 2 条（carrier_dedup fail-loud 纵深防御、E2E 第 4 interaction 处置留 R8 决策） | rounds/D1-fix-impl.md | reviews/D1-fix-review.md |
| R9 | 8.4 GraphBinding 接线收尾（runtime 层透传 + container 装配 + 集成用例，闭 R6a M1/N6） | ✅ 收口：实施（subagent 空终止后主 agent 接手：三段透传 + `.boxteam/graph-bindings` 装配 + 9 用例含真实 builder load→resolve→重建 + _agent_cache TODO；9/57/495/213 passed，ruff/compileall 绿）→ 审查（审查 agent 空终止后主 agent 亲自执行：2 变异全检出还原，md5 前后一致；非阻断 2 条：store 单文件并发窗口、container 装配无直接单测）；P2 评估：capability hash 维持声明式常量（漂移×注册表单例冲突，正确口径依赖 blueprint/invocation 拆分） | rounds/R9-impl.md | reviews/R9-review.md |
| R10 | 8.1 第一片 session-catalog.sqlite 基础设施（nodes 表/验证器/gate/fence 原语/CTE 查询，只建不切权威） | ✅ 收口：实施（GLM-5.3-flash subagent 一次通过：862 行模块 + 1325 行 143 用例，143/255 passed、ruff/compileall 绿）→ 审查（GLM-5.3-flash 独立 subagent）**通过**（必改 0，非阻断 N1-N4：list_children 非 int limit 错误类型灰区、invalid_limit 用例未覆盖非 int、8.1-C 时拆 gate/fence 模块、只读 connection 措辞）；4 变异全检出还原（md5 一致）；红线全过（session_paths.py 未改、未装配、openspec 未勾选、tmp_path only）；DDL 与任务书逐字一致；8 项对抗性核读全过（UUIDv4 位 2000 样本实测、CTE 环注入安全、IS ? NULL 语义、fence CAS、UTC 日期、断言真实性、862 行判定无需拆分、错误分类） | rounds/R10-impl.md | reviews/R10-review.md |
| R11 | 8.2-切片1 旧 JSON index → SQLite 迁移机器（不切权威） | ✅ 收口：实施（GLM-5.3-flash subagent：958 行 SessionCatalogMigrator——预检（含 sessions_root.name=="sessions" 防旧布局扫描）/resolver 读旧权威/备份清单 sha256/拓扑序冻结映射+quarantine 级联（illegal_id/illegal_date/parent_quarantined）/原子 journal（tempfile+fsync+os.replace+目录 fsync）/gate 内 create-or-get 幂等重建+全量对账/备份复验 + 52 用例，52/307 passed）→ 审查（GLM-5.3-flash 独立 subagent）**有条件通过**（必改 M1：state/workspace_id 对账分支零测试覆盖；非阻断 N1-N5；9 变异 3 组检出、2 组实证报告预测不成立——gate 无 await 挂起点（migrate 全链路同步 IO）、对账分支零覆盖；两个设计决定均判定正确：completed 短路备份复验是任务书两要求的唯一同时满足解、sessions_root.name 预检必要（非该命名触发 resolver 旧布局扫描导入））→ 修复（GLM-5.3-flash subagent：M1 补 2 负向用例（workspace_id 用例选根级会话——verify_workspace_consistency 只查父子且跳根节点，根行仅由对账分支兜底）+ N1-N5 全处置，54/309 passed，变异自检 2/2 检出）→ 复核（主 agent：复跑 54/309/ruff/compileall 全绿、独立变异 2/2 精确检出（DID NOT RAISE）、md5 ca93b9e8/5089c2ec 一致、红线全过）**通过** | rounds/R11-impl.md | reviews/R11-review.md |
| R12 | 8.2-切片2 物理树迁移 + session-control.sqlite 初始化（不切权威、不装配） | ✅ 收口：实施（GLM-5.3-flash subagent：新 session_control_store.py 296 行（thread_catalog/lifecycle_fence DDL、幂等初始化、verify_matches_catalog_main_thread）+ 迁移机器扩展至 2110 行（journal v2 四态状态机、.staging 三态物理迁移、session.json 剥离三键、内容清单逐文件 sha256、quarantine session+folder 隔离 orphaned、合法 folder 删除、session-control 初始化、恢复语义 fail closed、备份复验三层分层）+ 106 用例（81 迁移+25 control），106/361 passed）→ 审查（GLM-5.3-flash 独立 subagent）**通过**（必改 0，非阻断 N1-N9；6/6 变异执行：M1-M3 高风险全检出精确、M4/M5 实证为已申报覆盖缺口（原版代码有 _sweep_staging/completed 短路兜底，仅缺直接回归用例，N7/N8 建议固化）、M6 语义收紧一致；两个关键实施决策均判定正确：清单排除 session-control.sqlite 是重入收敛必要条件、quarantine folder 隔离有 AGENTS.md orphaned 决定性依据；恢复语义 5+2 场景全 fail closed；既有用例 7 处调整全部必要无掩盖回归；2110 行无需拆分） | rounds/R12-impl.md | reviews/R12-review.md |
| R13 | 8.1-A SessionCreationRecord 新模型创建流（不切权威、不装配） | ✅ 收口：实施（GLM-5.3-flash subagent：store 加法扩展 862→1268 行（SCHEMA_VERSION=2、v1→v2 同事务加法升级、session_creation_records 表 DDL 逐字、create_or_get/get/publish CAS 单事务/abort 四方法）+ 新 session_creation.py 765 行（gate 内 create-or-get→staging 准备（剥离版 session.json+SessionControlStore main row/fence(active,1)+durability barrier）→原子 rename 日期桶→gate 内 CAS publish；恢复四崩溃点+fail closed 变体；CAS 失败回收=隔离 orphaned/session-creation/<key>；per-key asyncio.Lock 并发收敛）+ 测试 169+38 用例，207/425/106 passed）→ 审查（GLM-5.3-flash 独立 subagent）**通过**（必改 0，非阻断 N1-N6；5/5 变异执行全检出（publish CAS/幂等冲突/恢复 sha256/定点回收/user_version 升级），md5 前后一致；DDL 与任务书逐字一致；加法扩展经三重替代证据链确认（untracked 文件无法 git diff：nodes DDL 归一化 diff/R10·R12 基线交叉/143+106 既有用例零失败）；对抗核读 12 项 10 过 2 非阻断（N3 控制库 durability 依赖 WAL checkpoint-on-close、N4 idempotency_key 未收紧 uuid4 hex）；既有用例仅适配 2 处 user_version 断言（任务书声明项）） | rounds/R13-impl.md | reviews/R13-review.md |
| R14 | 8.1-B NavigationSubtreeDeleteRecord 删除流（不切权威、不装配） | ✅ 收口：实施（GLM-5.3-flash subagent：store 加法扩展 +707 行（subtree_delete_records 表含 drained_session_ids 进度列、create_or_get 冻结{node_id,revision}/mark CAS 单事务整树 deleting/record_drain_progress/finish 深度降序删行/abort 不回滚/get/delete_empty_folder 七方法）+ control store +50 行（cas_fence_transition）+ 新 session_subtree_delete.py 434 行（gate 内冻结+mark 唯一逻辑可见性关闭点→gate 外逐 session fence CAS(active,1)→(deleting,2)+rename .deleting/<key>/<session_id>/+durability barrier 先于进度记录→gate 内 finish；恢复三窗口定点继续）+ 测试 207+33+28 用例，235/499/152 passed）→ 审查（GLM-5.3-flash 独立 subagent）**有条件通过**（必改 M1：cas_fence_transition False 路径事务泄漏（BEGIN IMMEDIATE 内 return 无 ROLLBACK，同实例二次 CAS 抛 OperationalError、持 RESERVED 锁）；非阻断 N1-N9；8 变异全检出还原（mark CAS/drain fence CAS/finish 完整性/恢复分支全覆盖）；证据链升级逐字节 diff（/tmp 基线备份：control 仅+50 行、store 8 纯插入块、creation/migration md5 与 R13/R12 基线一致）；四个主要实现决策均判定成立）→ 修复（GLM-5.3-flash subagent：方案②先读态后写事务（False/KeyError 在写事务前返回/抛出，写事务内复核保 CAS 原子性）+2 回归用例，35/235/501 passed，变异自检 2 用例红）→ 复核（主 agent：md5 4ab0f7a7 一致、35 passed、修复代码核读通过、独立变异 2/2 精确检出（orig_method.txt 还原→2 用例 FAILED）、还原全绿、git 228 不变）**通过** | rounds/R14-impl.md | reviews/R14-review.md |
| R15 | 8.2-切片3a SessionCatalogPathResolver 新 resolver（SQLite 数据源，不装配） | ✅ 收口：实施（GLM-5.3-flash subagent：新 session_catalog_resolver.py 1218 行（35 公开成员实现旧 resolver 全部接口面：读 17+导航写 7+创建/删除适配 7+属性 4；nearest_session_ancestor 包装旧语义含自身（store 不含自身——错位会导致 SessionService.create 派生错误）；allocate/register 兼容层（allocate 走创建流 preparing 段不 publish+marker 兼容文件、register 剥离三键+title 一致校验+CAS publish、abandon 复用隔离+abort）；resolve_thread_node main 折叠+非 main 受检叶名查找；复用 R13/R14 非公开方法带 TODO(切换轮)）+ 测试 67 用例（含新旧 resolver 等价树语义对照 3 例、逻辑移动磁盘指纹断言），67/568 passed）→ 审查（GLM-5.3-flash 独立 subagent）**通过**（必改 0，非阻断 N1-N6；6/6 变异执行全检出（title 校验/nearest ancestor 含自身/目录 fail-closed/folder.path/abandon 隔离/begin kind 校验），md5 每轮回 5114e84c；异常观察③裁定：三文件 mtime 更新但 md5 与基线逐一相等=内容零漂移无害；决策 1 承重性经 SessionService.create:375 独立确认；frozen_session_locators 疑点排除（dict 键=session ID）；非公开方法 4 调用点签名一致+TODO 在位） | rounds/R15-impl.md | reviews/R15-review.md |
| R16 | 8.2-切片3b-1 装配切换第一段（开关式接入 + session_service 换源，默认仍旧 resolver） | ✅ 收口：实施（subagent：path_utils 开关式工厂（`BOXTEAM_SESSION_CATALOG_RESOLVER` 恰为 "1" 启用；SQLite→新链、旧 index 无 SQLite→RuntimeError 含迁移提示、全新→空 catalog；lru_cache 首构造后固定；workspace_id 与 container 同源；TODO(切换收口后删除)）+ session_service 换源（title/parent_session_id 从 resolver 投影读，模式无关；catalog 模式剥离 manifest 三键）+ container 零改动核实 + catalog_workspace_helper（R17 复用入口）+ 12/3/5 新用例，12/583/227/105 passed）→ 审查（独立 subagent）**有条件通过**（必改 M-1：开关严格比较无有效锁定（lru_cache 同根掩蔽）；M-2：parent 派生 folder 间接场景零锁定；非阻断 N1-N6；8 变异含全部高风险点，8/8 还原 md5 回基线；三个关键裁决均判定正确：A 剥离条件化（旧 resolver 物理校验强制 manifest 携 parent_session_id+既有测试断言、无条件剥离打掉 6 既有用例）、B 投影+内存父链游走保运行时容错契约、C title_source 缺口如实）→ 修复（subagent：M-1 7 分支独立 sessions_root 锁定 + M-2 3 场景（修正任务书字面错误：兄弟会话不是祖先）+ N1 绑定断言 + R16-impl 变异预测实证修订，12/583/230 passed）→ 复核（主 agent：md5 6954680d/07e5ad09 与审查基线一致、20 passed、**独立变异 a/b 均精确检出**（开关放宽→M-1 用例 FAILED；parent 直取→3 个 M-2 用例全 FAILED）、M-2 口径处理核验正确、git 235 不变）**通过** | rounds/R16-impl.md | reviews/R16-review.md |
| R17 | 8.2-切片3b-2 catalog 模式测试适配第一段（生产 honor canonical ID + 测试 ID 映射 + business/agents 双模式绿） | ✅ 收口：实施（subagent：生产侧 store `create_or_get_creation_record` 加可选 session_id（事务前 validate/同 key 冲突拒绝改绑/不传逐行等价）+ resolver `allocate_session_dir` honor canonical ID（非 canonical 抛 ValueError fail loud）+ 3 处最小生产适配（session_service updated_at 对齐+manifest 重写、navigation folder 投影 path=None、update_folder 纯改名走 update_node_name）；测试侧迭代式 ID canonicalize（全量映射表 + 22 文件替换 + 负向用例排除清单 + 双模式语义改写）；双模式 core+business+agents：两模式均 1313 passed/2 failed(环境性 rg+IPv6)/7 skipped，catalog 首轮 103 failed 清零）→ 审查（独立 subagent）**有条件通过**（必改 3：M4 workspace_id 前置校验零测试锁定（变异 0 失败——register 落盘前无条件规范化，前置检查仅诊断）；4 项映射派生来源与报告表述不符（md5 反算不匹配）；catalog business+agents 复跑实为 3 failed——R18-3 store 单连接线程安全 flake ≈1/5 命中，门槛「全绿」是概率性；6/6 变异执行全检出+md5 逐字节还原；负向用例保护到位、语义改写无放宽掩盖、冻结文件零触碰）→ 修复（subagent：补 2 个直接负向用例（mismatch 拒绝 + 缺键规范化）+ §2.1/§3/§5 如实更正 + R18-3 提为 R18 首项 + 台账复现链，75/591/591 passed）→ 复核（主 agent：md5 一致、75 passed、**独立变异双向检出**（禁用前置校验→mismatch 用例 FAILED；删规范化行→normalizes 用例 FAILED）、报告更正与台账落地抽查、git 246 不变）**通过** | rounds/R17-impl.md | reviews/R17-review.md |
| R18 | R18-3 store 跨线程事务交叠缺陷修复（最高优先）+ infrastructure catalog 适配第二段 | ✅ 收口：实施（subagent：store 单连接改造为 `threading.RLock` 串行化完整事务窗口（__init__ 锁+_initialize/connection 属性/close 包锁+两个事务 CM 整窗持锁），SQL/DDL/BEGIN 模式/错误语义零改动；确定性回归 3 用例（Event park 强制交叠，修复前 3/3 必现 FAIL、修复后 PASS）+ 独立复现脚本（stress 200 轮盘点完整缺陷族：BEGIN×99/COMMIT×32/ROLLBACK×5/DatabaseError×22/InterfaceError×3/游标污染×3）；infrastructure 74F+20E → 457 passed 全绿（catalog+旧模式双绿；66 项显式映射+6 f-string 显式化+3 模式分支+legacy inline 迁移引导+gateway 5 项 hermetic proxy 修复）；唯一生产行为面改动 llm_request_log_service.py 默认 resolver 对齐同族工厂；business+agents catalog 连续 3 轮 724 passed 无 flake）→ 审查（独立 subagent）**通过**（必改 0，非阻断 4：①test_llm_request_log_service 断言修正未申报且旧模式 infra 基线实为 456+1failed（"零回归"掩盖基线修正）；②映射台账口径（67 distinct vs "65+1"；"脚本内断言"实为审查独立证实）；③RLock 前提表述不实（现库无真实嵌套路径，RLock 属防御性；现有回归对 RLock→Lock 变异无检出力=盲区）；④小笔误。R18-3 决定性证据：R17 遗留快照全量 diff 仅 7 hunk=申报锁改造面，SQL/DDL 逐字节零改；7 禁改文件 md5 逐一一致；R18 窗口 mtime 扫描恰 14 文件=申报面零未申报；4/4 变异检出+md5 逐字节还原；business+agents catalog 5 轮无 flake）→ 复核（主 agent 轻量抽验：store md5 0d995693 与审查快照一致、210 passed、infra 旧模式 457 passed 全绿）**通过** | rounds/R18-impl.md | reviews/R18-review.md |
| R19 | 8.2 收口：默认切换新 SQLite authority + 迁移 operator 入口 + 全量双模式验证 + OpenSpec 评估 | ✅ 收口：实施（subagent：①默认切换——`BOXTEAM_SESSION_CATALOG_RESOLVER` 语义反转为「默认 catalog，仅 `"0"`/`"legacy"` 显式 legacy opt-in」，探测逻辑/container 零改动，TODO(8.5 收口后移除)；②迁移入口——`migrate_workspace_session_catalog` async 函数 + `scripts/migrate_session_catalog.py` runner（identity 一致性预检/JSON 输出/失败非零）+ 7 用例与真实 CLI 三路径冒烟；③R18 非阻断全处置——RLock 嵌套重入用例（RLock→Lock 变异唯一检出）、R18-impl 5 处更正、基线 456+1 补记、签名注解收紧；④全量双模式：默认 catalog 全量 4F/3645P（3 项适配缺口当场修复复验 + rg 环境）、legacy 1F/3648P（rg）；hermetic proxy 修复 gateway 6 + web_tools 1 + integration conftest session 级（integration ERROR 噪声归零）；⑤OpenSpec 8.2 **不勾选**（缺旧附件迁移/digest/owner ref（依赖 8.3）、8.2-A 接线、quiesce gate 生产装配））→ 审查（独立 subagent）**有条件通过**（必改 M-1：§4「ERROR 929→0」口径不自洽（无留档、模式归属矛盾、不在 §5 清单）——仅报告准确性，非代码缺陷；非阻断 N1-N5（用例计数 7→6、runner 冒烟永真断言、入口 docstring 对错误 workspace_id 的 fail-closed 表述过强、三次运行无留档、既有债务）；B 组 5 组独立复跑全绿（开关+path_utils 41/store 211/core 603/legacy core+business 833/gateway+web_tools 301，补齐实施自报的 gateway 整树缺口）；变异 5/5 检出+md5 还原；红线全过（6 红线文件 md5 一致、container 零改动、OpenSpec 保持未勾选）；§5 根因独立坐实「非 canonical ID fail-loud 为 catalog delta 主体」）→ 修复（主 agent 直接处置：M-1 报告更正；N1 计数；N2 恒真断言改真实 catalog 读取；N3 入口新增 identity 一致性 fail-closed 预检 + docstring 更正 + 新负向用例；N4 留档纪律；88 passed/ruff/compileall 绿）→ 复核（主 agent：报告更正抽查、迁移测试 88 passed、开关 41 passed、**修复后默认模式全量单测 1F/3648P（唯一=rg 环境）**留档）**通过** | rounds/R19-impl.md | reviews/R19-review.md |
| R20 | 8.5-A ThreadCreationRecord child thread 创建机器 | ✅ 收口：实现→独立复核通过（必改 0；108/677 passed，ruff/compileall 绿；两个非必改建议已记录）：实施（subagent：`session_control_store.py` SCHEMA 1→2 原地升级 + thread_catalog kind 加 child + `thread_creation_records`/`thread_execution_intents` 表 + 9 方法；新 `thread_creation.py` ThreadCreationService——gate 内冻结 record+artifact manifest→gate 外 staging+fsync barrier→原子 rename→gate 内 CAS publish→admission intent 幂等；恢复 6 崩溃点）→ 审查（独立 subagent）**有条件通过**（必改 M-1 `_publish_in_gate` 缺 catalog active fresh 复验；M-2 publish 单事务原子性零测试保护 + aborted delegation 不换绑无直接测试；非阻断 N-1~N-7；红线全过、6 变异 4 检出+md5 还原）→ 修复（subagent：`thread_creation.py:1030-1063` gate 内 publish 前补 `get_node`+active 校验、`session_control_store.py` 零改动、+4 用例）→ **独立复核通过（必改 0）**：M-1 三方向变异全检出（永假/移到 publish 之后/移到 gate 之外），M-2 拆裂变异检出；定向 108 passed、`tests/unit/core` 677 passed、ruff/compileall 绿（`reviews/r20fixrev-*.log`）；`session_control_store.py` md5 与修复前基线逐位相同=零改动属实。**遗留两个非必改建议**（见 `reviews/R20-fix-review.md` §3）；**8.5 后续切片依赖见`rounds/R20-dependency-audit.md`**（含 2 处实测不兼容：child `graph_binding.graph_revision` 类型、`capability_profile` 平面标量口径）| rounds/R20-impl.md、rounds/R20-task-brief.md、rounds/R20-fix-impl.md、rounds/R20-fix-task-brief.md、rounds/R20-dependency-audit.md | reviews/R20-review.md、reviews/R20-fix-review.md |
| R21 | catalog resolver child thread 权威定位 | ✅ 收口：实施主体经用户提交 23f6860 进入主干；收尾（deepseek agent）完成最终双模式验证：catalog 10F/1149P/0E（迁移前基线 215F/52P/892E，A181+B16+C15=212 项全部收敛、892E 清零）、legacy 9F/1150P/0E（R19 基线 10F：零新增、fork_reader_locks 债务转绿）；7F 归因为既有深层债务（三重证据：R19 legacy 基线早列同 7 nodeid、双模式复跑同 7F、代码级根因 remap_finalize.py:161 full-copy 物化 item 的 projection_message_id 为 null——迁移让 setup 成功后缺口显形），移交 fork remap 专项轮；R21-impl.md 曾被截断至 82 行，已按磁盘快照恢复全文+收尾章节（391 行）。派生发现：custom_tool_test_workspace fixture 需现代化（布局已迁日期桶+catalog 未提交；rollout 仍 v1 格式，web 集成读取报 schema-upgrade-required，移交后续轮） | rounds/R21-impl.md、rounds/R21-finish-task-brief.md | reviews/R22-review.md（含 R21 文件面注记） |
| R22 | B/C 类 canonical 化（B 类剩余 3 文件 + C 类 f-string 随机非法前缀） | ✅ 收口：实施（12 文件：8 个 C 类 f-string 全部改 f"ses_{uuid4().hex}"；fork_modes workspace_id 改走 load_or_create_workspace_id；terminal_convergence canonical+子进程绝对路径；typed_detail 三处重启改走 saver._storage.sessions_dir；负向冲突注入值 4 项保留；test_context_plan_imported 无需改动预测正确）→ 审查（独立 subagent）**通过（必改 0）**：定向 319 passed 真实、ruff 绿、变异 2/2 精确检出还原（fork_modes 回退→16F、terminal_convergence 回退→11E）。审查发现提交态 7 个确定性回归（7a50fef 引入，非 R22 面）：6F langchain.py E1 收紧未同步 fixture、1F composition.py source_ordinal typed 字段冲突豁免缺失（生产 bug）——移交 R24 finalize 处置。注：r22-integration-legacy.log 曾被后续运行覆盖（19F/1102P/38E 与报告记载不符），产物完整性问题如实记录 | rounds/R22-impl.md、rounds/R22-task-brief.md | reviews/R22-review.md |
| R24 | thread_creation 与 GraphBinding 实测不兼容修复（graph_revision int + capability_profile 平面标量 + hash 交叉校验）+ 提交态回归修复 | ✅ 收口：实施（subagent：graph_revision 收紧为 int（对齐真实 GraphBinding，不保留字符串双轨）；capability_profile 经注入的 compute_capability_profile_hash 按平面标量口径校验并交叉校验 hash（core 不反向 import agents，装配侧注入，零复制）；make_graph_binding 夹具同步；110 定向/1180 unit/ruff/compileall 绿）→ finalize（subagent：修复 7a50fef 引入的 4 个提交态回归——①legacy 模式后端启动崩溃（OwnerThreadCreationFactory 构造期 fail-closed 移至唯一入口 _require_catalog_resolver）；②plan-order-integrity 误拒（E1 projector 接线回退至父提交）；③composition.py source_ordinal 身份冲突误报（M-A 生产 bug，两侧剥离 registry 分配状态+幂等重注册回归用例）；④real_rollout_fixture legacy 门控（按环境开关）；全量 unit 4173P/0F、sessions legacy 9F/1150P/0E（基线一致）、catalog 10F/1146P（既有债务），ruff/compileall 绿；净 -26 行） | rounds/R24-impl.md、rounds/R24-finalize-impl.md、rounds/R24-task-brief.md | reviews/R24-finalize-review.md（独立审查通过：必改 0；3 变异全检出还原、langchain 回退与父提交逐字节一致、legacy 启动经真实后端用例证实；2 条非阻断记录 + E1 落地后需回收 resolve_source_wire_role 休眠面） |
| R23 | 8.5-B intent 消费状态机（SCHEMA 2→3 + 确定性 identity + claim/mark-bound CAS + 可注入 binder worker） | ✅ 收口：实施随 7a50fef 提交（报告 rounds/R23-impl.md，台账此前漏记）→ 独立验证（subagent r23_intent）**通过**：五个验证面逐项属实；HEAD 复跑 131/239/773 passed、ruff/compileall 绿；两处任务书偏差均显式声明为推迟（lifespan 接线与真实 Job 留 R25/R26）；3 条非阻断观察项记录于 reviews/R23-review.md §8 | rounds/R23-impl.md、rounds/R23-task-brief.md | reviews/R23-review.md |
| R25 | 8.5-C delegate 改造（delegate 走 ThreadCreationService + delegation_id + collaboration ledger/CAS） | ✅ 主体已随 7a50fef 实现并提交（台账此前漏记）→ 状态审计（subagent r21_finish_flash，rounds/R25-state-audit.md）：5 项中 4 项实测在线——delegate 走 OwnerThreadCreationFactory.for_owner_session（delegation_id 四元组 sha256 确定性派生）、collaboration 表 schema v7、revision CAS 链完整、旧路径已物理移除（create_delegated/set_delegation_start_result/SessionDelegationDTO 仅存于注释）；定向 143 passed。**遗留 2 项**：①catalog_precondition_revision 仍为行计数（TODO(8.5)，行数今日单调，待删除/移动流落地时换计数表）；②CAS 2/CAS 3 不对称——同批并发 task 委派经 ToolNode gather 并发时 CAS 3 fail-closed 且同 key 不可重试、无并发测试覆盖，R26 前需决策（放宽 CAS 3 对齐 CAS 2 先例或新 key 重试 + 补并发用例） | rounds/R25-state-audit.md | （随实现提交于 7a50fef） |

## 决策记录

- 2026-09-15 03:2x：R1b 发现产品缺陷（按红线未修，留待产品决策）：① GatewayStateStore.replace_workspace_registry scoped save 不更新 remote_gateway_connections，新远程连接不落盘、投影孤儿化触发 fail-closed；② tests/e2e/system/gateway/test_gateway_browser_resource.py 本地用例既有失败——普通重启后 browser manager PID 不复用，与产品受控交接语义（_should_preserve_gateway_generation_for_handoff）不一致，需产品决策。

- 2026-09-15：Gateway registry 对"远程投影工作区引用未知 Gateway 连接"的 fail-closed 是正确产品行为，R1b 只修测试隔离，不改产品。
- 2026-09-15：OpenSpec 勾选只在代码+测试证据齐全时进行；strict CLI 不可用时保留未验证状态。
- 2026-09-15 02:5x：主 agent 修复存量缺陷——`EventType.DEBUG_ACTION`/`DEBUG_STOP` 事件类型缺失（agent_debug_service 引用但 job_event_bus/EventType、事件工厂注册表、TraceEventDTO Literal、TraceEventMapper 均未支持）。触及 app/core/job_event_bus.py、app/schemas/event/__init__.py、app/schemas/internal_v2/trace.py、app/services/mapping/trace_event_mapper.py。验证：test_agent_debug_service 2 passed（原 2 failed）；trace 相关 50 passed；ruff 触及文件仅剩 1 个存量 FURB162。
- 2026-09-15 02:4x：完整 tests/unit 基线：3 failed, 2892 passed, 7 skipped（15:23）。3 个存量失败：① test_codex_read_tool rg 缺失（环境问题，未安装 ripgrep，按 AGENTS.md 环境问题跳过原则留待用户决定）；②③ test_agent_debug_service 两个 EventType.DEBUG_ACTION 失败（已由主 agent 修复）。
- 2026-09-15 12:0x：会话收尾全量基线（R5a 后）：**1 failed, 2997 passed, 7 skipped**（18:23）。唯一失败仍为 test_codex_read_tool rg 环境问题；相对会话起点净增 105 个通过测试、修复 2 个存量失败。集成基线：test_debug_tools 10 passed、test_debug_prompt_flow 1 passed（连跑 5 次稳定）。
- 2026-09-15 16:3x（晚间会话收尾）：**会话总结**——11 轮全部闭环（R2b/R3c/R5b/R6a/R6b/R6c/R7/D1/D1-fix + R8 收尾），每轮"实现→独立审查→修复→复核"。最终基线：广回归 588 passed（584 基线 + D1-fix 4）；orchestration 213；agents 486+1 环境（rg）；E2E child-thread 面板 1 passed + 基线 1 passed。OpenSpec 任务终核：三份均不满足全量勾选条件，保持未勾选（缺口如实记录在各轮报告）。产品缺陷修复 1 项（D1-fix：委派后装配层吞 carrier）。provider 变更：subagent 仅 GLM-5.3-Flash 可用（审查与实施同模型）；GLM-5.3-Flash 本会话异常终止 4 次（R2b 实施者、D1-fix 实施者、D1-fix 审查者×2），均按"边做边写报告+主 agent 接手"模式收口，质量未降（变异抽查全检出）。
- 2026-09-15 16:3x：D1-fix 非阻断 2 条处置决策（R8）：①carrier_dedup fail-loud 纵深防御留后续轮（第一层修复后理论不可达，非紧急）；②E2E cassette 第 4 interaction（竞态形态）**保留**——修复后理论不再命中（request_reusable 不要求全部命中，无害），保留可观察竞态是否复发，复发即说明第一层修复不完整。
- 2026-09-15 16:3x：通道污染实录汇总（多轮审查证实）：read 工具与 bash 输出均会返回「过期变体」（与磁盘不符、md5 稳定）；git status 出现幽灵条目；job_output 串流。裁定方法：cp 副本重读 + awk/sed 双提取互 diff + md5 前后快照。详见 R6c/R7 审查报告 §0。
- 2026-09-15 深夜（goal-e2ce71e2 会话收尾）：R9 收口（8.4 GraphBinding 接线：runtime/执行服务/container 三段透传 + 9 用例 + 2 变异验证，主 agent 实施与审查——subagent 双双空终止）。R10（8.1 第一片）两次委派均死于起步（无报告无代码），未实施——任务书要点已写入会话交接文档 §4.1。provider 后端切回 hyfsub2api（harness 名仍 cyx），3 模型恢复可用（qwen3.8-flash/deepseek-v4.1-flash/GLM-5.3-Flash），审查与实施恢复用不同模型。GLM-5.3-Flash 单模型期间共 7 次空终止实录已写入交接文档 §2。会话经用户要求收口，交接文档与新 goal 文本已备（docs/plans/2026-09-15-session-continuation-handoff.md §6）。

- 2026-09-15 深夜（goal-bdcf1036 会话开局）：新会话按交接文档 §6 文本创建 goal（用户更正 provider=hyfsub2api，cyx 已不可用）。模型实测：hyfsub2api 目录仅 GLM-5.3-flash（探针返回"可用"），qwen3.8-flash / deepseek-v4.1-flash 被会话拒绝——交接文档 §2 的"3 模型恢复"预期落空，回到单模型模式（同上团队约定行）。R10 任务书全文写入 rounds/R10-task-brief.md（在交接文档 §4.1 要点基础上补齐：folder 节点 ID 用 ses_ 前缀、locator 日期==created_at UTC 日期校验、user_version=1、分页游标语义、fence 状态闭集与转移规则）。

- 2026-09-16（R10 收口 + provider 再变更）：R10 一次通过闭环（实施 subagent 143 用例全绿 → 独立审查 subagent 通过：必改 0、非阻断 N1-N4、4 变异全检出还原、DDL 逐字一致、8 项对抗性核读全过）——本 goal 首个全程 subagent 闭环（无空终止、无主 agent 接手）。用户指示 goal provider 改为 kunlunmetahyfnewapi（主 agent 模型路由已切 kunlunmetahyfnewapi/GLM-5.3）；实测 subagent 路由：kunlunmetahyfnewapi 被会话拒绝（"not allowed for this Session"），子代理允许列表仍仅 hyfsub2api——待用户确认处置。

- 2026-09-16（会话收尾）：用户指示"生成交接文档我创建个新会话继续"。会话级交接文档已写：docs/plans/2026-09-16-session-continuation-handoff.md（含新会话 goal 文本 §6、8.2 分片建议 §4.1、provider 实测步骤 §2）；技术交接文档已更新至 R10 末状态（§3.8 新增 R10 节、§4 验证证据、§5 风险清单、§7 结论）。本会话总结：R10 一轮闭环（8.1 第一片基础设施 143 用例），provider 三次变更实录（cyx→hyfsub2api→kunlunmetahyfnewapi），git 219 文件（217 存量 + 2 新增）。goal-bdcf1036 按用户指示暂停，新会话按交接文档 §6 重建 goal。

- 2026-09-16（新会话开局，goal-890d7c2e）：按交接文档 §6 文本创建 goal（40 轮上限）。provider 实测：list_subagent_models 允许列表仅 kunlunmetahyfnewapi（其下仅 GLM-5.3-Flash），探针 subagent 回复"可用"——与上一会话末相反（当时 kunlunmetahyfnewapi 被拒、hyfsub2api 可用），路由已变更，回到单模型模式（审查与实施同模型，独立性靠流程保证：全新 agent + 磁盘/md5 裁定 + 变异抽查）。R11 = 8.2-切片1 迁移机器（任务书 rounds/R11-task-brief.md：复用 SessionPathResolver 做旧权威读取与物理树对账、迁移机器只加新 profile 校验、main_thread_id 由迁移机器分配并 journal 冻结、create-or-get 幂等重建、备份=hash 清单+迁移后复验；不切权威、不动 R10 基线、不做物理树迁移/session-control.sqlite/附件迁移/8.2-A）。实施 subagent 已启动。

- 2026-09-16（R11 收口）：8.2-切片1 迁移机器闭环（实施→审查有条件通过 M1+N1-N5→修复→主 agent 复核通过）——本 goal 首轮，全程 subagent 闭环（实施/审查/修复均 subagent 一次完成，无空终止；复核由主 agent 执行）。54/309 passed、ruff/compileall 绿。关键发现：①migrate() 全链路同步 IO 无 await 挂起点，gate 在同循环内冗余（防御性保留，docstring 已写明语义边界）；②workspace_id 对账用例必须篡改根级会话（verify_workspace_consistency 只查父子同 workspace 且跳过根节点）；③created_at 缺失/无法解析在 resolver 加载层已 fail closed，迁移机 illegal_date quarantine 仅覆盖 naive 场景（口径已记录）。8.2 剩余分片决策：切片2 = 物理树迁移（Session staging 到日期桶 + Folder 删物理目录）+ session-control.sqlite thread catalog/fence 初始化（迁移机器扩展，journal 贯穿，fixture 测试，不装配）；切片3 = 权威切换与装配（resolver 数据源 + container + Gateway export + 回归）。附件迁移依赖 8.3 attachment catalog（未实施），8.2 内如实记录为遗留、8.3 后补。

- 2026-09-16（R12 收口）：8.2-切片2 物理树迁移闭环（实施→审查通过必改 0）——全程 subagent 闭环。106/361 passed、ruff/compileall 绿。关键决策（审查确认）：①内容清单排除 session-control.sqlite(+WAL 边车)——迁移新建产物不属 canonical bytes，不排除则 placed 重采集恒失败（重入收敛必要条件）；②quarantine folder 走 orphaned 隔离而非删除（app/core/AGENTS.md「保留可诊断信息」决定性依据，删除将烧毁 manifest）；③备份复验三层分层（物理未开始=完整 index+manifests；已开始=index-only；终验=新位置 sha+清单+隔离/删除布局+session-control 只读复验）。已知覆盖缺口 2 个（M4 staging 残留、M5 fence 篡改——原版代码均有兜底防御，仅缺直接回归用例，N7/N8 建议固化留后续）。遗留：act-then-journal 崩溃窗口重入 fail closed 需人工核账（任务书语义）；迁移完成后至切片3 之间旧 resolver 不可用（旧 index 退役直接后果）；父会话目录空 children/ 残留（8.5 统一清理）；rollout 内部 thread 化明确不做（main thread node==session node 折叠保持，8.5 升级）。下一步 R13 = 8.2-切片3 权威切换与装配。

- 2026-09-16（R13 收口）：8.1-A SessionCreationRecord 创建流闭环（实施→审查通过必改 0）——全程 subagent 闭环。207/425/106 passed、ruff/compileall 绿。切换前置缺口补齐：新模型创建流（record 表+staging+CAS publish+四崩溃点恢复）落地，store 升 schema v2（v1→v2 同事务加法升级）。关键选型（审查确认）：CAS 失败回收=隔离 orphaned/session-creation/<key>/（record 已 aborted 同 key 重试必拒、staging 区干净、审计保留）。审查发现 git untracked 文件无法 git diff 的证据链替代方案（DDL 归一化 diff+基线交叉+既有用例零失败三重链）——后续轮审查沿用。非阻断 N1-N6 留后续（N3 控制库 durability 依赖 WAL checkpoint-on-close、N4 idempotency_key 未收紧 uuid4 hex、N5 key 锁字典无淘汰）。8.2 剩余=切片3 权威切换与装配（新 resolver 读 SQLite+创建走 8.1-A+container/path_utils 切换+Gateway export+回归）——依赖已齐（R10 store/R11+R12 迁移机器/R13 创建流）。

- 2026-09-16（R14 收口）：8.1-B NavigationSubtreeDeleteRecord 删除流闭环（实施→审查有条件通过 M1→修复→主 agent 复核通过）——全程 subagent 闭环（复核主 agent）。235/499/152→修复后 35/235/501 passed、ruff/compileall 绿。切换前置缺口全部补齐（R10 store/R11+R12 迁移机器/R13 创建流/R14 删除流）。M1 教训：cas_fence_transition False 路径 BEGIN IMMEDIATE 内 return 事务泄漏——审查以「同实例二次 CAS」实证捕获（现有用例每次新建 store 未触发）；修复方案②（先读态后写事务）从结构消灭泄漏。审查证据链再升级：利用 /tmp 基线备份做逐字节 diff（R12/R13 审查遗留备份复用）。四个实现决策（冻结{node_id,revision}/空子树 deleting→finish/barrier 前移/preimage 幂等不重查）均审查确认。遗留（8.1-B 尾句外）：guard/lease 收敛（8.1-C/8.8）、跨进程锁（8.1-C）、retention 预检（8.1-D）、.deleting/ GC、abort 后纳管入口。下一步 R15 = 8.2-切片3a 新 resolver（SQLite 数据源，26 方法接口面，读/导航写走 store、创建走 R13、删除走 R14，不装配）。

- 2026-09-16（R15 收口）：8.2-切片3a 新 resolver 闭环（实施→审查通过必改 0）——全程 subagent 闭环。67/568 passed、ruff/compileall 绿。SessionCatalogPathResolver（1218 行/35 公开成员）落地，旧 resolver 全接口面覆盖。关键决策（审查确认）：①nearest_session_ancestor 包装旧语义含自身（store 不含自身——错位会导致 SessionService.create:375 派生错误，承重性独立确认）；②allocate/register 兼容层（allocate 走创建流 preparing 段不 publish+marker 兼容文件、register 剥离三键+title 一致校验+CAS publish——「register 时可见」契约保持）；③复用 R13/R14 非公开方法（_prepare_staging 等 4 个，带 TODO(切换轮) 提升为公开 API）。异常观察③裁定：三文件 mtime 会话内更新但 md5 与基线逐一相等=内容零漂移无害（touch/同内容重写）。切换轮适配清单（审查 N3 补全）：create_folder 形参移除（folder_id/created_at）、move_node/relocate_* 错误类 ValueError→RuntimeError、child_session_summary 条目顺序索引序→node_id 序且签名升级（list[str]→SessionChildSummary，调用方取 .session_id）、resolver 直连 store 未走 gate（切换轮明确 gate 纪律归属）、register 在 publish 前 unlink marker（N5 取舍）。8.2 剩余=切片3b 装配切换（path_utils/container 切新 resolver+未迁移工作区 fail closed+session_service 换源+Gateway export+回归）。

- 2026-09-16（R16 收口）：8.2-切片3b-1 装配切换第一段闭环（实施→审查有条件通过 M-1/M-2→修复→主 agent 复核通过）。主 agent 模型已切 cline-pass/deepseek-v4.1-flash；子代理路由实测未变（kunlunmetahyfnewapi/GLM-5.3-Flash）。交付：path_utils 开关式工厂 + session_service 换源 + container 零改动 + catalog_workspace_helper（R17 复用入口）。三个关键裁决（审查确认）：A 剥离口径 catalog 模式条件化而非无条件（旧 resolver 物理校验强制 manifest 携 parent_session_id——session_paths.py:527/786；既有测试断言 manifest title——test_session_service.py:75；无条件剥离打掉 6 既有用例）；B get() 派生走 list_authoritative_nodes+内存父链游走而非 get_node/nearest_session_ancestor（后者在旧 resolver 会因无关物理漂移抛错，破坏 resolve_session_node_for_runtime 容错契约）；C 新模式 title_source 回退 "default" 为已知缺口（catalog 无该列）。审查变异实证两处零锁定：M-1 开关严格比较（lru_cache 同根掩蔽——12 用例全逃逸）、M-2 parent 派生 folder 间接场景（business 227+core 15 全逃逸）——修复后主 agent 独立变异 a/b 均精确检出。M-2 任务书字面缺陷教训：「folderA 内兄弟会话成为深层会话 parent」错误（派生只沿父链游走，兄弟不是祖先）——修复按正确语义落地+补第 3 场景。三个后续轮次计划：R17 测试适配（30+ 文件 catalog 模式，工作量待实测评估）、R18 默认切新+删开关+全量收口（8.2 完整收口）。

- 2026-09-16（R17 审查处置）：**R18-3 store 单连接线程安全 flake 定性为生产隐患 + 默认切新前置阻塞项，R18 必须优先修复（先于其它 R18 适配项）**。实证（R17 审查独立复现 + 处置方复核）：① 复现链——`session_catalog_store.py` 单 `self._connection`（`check_same_thread=False`、无锁）被 `_write_transaction`（BEGIN IMMEDIATE）与 `_read_transaction`（BEGIN DEFERRED）共享；`message_stream_store._write_state_snapshot` 经 `asyncio.to_thread` → `_state_path` → `resolve_session_node_for_runtime` → `store.get_node` → `_read_transaction` 在工作线程开读事务，与主线程事务交叠 → `sqlite3.OperationalError`（读侧 `cannot start a transaction within a transaction`；COMMIT 侧变体 `cannot commit - no transaction is active`）；② 频次——审查单文件 5 连跑 4 passed / 1 failed（≈1/5，高于 impl 自报 ≈1/7），且已在 R17 门槛复跑（catalog business+agents）中命中一次（723 passed / 3 failed 的第 3 个失败），R17-impl §3「全绿」结论已降级；③ 双重定性——既是真实生产隐患（生产中 MessageStreamStore 快照写与其它 store 事务同样并发，流式写入/创建会间歇性失败；fail loud 不腐蚀数据），也是测试门槛稳定性威胁（catalog 模式 business+agents「全绿」因它概率性失守，R18 每轮验证都在掷骰子）；④ 修法建议——resolver 读路径独立只读连接或串行化锁（需改 `session_catalog_store.py`，超 R17 §2.1-A 红线，故 R17 不修）。R18 在该项修复前完成的其它适配，其复跑结论必须如实标注该 flake。

- 2026-09-17（R18 收口）：R18-3 store 跨线程事务交叠缺陷修复 + infrastructure catalog 适配第二段，审查**通过（必改 0）**。关键：R18-3 是 R17 审查发现的生产隐患 + 默认切新阻塞项——单连接无锁导致 `OperationalError` 家族（BEGIN/COMMIT/ROLLBACK 变体 + 游标污染），修复为 RLock 串行化完整事务窗口（确定性回归 3 用例修复前必现失败）。infrastructure 74F+20E → 457 passed 双模式全绿，business+agents catalog 连续 3 轮 724 passed 无 flake（R18 前约 1/5）。**基线修正记录（R18 审查非阻断 1）**：`test_llm_request_log_service.py` 在 R18 前旧模式 infra 基线实为 456 passed + 1 failed（ValueError→TypeError 断言与 HEAD 合同不符），R18 顺手修正未申报——"旧模式零回归"表述掩盖了该基线修正，此处如实补记。**转入 R19 的非阻断清单**：①补记基线修正；②映射台账口径更正（67 distinct）；③补 RLock 嵌套重入用例（RLock→Lock 变异盲区）；④小笔误。8.2 状态：迁移机器+创建流+删除流+新 resolver+装配切换机制+测试适配（core/business/agents/infrastructure 双模式全绿）均已完成；剩余 = R19 默认切新（开关由"默认旧"改为"默认新"）+ 全量单测在 catalog 模式收口 + OpenSpec 8.2 证据评估。

- 2026-09-17（R19 开局）：8.2 收口轮启动（任务书 `rounds/R19-task-brief.md`）：默认切换（`BOXTEAM_SESSION_CATALOG_RESOLVER` 语义反转——默认 catalog 权威，`"0"`/`"legacy"` 显式 legacy opt-in，唯一有意默认行为变更）+ 迁移 operator 入口（模块级 `migrate_workspace_session_catalog` + `scripts/migrate_session_catalog.py`）+ R18 非阻断处置 + 全量双模式收口验证 + OpenSpec 8.2 证据评估。**R18 非阻断处置已完成（R19-impl §3 与 rounds/R18-impl.md 就地更正同步落地）**：①基线修正补记——`test_llm_request_log_service.py` ValueError→TypeError 断言修正与「R18 前旧模式 infra 基线实为 456 passed + 1 failed」已写入 R18-impl 新增 §2.5 与 R19-impl；②映射台账口径更正——实际 67 distinct 源字面量（非"65+1"），"脚本内断言"实为审查独立计算证实，已在 R18-impl §2.2 就地更正；③RLock 嵌套重入用例已补（`test_same_thread_reentrant_lock_within_write_transaction`，非阻塞重入探针设计——避免 Lock 变异下死锁线程拖死 fixture teardown；实测 RLock→Lock 变异 md5 `4da35ccb…` 与 R18 审查 M2 同一变异下新用例 1 failed 干净检出、R18 既有 3 回归用例仍 3 passed，盲区与检出力同时实证）；④小笔误更正——§2.3 计数 10→11、M1 预测变体应为 BEGIN 侧、llm_request_log_service 签名注解 `Any | None` 收紧为工厂联合类型（`SessionPathResolver | SessionCatalogPathResolver | None`）。


- 2026-09-17（R19 收口，8.2 切片闭环）：默认切换新 SQLite authority 完成——`BOXTEAM_SESSION_CATALOG_RESOLVER` 语义反转为「默认 catalog，`"0"`/`"legacy"` 显式 opt-in」；迁移 operator 入口（`migrate_workspace_session_catalog` + `scripts/migrate_session_catalog.py`）可运行；R18 非阻断全处置。**验证基线（修复后）**：默认模式全量 `tests/unit` = 1 failed / 3648 passed / 7 skipped（唯一失败 = rg 缺失环境项，留档 `artifacts/r19-unit-default-full.log`）；legacy 显式模式 1F/3648P（同 rg）；hermetic proxy 修复消除 gateway 6 + web_tools 1 + integration 环境噪声。**8.2 OpenSpec 不勾选**（缺口：旧附件迁移与 digest/owner ref 校验依赖 8.3；8.2-A debug-domain 静态接线未做；quiesce gate 仅单人操作约定、生产装配未做）。**R20 起纪律**：全量/legacy 运行一律 tee 留档；integration 非 canonical ID 适配清单（R19 §5）为下一优先批次。用户 A 方案（完整收口 8.2 权威切换）已达成：迁移机器（R11/R12）+ 创建流（R13）+ 删除流（R14）+ 新 resolver（R15）+ 装配切换（R16）+ 测试适配（R17/R18）+ 默认切新与收口（R19）。


- 2026-09-17（会话交接停止，R20 未收口）：用户要求写交接报告并停止工作。**R20（8.5-A ThreadCreationRecord）已实施未收口**——生产代码在工作树上（`thread_creation.py` md5 2f572c288f50f133de5dbde6e44cc26a、`session_control_store.py` md5 02da3b43415fd57049d7faaeeaa6290a），104 定向 passed / core 673 passed，但审查**有条件通过且必改 2 项未处置**：M-1（publish 阶段缺 workspace catalog active fresh 复验 → 真实发布竞态窗口，推荐本轮补数行校验 + 删除双顺序负向用例）、M-2（publish 单事务原子性零测试保护 + aborted delegation 不换绑无测试；须更正 §10 预测并补测试），另有非阻断 N-1~N-7（含报告 3 处残留待填节、行数误差、published 重入静默重建建议改 fail closed）。**新会话最高优先 = R20 收口**（详见 docs/plans/2026-09-17-session-continuation-handoff.md §3）。本会话（goal-890d7c2e）成果：R11-R19 九轮闭环，**8.2 权威切换全链路完成并按用户 A 方案默认切新**（默认 catalog，`"0"`/`"legacy"` 显式 legacy opt-in；迁移 operator 入口 `scripts/migrate_session_catalog.py`）；默认模式全量 `tests/unit` 基线 1F/3648P/7S（唯一失败=rg 缺失环境项，留档 artifacts/r19-unit-default-full.log）；git 266 条目（有意 dirty，未清理）；OpenSpec 三份仍全部未勾选。provider 实测全程单模型 `kunlunmetahyfnewapi/GLM-5.3-Flash`（hyfsub2api 不在允许列表），主 agent 模型会话中途被切 cline-pass/deepseek-v4.1-flash、子代理路由未变。
- 2026-09-19（R21/R22/R24 收口）：R21 生产变更经用户提交 23f6860 进主干；R22 审查通过（必改 0）但其发现的 7 个提交态回归（7a50fef 引入）由 R24 finalize 修复（E1 projector 回退决策：R30 审计的 E1 切片目标为 assembly 层不含 projector，且 serde 对旧 envelope 默认 tail_only 会翻转历史 system 内容，违反旧 sealed plan 投影零回归）。subagent 路由 2026-09-19 起多次出现 unreadable_encrypted_agent_task 基础设施错误（模型切换后加密任务与 provider 不兼容），deepseek/GLM-5.3-Flash 多次失败后由主 agent + GLM-5.3 agent 接续完成，归属如实标注。**新登记后续轮**：①静态 fixture 工作区现代化（custom_tool_test_workspace 布局已迁日期桶+catalog 未提交，rollout 仍 v1 格式——需区分 v1 只读源（migration machinery 测试消费）与 v2 runtime 消费两类会话，经 legacy_import_v1_to_v2 或按当前 writer 重新生成）；②fork full-copy remap 7F 既有债务（message↔canonical item 对应缺口，与 ID 形态无关）；③R24 finalize 改动面（7 文件+1 新测试）待独立审查。
- 2026-09-19（边界提交制启动）：按用户指令「每完成一个边界就提交」，本轮交付 8 个提交——337e070（R24 finalize 回归修复）+ 5c6a827（channel_events 接受 jcs 前缀 sha256）+ ca86b0b（seal preflight stream carrier 配对消歧）+ acd4755（session_service 剥离形态 manifest 重写）+ d08101f（debug schema public_v2→internal_v2）+ 0193b3f（gateway 测试状态隔离与配置刷新）+ a24be7d（browser 测试 activePageId 对齐）+ fc7ce28（台账）。重要发现：①R21 第一轮曾把 custom_tool_test_workspace 模板搬进日期桶并残留旧 JSON 索引（模板内部不一致致 test_real_rollout_fixture 3E），已回滚——模板保持旧布局作为 migration machinery 测试的 v1 源，web 测试 catalog 化应在测试副本上做（R25a）；②R23 已在 7a50fef 实现并提交（SCHEMA 2→3 + initial_execution_binding_worker，报告 rounds/R23-impl.md），台账此前漏记，独立验证中；③browser discovery 测试为真实 LLM 依赖 + trace 尾部窗口分页（tool_call_start 实测第 63 事件/共 470，超出首页窗口），测试需翻页，登记 R25a。subagent 通道部分恢复：spawn 加密消息到 newapi-local 线程呈现为 [encrypted content omitted]，绕过方式=任务书写盘 + followup 指路径；deepseek 503 CPU 过载为瞬态。
