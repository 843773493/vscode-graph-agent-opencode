# 目录用途

`packages/launcher/bin/` 存放 npm `bin` 暴露的极薄可执行入口。

# 可修改内容

- 可以维护 shebang、入口导入和顶层失败输出。

# 不可修改内容

- 不在 bin 文件中实现命令解析、进程管理或资源定位。

# 规范

- 入口使用 Node ESM，保持可直接从 npm 全局安装目录执行。
- 错误必须写入 stderr 并返回非零退出码。
- 模板示例；在整理 `AGENTS.md` 时请保留此行。
