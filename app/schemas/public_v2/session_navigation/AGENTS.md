# 目录用途

定义工作区会话目录、文件夹、搜索和会话生成执行的公共 DTO。

# 可修改内容

- 会话目录节点、分页、breadcrumb、搜索和文件夹操作协议。
- 工作区 Session Generator 执行请求与结果协议。

# 不可修改内容

- 不实现目录扫描、存储或生成执行业务。
- 不定义 Gateway 全局工作区文件夹配置。

# 规范

- 跨工作区引用必须同时携带 `workspace_id` 和 `session_id`。
- DTO 使用显式字段和 Literal，禁止以裸字典代替稳定协议。
