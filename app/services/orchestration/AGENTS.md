# 目录用途

`app/services/orchestration/` 放流程编排与运行控制相关实现，负责串联业务服务、控制自动继续、调度执行流程。

## 可修改内容

- 可以新增、修改、删除编排服务实现文件。
- 可以维护 job / session / agent 之间的执行顺序与控制逻辑。

## 不可修改内容

- 不要把纯业务状态计算放进这里。
- 不要把基础设施实现细节放进这里。
- 不要把映射转换逻辑放进这里。

## 规范

- 以流程控制为中心，不承担数据持久化职责。
- 保持装配边界清晰，依赖协议而不是具体实现。
- 失败必须立即暴露，不能默认继续。
