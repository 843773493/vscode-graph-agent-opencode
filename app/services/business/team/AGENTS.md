# 目录用途

存放团队协作领域服务，负责团队成员关系、任务面板权限、任务状态流转和跨会话任务分派。

# 可修改内容

- 团队成员和任务的业务规则。
- 团队任务分派、成员激活与结果通知流程。

# 不可修改内容

- 不直接实现文件持久化细节。
- 不定义 Agent 工具 schema 或 FastAPI 路由。

# 规范

- 团队成员关系独立于 Session 父子树；手动加入的会话不得伪造 delegated 来源。
- 只有协调者能增加成员和分派任务；普通成员只能更新分配给自己的任务。
- 所有跨会话激活和通知必须通过显式编排接口，失败必须写入任务或成员状态并抛错。
