# 目录用途

本目录维护 Job 状态规则、单会话调度队列和待处理请求业务状态。

# 可修改内容

- Job 状态模型与调度规则。
- Steering、Queued、取消和立即发送的业务语义。
- 待处理请求的持久化协调逻辑。

# 不可修改内容

- 不在此处实现 HTTP API、页面状态或 Gateway 路由。
- 不直接依赖 infrastructure 层的具体实现。

# 规范

- 同一会话最多只能有一个 active Job。
- 持久化能力通过 abstractions 协议注入。
- 所有队列状态变更必须在调度锁内完成。
