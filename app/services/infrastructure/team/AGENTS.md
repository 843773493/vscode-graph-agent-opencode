# 目录用途

存放工作区团队任务面板的持久化实现，负责 `.boxteam/teams/` 下团队快照和事件日志的原子读写。

# 可修改内容

- 团队快照、事件日志的文件存储。
- 团队存储路径校验和序列化。

# 不可修改内容

- 不实现成员权限、任务状态流转或 Agent 调度规则。
- 不调用模型、SessionOrchestrator 或 Agent 工具。

# 规范

- 团队数据只能写入显式工作区的 `.boxteam/teams/`。
- 快照写入必须使用同目录临时文件原子替换。
- 文件损坏或 ID 非法时直接抛错，不得跳过或返回空对象。
