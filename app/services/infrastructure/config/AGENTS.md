# 目录用途

`app/services/infrastructure/config/` 存放配置基础设施的聚焦组件，包括不可变快照和文件变化监听。

# 可修改内容

- 配置快照、fingerprint 与来源元数据。
- 配置文件目录监听和变化筛选。

# 不可修改内容

- 不在本目录实现 Agent、MCP 或 Gateway 的业务编排。
- 不直接修改运行中的应用组件。

# 规范

- 监听器只报告变化，候选配置的校验与提交由调用方负责。
- 配置加载失败必须向上抛出，不能吞掉异常或伪造成功状态。
