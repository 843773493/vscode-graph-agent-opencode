# 目录用途

`app/services/mapping/` 放纯转换逻辑，负责在不同 DTO、事件模型和内部结构之间做映射，不承载业务决策。

## 可修改内容

- 可以新增、修改、删除映射函数文件。
- 可以整理事件转换、观测数据转换、响应组装等纯映射逻辑。

## 不可修改内容

- 不要在这里写业务判断、流程编排或 I/O。
- 不要在这里直接访问容器、数据库或外部系统。

## 规范

- 保持函数尽量纯粹、无副作用。
- 映射失败要显式暴露，不要静默丢字段。
- 命名要能直接体现输入与输出的语义。
