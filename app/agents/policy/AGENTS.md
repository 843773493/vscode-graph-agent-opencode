# 目录用途

存放 Agent 运行时使用的纯策略计算，例如工具可用性解析和工具依赖约束。

# 可修改内容

- 无 I/O 的 Agent 策略数据结构、集合运算和校验函数。
- 可由配置、业务服务和运行时共同复用的权威策略 API。

# 不可修改内容

- 不在这里读取配置文件、构建 LangChain 工具或执行工具。
- 不在这里实现 API、会话业务或前端展示。

# 规范

- 策略函数必须保持纯粹、确定且可单独测试。
- 无效策略必须抛出包含冲突工具名称的明确错误。
