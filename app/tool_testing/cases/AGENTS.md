# 目录用途

`app/tool_testing/cases/` 存放各个工具独立的模型调用测试用例。

# 可修改内容

- 可以新增实现 `ToolTestCase` 协议的工具用例。
- 可以维护用例输入、工具构建和结果断言。

# 不可修改内容

- 不在用例中调度多个 provider。
- 不在用例中写 HTTP 接口或前端状态。
- 不绕过测试运行器直接写结果索引。

# 规范

- 每个文件只负责一个工具。
- 准备阶段只操作本次 attempt 的隔离目录。
- 断言应检查真实工具调用结果，不以模型正文自述为成功依据。

