# 目录用途

存放仅供测试运行时显式启用的后端测试基础设施，不承载产品业务逻辑。

## 可修改内容

- 测试用模型 transport、录制回放、资产加载和测试上下文。
- 只在明确测试配置启用时参与应用启动的测试支持代码。

## 不可修改内容

- 不放生产模型策略、业务事件转换或用户数据持久化逻辑。
- 不得在没有显式测试配置时改变生产模型调用路径。

## 规范

- 组件必须快速失败，错误包含配置、资产和请求上下文。
- 不得写入用户全局配置、工作区 `.boxteam/` 或项目根目录运行产物。
- 新增子目录必须继续提供对应 `AGENTS.md`。
