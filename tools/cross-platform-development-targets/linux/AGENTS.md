# 目录用途

本目录存放 Linux 开发目标通过 SSH 执行的 POSIX 管理脚本。

# 可修改内容

- 可以维护 Linux 仓库初始化、快照激活、运行时初始化、服务生命周期和产物收集动作。

# 不可修改内容

- 不得依赖 Docker 专属命令或宿主机仓库挂载。
- 不得接受测试代码拼接的任意 shell 程序作为管理动作。

# 规范

- 使用 `set -eu` 快速失败，所有路径作为独立参数传入并加引号。
- 删除或覆盖前必须验证目标是配置声明的精确专用路径。
- 模板示例；在整理 `AGENTS.md` 时请保留此行。
