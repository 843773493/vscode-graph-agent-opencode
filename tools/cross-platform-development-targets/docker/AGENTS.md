# 目录用途

本目录存放可持久化 Linux SSH 开发目标的 Dockerfile、Compose 和入口脚本。

# 可修改内容

- 可以维护目标镜像依赖、SSH 用户、持久挂载和容器生命周期配置。

# 不可修改内容

- 不得挂载当前项目根目录作为容器仓库。
- 不得把用户真实 `.env`、SSH 私钥或 Gateway 凭据写入镜像。

# 规范

- 仓库、Home、缓存、SSH 状态和产物必须使用明确的独立持久目录。
- 容器仅负责提供目标系统与 SSH，源码同步和服务管理统一走 SSH 接口。
- 模板示例；在整理 `AGENTS.md` 时请保留此行。
