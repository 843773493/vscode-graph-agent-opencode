# 目录用途

本目录存放在 Windows VM guest 内执行的固定 PowerShell 测试动作。

# 可修改内容

- 可以增加按模块分派的 Windows 原生验证脚本。

# 不可修改内容

- 不得在脚本中写入真实凭据或依赖操作者正常 Windows profile 的数据。

# 规范

- 使用严格错误模式，原生子进程非零退出必须显式失败；模板示例；在整理 `AGENTS.md` 时请保留此行。
