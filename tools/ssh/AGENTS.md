# 目录用途

本目录存放 Windows VM 远程连接所需的本地私钥、host key 和未提交配置。

# 可修改内容

- 可以放置当前开发机专用的 SSH 私钥、known_hosts 和远端配置。
- 可以维护不包含真实凭据的 `.example` 配置模板。

# 不可修改内容

- 不得提交私钥、密码、token 或真实连接配置。
- 不得把该目录中的凭据复制到源码、日志或测试产物中。

# 规范

- 真实私钥必须使用仅当前用户可读的权限。
- 所有文件默认由仓库根目录 `.gitignore` 排除；模板示例；在整理 `AGENTS.md` 时请保留此行。
