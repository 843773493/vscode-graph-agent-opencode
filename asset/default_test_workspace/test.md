# test.md

这是一个用于 agent 模式示例的统一测试工作区。

目标不是一次性完成全部修改，而是演示完整的迭代链路：

1. 先用 `glob` 找文件。
2. 再用 `grep` 找关键标记。
3. 用 `read` 看上下文。
4. 选择 `edit`、`patch` 或 `multiedit` 修复问题。
5. 修改后查看 diagnostics，再决定是否继续。
6. 必要时用快照撤销。

当前工作区刻意保留了几个类型错误，方便观察诊断回传。
