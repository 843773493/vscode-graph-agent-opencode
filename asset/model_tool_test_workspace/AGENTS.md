# 目录用途

`asset/model_tool_test_workspace/` 存放模型工具调用测试的只读种子资源。

# 可修改内容

- 可以为新工具添加独立测试资源目录和小型输入文件。

# 不可修改内容

- 不存放测试运行结果。
- 不存放 API key、用户配置或用户工作区数据。

# 规范

- 运行时必须先复制资源到工作区 `.boxteam/tool_tests/` 后再修改。
- 资源应保持小型、可读、结果可确定。

