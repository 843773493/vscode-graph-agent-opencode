# tests/unit/configs

## 目录用途

存放独立 `configs/` Python 源根的安装、布局和迁移单元测试。

## 可修改内容

- 配置安装器、路径布局和配置迁移测试。
- 测试专用配置样例与 fixture。

## 不可修改内容

- 不写入真实用户的 `${BOXTEAM_HOME:-~/.boxteams}/`。
- 不将配置测试归入无关的 `app/` 模块。

## 规范

- 配置文件读写必须使用 pytest 临时目录。
- 迁移测试必须断言最终权威布局，不保留兼容入口。
