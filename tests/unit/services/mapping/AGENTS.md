# tests/unit/services/mapping

## 目录用途

存放纯映射与结构转换逻辑的单元测试。

## 可修改内容

- 新增、修改、删除 mapper 单元测试。
- 为输入输出结构转换补充边界用例。

## 不可修改内容

- 不要测试业务流程、持久化或外部系统调用。
- 不要依赖真实 Agent 或 LLM。

## 规范

- 测试应聚焦输入到输出的确定性映射。
- 映射失败或不支持的输入要有明确断言。
