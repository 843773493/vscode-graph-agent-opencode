# tests/unit/services/orchestration

## 目录用途

存放编排与流程控制服务的单元测试，例如任务执行、事件录制等。

## 可修改内容

- 新增、修改、删除编排服务的单元测试。
- 使用 fake/stub 替换下游依赖。

## 不可修改内容

- 不要测试具体业务规则。
- 不要引入真实 Agent 运行时或 LLM 调用。

## 规范

- 依赖注入优先使用 fixture 或构造函数传入 fake 实现。
- 异步流程应明确验证事件顺序和副作用。
