# tests/unit/schemas

## 目录用途

存放 `app/schemas/` 公共数据模型和事件模型的单元测试。

## 可修改内容

- Schema 校验、序列化和兼容约束测试。
- 子版本 schema 分区及其测试说明。

## 不可修改内容

- 不在此测试业务服务行为。
- 不启动 HTTP 服务验证完整协议链路。

## 规范

- 测试必须覆盖合法输入和明确的非法输入。
- 断言应针对公开字段及稳定序列化结果。
