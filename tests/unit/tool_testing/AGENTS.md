# 目录用途

`tests/unit/tool_testing/` 存放模型工具测试框架及各用例的单元测试。

# 可修改内容

- 可以新增用例准备、真实工具执行、结果评估和注册行为测试。

# 不可修改内容

- 不调用真实 LLM 或外部 provider。
- 不写入仓库内的模型测试运行目录。

# 规范

- 测试依赖通过 pytest fixture 注入。
- 工具执行必须使用临时工作区，并断言真实文件结果。
