# 目录用途

`tests/unit/gateway/runtime/` 存放 Gateway 工作区进程生命周期与进程日志基础设施的单元测试。

## 可修改内容

- 进程启动、停止、健康检查和日志文件管理的单元测试。
- 相关测试辅助对象与 fixture。

## 不可修改内容

- 不启动依赖真实用户配置的长期后台进程。
- 不把测试日志写入真实 Gateway 控制面目录。

## 规范

- 文件系统测试统一使用 pytest 临时目录。
- 权限断言需要按操作系统能力显式跳过不适用平台。
