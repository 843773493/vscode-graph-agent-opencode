# 目录用途

存放跨进程、跨语言稳定协议的契约测试，包括 API、客户端协议和工作区辅助服务协议。

## 可修改内容

- schema、生成类型、请求/响应、SSE、认证和代理边界测试。

## 不可修改内容

- 不要验证完整用户流程；这类测试属于 Integration 或 E2E。
- 不要用客户端截图代替协议断言。

## 规范

- 契约断言必须面向公开边界，不依赖内部类或私有实现。
- 失败必须明确指出不兼容字段或行为。
