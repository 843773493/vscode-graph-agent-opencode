# 目录用途

存放工作区后端相关的跨模块集成测试。

## 可修改内容

- Agent、Session 和后端服务之间的集成测试。
- 后端集成测试专用 fixture 与受控模型服务替身。

## 不可修改内容

- 单个后端类或函数的单元测试。
- 不使用服务替身的完整后端产品 E2E。
- Gateway、客户端和生产代码。

## 规范

- 优先通过真实 HTTP 后端进程验证边界。
- 模型替身必须明确记录请求并返回确定性协议响应。
- 工作区、配置、端口和进程必须按测试文件隔离。
