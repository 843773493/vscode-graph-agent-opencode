# 目录用途

存放 Session、Job、模型协议和配置迁移之间的集成测试。

## 可修改内容

- 真实后端进程配合本地模型协议桩的 Session 流程测试。
- Session 生成、重试和上下文迁移的跨模块测试。

## 不可修改内容

- 单个 Session service 的单元测试。
- 使用真实外部 Provider 的验收 E2E。

## 规范

- 每个测试文件使用独立模型端口、后端端口和工作区。
- 模型桩必须校验请求协议和认证信息，并保留可审计请求状态。
