# 目录用途

存放客户端与真实 Gateway/后端以及受控服务之间的集成测试。

## 可修改内容

- 浏览器客户端、Gateway 和后端之间的集成测试。
- 客户端集成测试专用的模型或 HTTP 服务替身。

## 不可修改内容

- 纯 React 组件或状态 reducer 单元测试。
- 关键依赖全部真实的完整产品 E2E。

## 规范

- 浏览器测试必须验证真实 HTTP 请求和页面行为。
- 被替换的服务边界必须在测试名称或 fixture 中明确。
- 二进制、截图和指标只写入对应的 `out/tests/integration/` 目录。
