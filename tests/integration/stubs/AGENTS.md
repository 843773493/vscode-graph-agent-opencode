# 目录用途

集中存放 Integration 使用的模型、Gateway target、MCP 和网页协议替身。

## 可修改内容

- 明确、确定性、无外部副作用的测试替身和请求记录器。

## 不可修改内容

- `tests/e2e/` 不得导入本目录。
- 不要伪造被测生产模块本身。

## 规范

- 类型和命名必须包含 `stub`/`fake` 等明确语义。
- 替身不得被生产代码导入，也不得静默替代真实依赖。
