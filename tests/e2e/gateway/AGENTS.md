# 目录用途

存放 Gateway、多工作区、SSH 转发和测试容器相关的端到端测试与辅助代码。

# 可修改内容

- Gateway e2e 测试文件。
- 启动/停止测试 SSH 容器和远端后端的测试辅助代码。

# 不可修改内容

- 不放置与 Gateway 路由无关的浏览器或终端工具测试。
- 不把测试容器运行产物写入项目根目录。

# 规范

- 测试端口必须由 e2e 端口分配工具管理。
- 容器、SSH 隧道和后端进程退出时必须清理。
