# 目录用途

存放浏览器扩展工具、可附加浏览器资源和浏览器管理器相关的端到端测试。

# 可修改内容

- 浏览器工具链 e2e 测试。
- 本目录内测试使用的 fixture 和测试辅助函数。

# 不可修改内容

- 不放置与浏览器工具无关的 gateway、terminal 或通用 agent e2e。
- 不依赖跨文件共享的可变状态完成测试。

# 规范

- 每个测试文件应独立准备工作区、端口和运行进程。
- 临时页面优先使用自包含 `data:` URL，避免依赖不可见的本地临时端口。
