# 目录用途

存放测试套件注册、能力矩阵和统一命令入口，不包含具体测试实现。

## 可修改内容

- `matrix.jsonc`、ESM runner、套件过滤和结构化汇总。

## 不可修改内容

- 不要隐藏失败、吞掉退出码或把未满足前置条件记为通过。
- 不要让 E2E 套件回退执行 Integration stub 套件。

## 规范

- 所有 JavaScript 使用 ESM 并通过 Bun 执行。
- 矩阵显式声明层级、客户端、运行面、命令和真实前置条件。
